---
phase: quick-260814-6n1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/assets/brand/logo-full.png
  - src/lib/assets/brand/logo-mark.png
  - static/favicon.png
  - static/apple-touch-icon.png
  - static/favicon.svg
  - src/lib/components/Header.svelte
  - src/lib/components/Footer.svelte
  - src/app.html
  - static/site.webmanifest
  - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
  - logo-bg.png
autonomous: true
requirements: [SITE-03, SITE-04, A11Y-01]

must_haves:
  truths:
    - "A visitor sees the real circular brand emblem in the sticky header, at 52px, with no yellow accent circle behind it."
    - "A visitor sees the full brand lockup (emblem plus wordmark) on a white card in the footer brand column, legible against brand-blue."
    - "The browser tab, bookmark bar, and iOS home screen show the brand emblem, not the old placeholder icon."
    - "The header link still announces itself as the two-line Polish wordmark text: the images add no duplicate accessible name."
    - "The repo root no longer carries the delivered source file logo-bg.png, and the concept sheet logo.png plus the design zips are untouched."
  artifacts:
    - "src/lib/assets/brand/logo-full.png (alpha-trimmed full lockup)"
    - "src/lib/assets/brand/logo-mark.png (alpha-trimmed emblem, roughly square)"
    - "static/favicon.png (512x512, transparent, emblem centered)"
    - "static/apple-touch-icon.png (180x180, white background, emblem at about 80 percent)"
    - ".planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md Amendment v1.3"
  key_links:
    - "Header.svelte imports logo-mark.png?enhanced and renders it through enhanced:img (vite.config.ts runs enhancedImages() before sveltekit())."
    - "Footer.svelte imports logo-full.png?enhanced and renders it inside a white rounded card in the brand column."
    - "src/app.html and static/site.webmanifest reference only the two PNG icons after the placeholder SVG is removed."
---

<objective>
Replace the placeholder brand marks across the site with the client's delivered logo: the real emblem in the header, the full lockup in the footer, and a regenerated favicon set.

Purpose: the site currently ships a bespoke IconBear placeholder badge and a scaffold icon set. The client has delivered the final brand asset, so every brand surface should show the real identity before Phase 4 work continues.
Output: two optimized brand assets under src/lib/assets/brand/, a regenerated favicon pair under static/, wired Header and Footer components, cleaned icon references, a UI-SPEC amendment, and removal of the now-integrated source file from the repo root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

@src/lib/components/Header.svelte
@src/lib/components/Footer.svelte
@src/lib/components/NewsCard.svelte
@src/app.html
@static/site.webmanifest
@vite.config.ts
</context>

<key_facts>
Verified during planning, treat as given (do not re-derive):

- `logo-bg.png` at repo root is 1079x514, PNG, 4 channels (RGBA with a transparent background). `sharp` loads fine in this repo via dynamic ESM import: `const sharp = (await import('sharp')).default`. Note that `require('sharp/package.json')` throws (blocked subpath), so do not probe the version that way.
- `sharp` is present only as a transitive dependency of `@sveltejs/enhanced-img`. It is used here by a throwaway script only. Do NOT add `sharp` to `package.json` and do NOT commit the script.
- The in-repo `enhanced:img` styling convention is a wrapper element plus a `:global(img)` rule (see `NewsCard.svelte`: `.cover :global(img) { display: block; ... }`). Follow it.
- Static `?enhanced` imports type-check without extra config: `@sveltejs/enhanced-img/types/index.d.ts` pulls in `ambient.d.ts`, which declares `module '*?enhanced'`, and `NewsCard.svelte` already imports `Picture` from that package so the ambient module is in the program.
- `.prettierignore` ignores `/static/` and `/.planning/`. So `static/site.webmanifest` keeps its existing 2-space indentation (do NOT retab it), and UI-SPEC edits are not reformatted. `src/app.html` IS formatted by prettier and uses tabs.
- No Playwright spec asserts on the header badge, on `IconBear`, or on a global image count. The only image assertion is `tests/o-nas.spec.ts:64`, scoped to `section[aria-labelledby="obiekt-heading"] img`, which neither header nor footer touches. Re-run the grep sweep in Task 3 to confirm, but expect no test edits.
- `static/favicon.svg` is referenced in exactly two places: `src/app.html:7` and `static/site.webmanifest` icons[0]. The comment at `src/routes/+layout.svelte:2` says "static/favicon.*" and needs no change.
- Design tokens available: `var(--color-surface)` (white surface), `var(--radius-sm)` (the radius used by the footer `.logo-slot` chips), `var(--color-accent)`, `var(--color-accent-active)`.
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: Generate and visually verify the brand assets and favicon set</name>
  <files>src/lib/assets/brand/logo-full.png, src/lib/assets/brand/logo-mark.png, static/favicon.png, static/apple-touch-icon.png</files>
  <action>
Create the directory `src/lib/assets/brand/`.

Write a throwaway ESM script at the session scratchpad path `${SCRATCHPAD}/gen-brand-assets.mjs` (the scratchpad directory given in your environment). Do NOT create it inside the repo: `eslint .` and `prettier --check .` scan repo source and would fail on it, and it must not be committed.

The script loads sharp via `const sharp = (await import('sharp')).default;` and reads `logo-bg.png` from the repo root. It produces four outputs:

1. `src/lib/assets/brand/logo-full.png` — the whole lockup with the transparent border trimmed away. Use sharp `.trim()` (threshold 0) so the top-left transparent pixel defines the background, and keep the alpha channel (do not flatten).

2. `src/lib/assets/brand/logo-mark.png` — the circular emblem alone. Extract the left region of the source (`.extract({ left: 0, top: 0, width: 550, height: 514 })`) and then `.trim()` to alpha. The emblem including its sun rays sits roughly in that region, so the trimmed result should come out roughly square.

3. `static/favicon.png` — 512x512, transparent background, emblem centered. Resize `logo-mark.png` with `fit: 'contain'` and a fully transparent background (`{ r: 0, g: 0, b: 0, alpha: 0 }`).

4. `static/apple-touch-icon.png` — 180x180, opaque white background, emblem at about 80 percent with safe-zone padding (the manifest marks this icon `maskable`). Resize the emblem to 144x144 `contain` on white, then extend by 18px on all four sides with a white background, so the emblem occupies the middle 80 percent.

Run the script with `node`.

Then VISUALLY VERIFY before anything is wired in: use the Read tool on `src/lib/assets/brand/logo-mark.png` and on `src/lib/assets/brand/logo-full.png` and look at them.
  - `logo-mark.png` must show the complete circular emblem with every sun ray intact (no ray clipped at the right edge) and NO fragment of a wordmark letter.
  - `logo-full.png` must show the emblem plus the complete wordmark "Żłobek Gminny w Stromcu" with no clipped glyph.
If the mark is clipped or carries a stray letter, adjust the extract width (try values between 500 and 600), re-run, and Read again. Iterate until both crops are clean. Also Read `static/apple-touch-icon.png` once to confirm the white plate reads correctly with the emblem inset.

Do not modify `logo.png` (the concept sheet) and do not modify the design zip files at the repo root. `logo-bg.png` stays in place for now: Task 3 removes it.
  </action>
  <verify>
    <automated>node -e "import('sharp').then(async m=>{const s=m.default;const q=async p=>{const d=await s(p).metadata();return {p,w:d.width,h:d.height,ch:d.channels};};const r=await Promise.all(['src/lib/assets/brand/logo-full.png','src/lib/assets/brand/logo-mark.png','static/favicon.png','static/apple-touch-icon.png'].map(q));console.log(JSON.stringify(r,null,1));const f=r[0],mk=r[1],fav=r[2],ati=r[3];const ratio=mk.w/mk.h;if(!(ratio>0.85&&ratio<1.18))throw new Error('mark not roughly square: '+ratio);if(!(f.w/f.h>1.6))throw new Error('full lockup not horizontal');if(f.ch!==4||mk.ch!==4)throw new Error('brand assets lost alpha');if(fav.w!==512||fav.h!==512||fav.ch!==4)throw new Error('favicon.png must be 512x512 RGBA');if(ati.w!==180||ati.h!==180)throw new Error('apple-touch-icon must be 180x180');const stats=await s('static/apple-touch-icon.png').stats();if(stats.isOpaque!==true)throw new Error('apple-touch-icon must be opaque white-backed');console.log('OK');})"</automated>
    <human-check>Both generated brand PNGs were opened with the Read tool and inspected: emblem complete with all rays, no stray wordmark letters in the mark, no clipped glyphs in the full lockup.</human-check>
  </verify>
  <done>All four image files exist with the asserted dimensions and alpha properties, the two brand assets were visually inspected and are clean crops, and no generator script was added to the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the emblem into the header and the full lockup into the footer</name>
  <files>src/lib/components/Header.svelte, src/lib/components/Footer.svelte</files>
  <action>
**Header.svelte** (UI-SPEC v1.2 section 3 amendment):

In the script block, remove the `IconBear` import and add `import logoMark from '$lib/assets/brand/logo-mark.png?enhanced';`. Leave the `page`, `navLinks`, and `MobileNav` imports, the `isActive` helper, and the scroll-shadow state untouched.

In the markup, replace the placeholder span that wraps `IconBear` with a wrapper span carrying a new class (for example `brand-mark`) containing `<enhanced:img src={logoMark} alt="" sizes="52px" />`. The emblem is a full-colour, self-contained circular mark, so drop the accent-circle background, the icon colour tokens, and the hard `box-shadow` entirely. The image is decorative: `alt=""` is required, because the two-line wordmark text remains the accessible name of the link. Keep the two-line wordmark markup and its text byte-identical.

In the style block, delete the old placeholder badge rule and add the wrapper rules following the `NewsCard.svelte` convention:
  - wrapper: `flex: none; display: flex;`
  - `:global(img)` inside the wrapper: `display: block; height: 52px; width: auto;`
Keep the sticky positioning, the 4px accent bottom border, the scroll shadow, the 44px minimum link height, and every nav rule exactly as they are.

Update the component's head comment so it describes the real emblem instead of a placeholder badge. No em dashes and no emoji in any comment or copy.

**Footer.svelte**:

In the script block add `import logoFull from '$lib/assets/brand/logo-full.png?enhanced';`.

In the brand column, immediately ABOVE the `.footer-wordmark` paragraph, insert a wrapper div (for example `brand-lockup`) containing `<enhanced:img src={logoFull} alt="" sizes="280px" />`. The lockup sits next to the existing wordmark text, so it is decorative: `alt=""`.

Style the wrapper as a white card so the lockup's blue and orange wordmark stays legible on brand-blue:
  - `background: var(--color-surface);`
  - `border-radius: var(--radius-sm);` (matches the existing `.logo-slot` chips)
  - modest padding, for example `10px 12px`
  - `max-width: 280px;` and `margin: 0 0 12px;`
  - `:global(img)` inside the wrapper: `display: block; width: 100%; height: auto;`
Leave the wave divider, all four columns, the plain-text contact lines, the placeholder logo slots, the BIP link contract, and the copyright line untouched.

If `svelte-check` reports that the `?enhanced` module has no type declaration (it should not, see key_facts), fix it by adding `/// <reference types="@sveltejs/enhanced-img" />` at the top of `src/app.d.ts` rather than by casting or by using `any`.
  </action>
  <verify>
    <automated>grep -c 'enhanced:img' src/lib/components/Header.svelte && grep -c 'enhanced:img' src/lib/components/Footer.svelte && grep -c 'logo-mark.png?enhanced' src/lib/components/Header.svelte && grep -c 'logo-full.png?enhanced' src/lib/components/Footer.svelte && grep -c 'wordmark-tagline' src/lib/components/Header.svelte && npm run check && npm run lint</automated>
  </verify>
  <done>Header renders the real emblem at 52px with no accent circle and no hard shadow, footer renders the full lockup on a white rounded card above the wordmark text, both images are decorative with empty alt, and `npm run check` plus `npm run lint` pass (the removed IconBear import leaves no unused binding).</done>
</task>

<task type="auto">
  <name>Task 3: Retire the placeholder icon, amend the UI-SPEC, and remove the delivered source file</name>
  <files>src/app.html, static/site.webmanifest, static/favicon.svg, logo-bg.png, .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md</files>
  <action>
**Icon references.** The old scaffold vector icon cannot be regenerated from a raster source, so retire it:
  - Delete the file `static/favicon.svg` with `git rm`.
  - In `src/app.html`, delete the vector icon `<link rel="icon" ...>` line (line 7). Keep the 512x512 PNG icon link, the apple-touch-icon link, the manifest link, and the theme-color meta. Preserve tab indentation (prettier formats this file).
  - In `static/site.webmanifest`, remove the first entry from the `icons` array so exactly two entries remain: the 512x512 PNG (`purpose: any`) and the 180x180 apple-touch-icon (`purpose: maskable`). PRESERVE the existing 2-space indentation: `/static/` is listed in `.prettierignore`, so do not retab this file and do not reformat it.

**Test sweep.** Run a grep over `tests/` for assertions that could be invalidated by the two new images (search for the old badge class, `IconBear`, icon file names, and unscoped `img` locators or counts). Planning found none, so expect no changes. If the sweep does turn up an assertion that now fails, update it in lockstep rather than weakening it.

**UI-SPEC amendment.** Append a short `## Amendment v1.3 (2026-08-14): real brand assets` section to the END of `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md`, matching the terse prose style of the existing v1.1 and v1.2 amendments. Record exactly these four points:
  1. Header: the real circular brand emblem (`src/lib/assets/brand/logo-mark.png`, rendered through `enhanced:img` at 52px height) replaces the 52px accent-circle IconBear placeholder badge. The accent circle background and its hard shadow are gone: the emblem is a full-colour self-contained mark placed directly on the header surface. The two-line wordmark stays the accessible name of the link, so the image is decorative with an empty alt. Sticky header, 4px accent bottom border, and the nav chip contract are unchanged.
  2. Footer: the full lockup (`src/lib/assets/brand/logo-full.png`) is added to the top of the brand column on a white card (surface background, radius-sm to match the logo slots, max-width 280px), so the lockup's blue and orange wordmark stays legible on brand-blue. Decorative, empty alt, next to the existing white wordmark text.
  3. Favicons: `static/favicon.png` (512x512, transparent) and `static/apple-touch-icon.png` (180x180, white plate, emblem at about 80 percent for the maskable safe zone) are regenerated from the emblem. The placeholder vector icon is retired and its references are removed from `src/app.html` and `static/site.webmanifest`.
  4. IconBear remains in `src/lib/icons/` and the bespoke icon contract (v1.2 section 7) is otherwise unchanged: only the logo badge use is superseded.
No emoji and no em dashes: use commas, colons, and parentheses. En dash only inside numeric ranges.

**Source file cleanup.** Delete `logo-bg.png` from the repo root with `git rm` (its content now lives in `src/lib/assets/brand/`). Leave `logo.png` (the concept sheet, which must never be committed) and the design zip files at the root exactly as they are: when staging, add files by explicit path, never `git add -A` or `git add .`.
  </action>
  <verify>
    <automated>test ! -f static/favicon.svg && test ! -f logo-bg.png && test -f logo.png && test "$(grep -c 'rel="icon"' src/app.html)" = "1" && test "$(grep -c 'apple-touch-icon' src/app.html)" = "1" && node -e "const m=JSON.parse(require('fs').readFileSync('static/site.webmanifest','utf8'));if(m.icons.length!==2)throw new Error('expected 2 icons, got '+m.icons.length);if(!m.icons.every(i=>i.type==='image/png'))throw new Error('non-png icon entry remains');console.log('manifest OK')" && grep -c 'Amendment v1.3' .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md && git status --porcelain | grep -v 'logo.png' | grep -v '.zip' && npm run check && npm run lint && npm run test:unit && npm run build</automated>
  </verify>
  <done>Only the two PNG icons are referenced from `src/app.html` and `static/site.webmanifest`, the placeholder vector icon and the delivered source file are removed from the repo, the concept sheet and the design zips are still untracked and untouched, the UI-SPEC carries Amendment v1.3, and `npm run check`, `npm run lint`, `npm run test:unit` all pass with `npm run build` exiting 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client deliverable to repo | A binary asset supplied by the client crosses into version control and into the build pipeline. |
| repo to public deploy | Root-level working files (concept sheet, design zips) could be published if staged carelessly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q6N1-01 | Tampering | `logo-bg.png` client deliverable | low | mitigate | Never ship the raw delivered file. Task 1 re-encodes it through sharp (decode plus re-encode strips any non-image ancillary payload) and asserts dimensions and channel count, and Task 3 removes the original from the repo. |
| T-Q6N1-02 | Information disclosure | repo root working files | medium | mitigate | Task 3 stages by explicit path only, never `git add -A`. The verify gate asserts `logo.png` still exists untracked and the design zips are untouched, so the unreleased concept sheet is never published. |
| T-Q6N1-03 | Denial of service | `enhanced:img` build step | low | accept | Assets are processed at build time only, not per request. Source is roughly 1079x514, well inside normal build budgets. |
| T-Q6N1-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs NOTHING. `sharp` is already present as a transitive dependency of `@sveltejs/enhanced-img` and is used only by an uncommitted one-off script. No package legitimacy checkpoint is required because no package-manager install task exists. Do not add `sharp` to `package.json`. |
</threat_model>

<verification>
Full gate, all must pass from the repo root:

1. `npm run check` (wrangler types plus svelte-check: types and a11y)
2. `npm run lint` (`prettier --check .` then `eslint .`)
3. `npm run test:unit`
4. `npm run build` exits 0
5. `git status --porcelain` shows `logo.png` and the design zips still untracked and unmodified

Visual confirmation (Read the generated PNGs during Task 1, and spot-check the rendered pages if a dev server is already running):
- Header emblem is complete, sits at 52px, sits directly on the white header surface with no yellow circle behind it, and the two-line wordmark is unchanged.
- Footer lockup is fully legible on the brand-blue block thanks to the white card.
</verification>

<success_criteria>
- Real brand emblem in the header at 52px, no accent circle, no hard shadow, wordmark text unchanged and still the link's accessible name.
- Full brand lockup on a white rounded card at the top of the footer brand column, max-width about 280px, responsive.
- `static/favicon.png` (512x512 transparent) and `static/apple-touch-icon.png` (180x180 white plate) regenerated from the emblem; placeholder vector icon deleted and de-referenced from `src/app.html` and `static/site.webmanifest`.
- Both new images are decorative (`alt=""`), so WCAG 2.1 AA is preserved and axe reports no new violations.
- `01-UI-SPEC.md` carries Amendment v1.3 recording the three surface changes and confirming IconBear stays in `src/lib/icons/`.
- `logo-bg.png` removed from the repo root; `logo.png` and the design zips untouched; no generator script committed; `package.json` unchanged.
- Full verification gate passes.
</success_criteria>

<output>
Create `.planning/quick/260814-6n1-implement-new-brand-logo-across-header-f/260814-6n1-SUMMARY.md` when done.
</output>
