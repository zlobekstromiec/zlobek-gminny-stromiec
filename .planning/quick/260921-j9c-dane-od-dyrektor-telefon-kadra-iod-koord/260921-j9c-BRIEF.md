# Brief: dane od dyrektor z 2026-09-21 (e-mail + 14 załączników + 2 dodatkowe pliki)

Source: an e-mail from the director (Kamila Dobosz) received 2026-09-21, with documents
prepared by the IOD, plus a second e-mail replacing `09_Piktogram` and adding
`Druk rezygnacji`. The raw inbox sits UNTRACKED in `docs/documents/` (do not commit it;
copy what is published into `static/dokumenty/` under ASCII slugs). Text extractions of
every file are in the scratchpad:
`/private/tmp/claude-501/-Users-devopsdom-src-client-zlobekstromiec/05755785-08c0-4237-b2ae-1262c490eef2/scratchpad/docs/`
(`zip/` = extracted originals with correct Polish names, `wniosek/` = the inner zip,
`txt/` = plain-text conversions). Re-extract with Python `zipfile` decoding names as
cp437→cp1250 if that directory is gone; `unzip` mangles the Polish names.

## The director's e-mail, verbatim facts

- **Telefon do żłobka: 500 051 228** (an official line; the 2026-08-18 removal was of her
  PRIVATE mobile, see the comment block in `src/lib/content/site.ts:57-71`).
- **Kadra** (exactly as written, in this order):
  Dyrektor – mgr Kamila Dobosz · Opiekun – Agnieszka Bernaciak · opiekun – Justyna Kamińska ·
  pomoc – Renata Rumniak-Cyngot.
- **Do NOT publish** information about holidays (wakacje), number of groups, or age
  brackets of groups. (Audit found none of this in `src/` today; confirm and leave it so.)
- **Dostępność architektoniczna** (Publiczny Żłobek w Stromcu, ul. Radomska 72, 26-804 Stromiec):
  osoby na wózku mogą wjechać do budynku, wejście główne z podjazdem; drzwi szklane; brak
  windy, brak schodów; jest toaleta dla osób z niepełnosprawnościami; jest parking dla osób z
  niepełnosprawnościami; brak tyflomap; pomieszczenia nie są oznaczone tabliczkami z
  wypukłymi cyframi i alfabetem Braille'a; brak dźwiękowego systemu nawigacji dla osób
  niewidomych; brak możliwości wstępu z psem asystującym.
- **Koordynator dostępności podmiotu publicznego: Ewelina Remion, tel. 664 784 676.**
- **Dostępność komunikacyjno-informacyjna:** brak możliwości skorzystania z tłumacza języka
  migowego; w budynku nie ma pętli indukcyjnych.
- **Fees ("poprawka w kwocie"):** „kwotę rodzic ma obniżoną o 837 zł z programu Aktywny
  Maluch, oraz może starać się w ZUS o dopłatę 1500 zł z programu Aktywnie w Żłobku. Łączna
  kwota pokrywa czesne w wysokości 2337 zł."
- Document **08 (Zapisy kafel RODO)** is to be pasted DIRECTLY on the site; the rest of the
  RODO documents as downloadable attachments under it.

## Facts from the IOD documents

- **IOD: Michał Paprocki, e-mail `iod.zlobek@ugstromiec.pl`** (docs 06, 07, 08, 09). The site
  today carries `iod@ugstromiec.pl` (`site.ts:88`) and no name. This is the name the letter
  of 2026-09-01 asked for (art. 11 ustawy z 10.05.2018).
- **Administrator** in every IOD document: „Publiczny Żłobek w Stromcu, ul. Radomska 72,
  26-804 Stromiec". This settles the open question from the 2026-09-01 letter (item 2): use
  this wording everywhere the site names the administrator.
- Doc 08 also gives the administrator contact for RODO requests: `publicznyzlobek@ugstromiec.pl`
  and the postal address.
- **Discrepancy to flag, not resolve:** klauzula 07 says recordings are kept **7 days**,
  the updated piktogram 09 says **5 days**. Publish both files as delivered; do not write a
  retention figure into site prose. Note it in SUMMARY for the user to ask the director.
- **wniosek.zip is unusable:** `wniosek do żłobka-1.doc` and Załączniki 1–6 are templates
  of Publiczny Żłobek w **Starej Błotnicy** (2025/2026). Do NOT publish any of them. Keep the
  existing placeholder `rekrutacja-wniosek.json` + stub file as is. Flag in SUMMARY.
- **Druk rezygnacji** OUTSIDE the zip (`docs/documents/Druk rezygnacji - żłobek.doc`) is the
  corrected Stromiec version (rok 2026/2027, adres Radomska 72). The copy inside wniosek.zip
  is the Stara Błotnica one. Publish only the outside one.
- **09_Piktogram** OUTSIDE the zip (`docs/documents/09_Piktogram_monitoring_wizyjny.docx`)
  is the updated version (IOD e-mail filled in, 5 dni). Publish only that one.
- **statut.pdf** (3 MB, 6 pages) is the real statute: uchwała nr XXIII.133.2026 Rady Gminy
  Stromiec z dnia 26 marca 2026 r. It replaces the 623-byte stub `static/dokumenty/statut-zlobka.pdf`.
- No „Regulamin rekrutacji" was delivered; `rekrutacja-regulamin.json` stays placeholder.
- Other delivered documents: `REGULAMIN ORGANIZACYJNY Stromiec.docx` (obowiązuje od
  1.09.2026), `Standardy Ochrony Małoletnich … (zaktualizowane) (1).docx` (zarządzenie nr
  2/2026 z 31.08.2026), `Zasady_adaptacji_Publiczny_Zlobek_Stromiec.docx` (od 01.09.2026),
  klauzule 01–07 (kandydat do pracy, konkursy, praktykant/stażysta, umowy cywilnoprawne,
  wolontariat, dzieci/rodzice/opiekunowie, monitoring wizyjny).

## What the site has today (from the audit; file:line are current)

1. **Phone:** no field. Restoring = add `phoneDisplay` + `phoneHref` back to `contact` in
   `src/lib/content/site.ts` and re-enable the surfaces that were emptied on 2026-08-18:
   `ContactAndMap.svelte` (Telefon row, :5,33-34,141), `Footer.svelte:4`, `TopBar.svelte:6`,
   `Hero.svelte:54,59,168` (secondary CTA now „Napisz"), `src/routes/kontakt/+page.svelte:24,63,79`,
   `KontaktForm.svelte:272`, `ZgloszenieForm.svelte:336`, `forms.ts:279-282` (`KOPIA_KONTAKT_BOK`).
   Read each comment block first; they describe what was removed. The number must be
   rendered from `contact.*` only, never as a literal in markup. Tests asserting ABSENCE:
   `tests/home.spec.ts:105-107`, `tests/kontakt.spec.ts:111-115,219-221` — flip them to
   assert presence of exactly one `tel:` link per surface with `href="tel:+48500051228"`
   and visible text „500 051 228". Memory note: the 260901-duo task made the contact card
   single-column from 1024 px because it had THREE rows; a fourth row returns, so check the
   card layout still reads well (screenshot).
2. **Fees:** `src/lib/content/cennik.json` (`stawka: 2337, obnizka: 837, placeholder: true`,
   `zus` sentence without an amount), reader `src/lib/cennik.ts` (płaci = 1500), prose
   `src/lib/content/cennik.ts` (no figures by HARD RULE 1), FeeBox via `rekrutacja.ts:152-168`,
   homepage tile `src/lib/w-skrocie.ts:257`. The arithmetic already matches the director;
   what is missing is the explicit **1500 zł** ZUS amount and the explicit statement that
   837 + 1500 = 2337 covers the whole fee. Interpretation to implement: on every surface
   that states the fee, the parent reads the three figures coherently: czesne 2 337 zł,
   obniżka 837 zł z programu „Aktywny Maluch" (automatic, no application), rodzic płaci
   1 500 zł, and a ZUS dopłata of **1 500 zł** from „Aktywnie w żłobku" on the parent's
   application can cover that remainder in full. Put the 1500 amount into the editor-owned
   `zus` sentence in `cennik.json` (the store is the single source; prose must not hard-code
   figures). Since the director has now confirmed the amounts in writing, flip
   `cennik.json` `placeholder` to `false` (the `// PLACEHOLDER:` comments about wyżywienie
   detail and płatności in `cennik.ts` stay, they are separate unknowns). Keep the validator's
   `MARKER_ZUS='Aktywnie w żłobku'` rule satisfied. Flag the interpretation in SUMMARY.
3. **Documents:** store `src/lib/content/dokumenty/*.json` (`nazwa, kategoria, plik, wersja
   DD.MM.YYYY, zrodlo_bip?, placeholder`), categories `src/lib/kategorie-dokumentow.ts:26-33`
   (`rekrutacja|statut|rodo`), labels `src/lib/server/dokumenty.ts:50-53` (`RODO` label
   exists, band is empty today), file rules `src/lib/pliki.ts` (pdf/doc/docx, 10 MB), public
   page `src/routes/dokumenty/+page.svelte:36-60`, admin editor + validator, and
   `tests/dokumenty.spec.ts:27-78` which asserts the RODO heading is ABSENT (flip it).
   `tests/zastepcze.unit.ts` inventories placeholder flags across all content JSON.
   Target layout (add one category, `organizacja`, label „Organizacja żłobka", between
   statut and rodo, or a better name if the planner prefers; update `KATEGORIE`, `NAGLOWEK`,
   admin `opcjeKategorii`, `docs/instrukcja-cms.md` if it enumerates categories, and tests):
   - rekrutacja: existing wniosek (placeholder stays), existing regulamin (placeholder stays),
     **Druk rezygnacji** (.doc, wersja = date of the e-mail 21.09.2026).
   - statut: **Statut żłobka** real PDF, `placeholder: false`, wersja 26.03.2026, keep `zrodlo_bip`.
   - organizacja: **Regulamin organizacyjny** (docx, 01.09.2026), **Standardy Ochrony
     Małoletnich** (docx, 31.08.2026), **Zasady adaptacji dziecka** (docx, 01.09.2026).
   - rodo: klauzule 01–07 (docx, wersja 21.09.2026, human-readable names such as
     „Klauzula informacyjna dla kandydata do pracy"), **Piktogram monitoringu wizyjnego**
     (updated docx). Doc 08 is NOT a download: its text becomes the intro of the RODO band
     on `/dokumenty` (heading „RODO", the paragraphs of 08 above the list, IOD and
     administrator e-mails rendered via `contact.*`/`AdresEmail.svelte`, ending with
     „Materiały do pobrania:" then the list). The band intro must be editor-safe: put the
     text in a content module (`src/lib/content/rodo.ts` or similar), not in markup.
   Copy files to `static/dokumenty/` with ASCII kebab-case names. Every href must return
   200 (existing test). Update the homepage/rekrutacja document filters if the new
   category should not leak into them (they filter `kategoria === 'rekrutacja'`, fine).
4. **Kadra:** `src/lib/content/o-nas.json` already lists the four people with three empty
   `rola` values. Set roles as written: Kamila Dobosz → „Dyrektor" (name as „mgr Kamila
   Dobosz" only if the o-nas validator/test accepts it; otherwise keep „Kamila Dobosz" and
   note it), Agnieszka Bernaciak → „Opiekun", Justyna Kamińska → „Opiekun", Renata
   Rumniak-Cyngot → „Pomoc". Order as in the e-mail (dyrektor first). Check
   `tests/o-nas.spec.ts:75-104` and `tests/admin-walidacja-strony.unit.ts`.
5. **IOD:** `site.ts:88` → `iodEmail: 'iod.zlobek@ugstromiec.pl'`, add `iodName: 'Michał
   Paprocki'`. Consumers: `forms.ts:315-325` (name him, drop the IOD half of the PLACEHOLDER
   comment; the koordynator half is also now answered, see 6), `polityka.ts:73` (name him),
   administrator wording in `forms.ts:308` and `polityka.ts:66-77` aligned to „Publiczny
   Żłobek w Stromcu, ul. Radomska 72, 26-804 Stromiec". Tests: `tests/forms-copy.unit.ts:160-168`,
   `tests/polityka-prywatnosci.spec.ts:107-113`. Consider replacing the body of
   `KLAUZULA_ADMINISTRATORA` with the official klauzula 06 (dzieci/rodzice/opiekunowie),
   structured under its own headings (Administrator, Inspektor Ochrony Danych, Cele i
   podstawy, Odbiorcy, Okres przechowywania, Prawa osoby, Podanie danych, Przekazywanie
   poza EOG, Automatyzacja i profilowanie), because it is the IOD's authoritative text for
   exactly that audience; the forms-specific KLAUZULA in forms.ts stays (the IOD documents do
   not cover website forms). Add one sentence on /polityka-prywatnosci pointing to the RODO
   documents on /dokumenty (monitoring klauzula lives there).
6. **Koordynator + dostępność facts:** `src/routes/deklaracja-dostepnosci/+page.svelte` is a
   56-line stub; the full statutory declaration is Phase 6 plans 06-09/06-10 (they expect
   `src/lib/content/deklaracja.json` + `deklaracja.ts`; do NOT pre-build that schema). For
   this task: record the facts in one typed content module (e.g. `src/lib/content/dostepnosc.ts`
   exporting the koordynator contact and the two fact lists verbatim), render them on the
   stub as interim sections („Koordynator dostępności", „Dostępność architektoniczna",
   „Dostępność komunikacyjno-informacyjna") under the existing h1, keep `noindex`, narrow the
   PLACEHOLDER comment to „pełna deklaracja zgodna z Warunkami technicznymi v2.0 w Fazie 6".
   Phone rendered as a `tel:` link with the same formatting rule as the żłobek phone.
   SUMMARY must state that three of the five placeholder classes named in ROADMAP Phase 6
   criterion 2 (koordynator, IOD name, architektoniczna, komunikacyjno-informacyjna → four)
   are now supplied, so 06-09 should read them from this module instead of leaving placeholders.

## Constraints that bite here
- Polish only, no emoji, no em dashes (en dash only in numeric ranges). Every new panel string
  must be in `docs/instrukcja-cms.md` if the manual quotes that screen (`tests/instrukcja.unit.ts`).
- Editorial panel route list: adding a category does not add a route; no `TRASY` change needed.
- Verify: `npm run check && npm run lint && npm run test:unit && npm run test`. Kill a stale
  wrangler on :4173 before trusting Playwright reds. Clean `.playwright-mcp/` and root
  screenshots before `prettier --check .`.
- Do not touch `docs/documents/` (untracked inbox) and do not add it to git.
- Conventional Commits, `type(scope): subject` ≤72 chars, scope `quick-260921-j9c`.
