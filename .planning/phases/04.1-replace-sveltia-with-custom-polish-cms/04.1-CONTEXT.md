# Phase 04.1: Replace Sveltia with custom Polish CMS - Context

**Gathered:** 2026-08-15
**Status:** Ready for planning

<domain>
## Phase Boundary

A custom, Polish-only admin panel at `/admin`, entered with a one-time code sent to the staff member's e-mail, that edits every content type editable today (o-nas, plan dnia, dokumenty, aktualności with cover images) plus the recruitment open/closed switch, persists changes as commits to this repository, and publishes them through the existing Cloudflare Pages rebuild. Sveltia CMS, the `sveltia-cms-auth` Worker and the GitHub OAuth App are removed once the new panel is proven live.

**Requirements in scope:** CMS-01, CMS-02, CMS-03, all three re-scoped by this phase (see D-19).

**Locked upstream, do NOT re-decide:**

- Content stays plain JSON under `src/lib/content/`, read at build time by the existing server readers (05 D-09, ROADMAP SC5).
- Content files keep their current shape so the Phase 2-3 readers, the prerender and the existing test suites pass unchanged (ROADMAP SC5).
- Strict validated fields, never a free-form page body; narrative fields allow bold, links and lists only (02 D-05, D-08).
- Direct publish to main, no draft or editorial workflow (02 D-20).
- Polish across every surface, including labels, hints, validation messages, confirmations, empty states and errors (SITE-06, CMS-03, ROADMAP SC2).
- Panel lives at `/admin`; a Polish instrukcja ships with the phase (02 D-10, D-21).
- Per-entry Polish "treść zastępcza" placeholder boolean stays (02 D-09).
- Design system is locked (`01-UI-SPEC.md` and its amendments). Copy rules: no emoji, no em dashes, en dash only in numeric ranges.
- Uploads live in `src/lib/assets/uploads/` and must stay inside the Vite/`enhanced-img` build pipeline (05 code_context).
- Near-zero cost: free tiers only. Every decision below was checked against this and none introduces a bill.

**NOT in this phase:** the gallery and cennik editors (Phase 5 builds them on this panel), full "Ustawienia strony" editing of contact details and keyFacts, and any change to the form pipeline built in Phase 4.

</domain>

<decisions>
## Implementation Decisions

### Logowanie: e-mail one-time code

- **D-01: Self-built Polish e-mail code login, NOT Cloudflare Access.** Staff enters an e-mail address, receives a numeric code by e-mail, and exchanges it for a signed session cookie. Built on infrastructure that already exists and is already unit-tested: the Resend sender on `send.zlobekstromiec.pl`, the `FORMS_KV` namespace, the salted-hash rate limiter in `src/lib/server/forms/ratelimit.ts`, and the sanitizers in `sanitize.ts`.
  - Cloudflare Access one-time PIN was evaluated and rejected on the phase's own success criterion 2. Access is free to 50 users and would remove the code-generation and session work, but the PIN screen is Cloudflare's page, its full Polish localisation is not guaranteed, and replacing an English third-party login with a different English third-party login defeats the reason this phase exists. Secondary reasons: the configuration would live in a dashboard rather than the repo (worse handover), and local dev plus Pages previews would need a bypass path.
  - Cost: free. Login codes draw on the same Resend free tier as the two forms (100/day, 3000/month), which a handful of daily logins does not threaten.
- **D-02: The allowlist of permitted e-mail addresses is ONE encrypted Cloudflare Pages secret** (for example `ADMIN_EMAILS`, comma separated). Nothing personal is committed. **This is a hard constraint, not a preference: `zlobekstromiec/zlobek-gminny-stromiec` is a PUBLIC repository**, so staff e-mail addresses in git would be both a harvesting target and personal data published by a public body.
  - Known Phase 4 gotcha that applies here: a Pages secret only reaches deployments created AFTER it is set, so adding an editor is "set the secret, then trigger a rebuild".
  - An address not on the list must be treated identically to one that is, with no signal that it is unknown, so the panel cannot be used to enumerate who works at the żłobek.
- **D-03: Session lasts 30 days and renews on use,** so a person who edits weekly effectively stops seeing the login screen on that device. A visible „Wyloguj" control is mandatory for shared computers.
- **D-04: One machine identity authors every commit; the editor is named by a short non-personal handle in the commit message,** for example `tresc(aktualnosci): dodano wpis "Dzien otwarty" (edytor: anna.k)`. A full e-mail address must never reach a commit author field or message, for the same public-repo reason as D-02. This supersedes 02 D-19 (per-editor GitHub accounts), which is dead because staff no longer have GitHub accounts.
- **D-05: The allowlist starts with `devzlobekstromiec@gmail.com` plus any working private address the staff already use, and swaps to `@ugstromiec.pl` addresses later.** Rationale: `zlobek@ugstromiec.pl` does not exist yet (pending Gmina approval), and gating this phase on Gmina IT would repeat the FORM-01 pattern of shipping an unverifiable requirement. Because the list is a secret (D-02), the swap costs one secret update plus a rebuild and no code change.

### Write path: how edits persist

- **D-06: Content stays in git. The panel commits to this repository through the GitHub API.** A runtime store (KV, D1, R2) was rejected: it would violate ROADMAP SC5, force a rewrite of every reader built in Phases 2-3, end the prerendered zero-JS model that is a foundational project property, and remove the build-time file access that `enhanced-img` needs. This closes the open write-path research question recorded in 05 D-11.
- **D-07: One atomic commit per save, using the GitHub Git Data API** (create blobs, build a tree, create a commit, update the ref), NOT the simple Contents API. Rationale: an aktualności entry is two files, the JSON and its cover image. One commit per file would mean two commits, two Pages builds, roughly four minutes, and a window where the post is live without its photo.
- **D-08: The panel authenticates to GitHub as a GitHub App owned by the `zlobekstromiec` Org and installed on this one repository,** minting short-lived installation tokens from a private key held as a Pages secret. Chosen over a fine-grained PAT for durability at handover: an App has no expiry date to lapse silently and survives the `devzlobekstromiec` user account being removed, whereas an expiring PAT is a time bomb in a project whose whole point is that no developer is around afterwards. It also gives the bot author identity that D-04 assumes. Permissions must be scoped to Contents:write on this repository and nothing more.
- **D-09: The panel emits prettier-shaped JSON (tabs, trailing newline) so content files stay inside the normal quality gate.** This closes the Phase 3 UAT blocker recorded in STATE.md, where CMS-written 2-space JSON blocked ALL local commits through the prettier pre-commit hook. A unit test asserting panel output is prettier-clean is required, because this exact defect has already bitten the project once. `.prettierignore` is deliberately NOT used: it would hide future regressions in panel output.
- **D-10: A save that would overwrite a change made since the entry was opened is REFUSED, with a Polish message telling the editor the entry changed and to reload.** The Git Data API needs the current SHA anyway, so conflict detection is effectively free, and the failure it prevents (silently destroying a colleague's edit) is the worst kind.
- **D-11: One „Zapisz" per page, not per section.** An editing session on o-nas (misja, wartości, plan dnia, kadra, facility text) produces one commit and one build regardless of how many fields were touched. Rationale: **Cloudflare Pages allows 500 builds per month on the free plan** and every save is a build; developer commits count against the same ceiling.

### Zdjęcia: upload and cover images

- **D-12: The browser resizes and re-encodes the photo BEFORE anything is sent.** A 5 MB phone photo becomes roughly 200-400 KB. Rationale is threefold: the free Workers plan allows about 10 ms of CPU per request and base64-encoding a multi-megabyte file is real CPU rather than I/O wait; git history keeps every version of every image forever; and uploads must work on a weak connection at the żłobek. A useful side effect is that the browser's own decoder handles iPhone HEIC on iOS. The editor sees a preview of exactly what will be published.
- **D-13: The panel crops to the card's fixed aspect ratio automatically, centred, and shows the result before saving.** A drag-to-position cropper was considered and deferred (see Deferred Ideas): it would produce better results for portrait photos but costs a real interactive component that must be keyboard operable and axe-clean.
- **D-14: The panel generates the filename; staff never type one.** An ASCII slug derived from the entry (for example `2026-08-20-dzien-otwarty.jpg`), stripping Polish diacritics the same way the Sveltia slug rules had to. **Constraint carried from T-03-03: the basename lookup IS the path-traversal defence**, so generated names must be guaranteed to resolve inside `src/lib/assets/uploads/` and an unknown basename must continue to yield the tint fallback rather than a filesystem read.
- **D-15: Alt text is REQUIRED before an image can be saved,** with a short Polish hint explaining what to write (what is in the photo, not „zdjęcie"). The site is a public body under WCAG 2.1 AA and Phase 6 must certify that baseline in a legal document, so accessibility is made structurally impossible to forget rather than left to staff discipline. The Phase 3 reader continues to treat `obraz_alt` as optional and degrade safely, so this is a panel-level rule that can never break the build.

### Panel: scope and behaviour

- **D-16: Scope is everything editable today PLUS one recruitment open/closed switch.** That is o-nas, plan dnia, dokumenty, aktualności with covers (ROADMAP SC3), and `recruitmentOpen`, currently a boolean a developer flips by hand in `src/lib/content/site.ts`. One boolean written to a file the panel already handles, and it removes the last thing the żłobek genuinely needs a developer for during the year. Full "Ustawienia strony" editing (contact details, keyFacts) stays out (see Deferred Ideas).
- **D-17: Server-rendered admin routes using SvelteKit form actions, with hydrated islands ONLY where they earn it** (image crop and upload). Matches the pattern the whole site already follows, degrades without JavaScript, inherits the accessibility approach that is already AA proven, and keeps each screen independently testable. A client-side SPA was rejected as a second architecture in an otherwise prerendered, near zero-JS codebase. The `/admin` routes must be excluded from prerender and carry `noindex`.
- **D-18: Publish feedback is an honest Polish message, with no new infrastructure.** After a save the panel states the change is saved and will appear on the site within about two minutes, and links to the affected page. This keeps the promise 02 D-12 already made in the instrukcja. Polling the Cloudflare API for real build status was considered and deferred: it would be genuinely reassuring but costs another API token as a Pages secret, polling code and a failure mode.
- **D-19: The panel reuses the locked design system's tokens (colours, fonts, radii) in a denser, calmer layout suited to forms.** It should feel like the same product a staff member already knows, and it inherits the accessible-tier palette and focus styles that are already AA proven. The roadmap marks this phase UI hint: yes, so `/gsd-ui-phase 4.1` should pin the details before planning.

### Migracja i wymagania

- **D-20: Sveltia is removed ONLY after the new panel passes UAT on the live deployment** with a real staff login and a real edit. A final plan in this phase then removes `@sveltia/cms`, `static/admin/*` (bundle, `config.yml`, `locale-pl.js`, `preboot.js`, `admin.css`, vendored fonts), the `cms:sync` script, the `sveltia-cms-auth` Worker, the GitHub OAuth App and the `/admin` CSP block in `_headers`, in one clean sweep. Rationale: if the panel has a defect, staff still have a working editor rather than none, and the site is about to go public.
- **D-21: REQUIREMENTS.md CMS-01 and CMS-03 are reworded in this phase and re-verified at completion.** Both currently name Sveltia and GitHub OAuth as implementation and are ticked complete; CMS-03 was marked complete with a known English-chrome caveat (02-05 decision) that this phase closes outright. Reword to describe the outcome, not the vendor: staff sign in without a GitHub account, and the panel is Polish end to end. Flagged as debt in STATE.md and resolved here.

### Claude's Discretion

- Code lifetime, attempt limits, lockout policy, code length and cookie flags for the login, following the patterns already proven in `src/lib/server/forms/ratelimit.ts` (bucketed KV keys, salted hashing, fail-open degrade where a lockout would cost more than it protects).
- GitHub App permission scoping detail, installation-token caching, and API error handling and retry.
- Exact maximum image width, JPEG quality target and how the crop preview is rendered.
- Delete and replace flows, confirmation copy, and the shape of Polish error messages.
- Admin route layout, file organisation under `src/routes/admin/` and `src/lib/server/admin/`, and whether the repeated basename-resolution idiom is finally extracted into a shared helper.
- Rewrite scope of the Polish instrukcja (it currently documents Sveltia and GitHub login end to end and will need substantial replacement).
- Whether the panel logs edits anywhere beyond git history.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase definition
- `.planning/ROADMAP.md` §"Phase 04.1" (lines 194-215) - goal, the five success criteria, and the three open decisions this discussion closed. **SC5 is the binding constraint on the write path.**
- `.planning/REQUIREMENTS.md` - CMS-01 (line 65), CMS-02 (66), CMS-03 (67) and the traceability table (lines 126-128). All three are reworded by D-21.
- `.planning/STATE.md` §Blockers/Concerns - the Phase 04.1 requirements-staleness item, the Phase 3 UAT prettier-vs-CMS-indent blocker closed by D-09, and the `zlobek@ugstromiec.pl` mailbox item behind D-05.

### Design contract (LOCKED, inherited)
- `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md` - two-tier palette (expressive vs accessible), type scale, focus and contrast contract, Amendment v1.2 copy rules (no emoji, no em dashes). D-19 reuses these tokens. There is NO Phase 04.1 UI-SPEC yet; run `/gsd-ui-phase 4.1` before planning.

### Prior decisions this phase inherits or supersedes
- `.planning/phases/02-about-documents-cms/02-CONTEXT.md` - D-05 (strict validated fields), D-08 (limited rich text), D-09 (placeholder boolean), D-10 (/admin), D-12 (publish delay explained in the instrukcja, kept by D-18), D-20 (direct publish to main), D-21 (Polish instrukcja). **D-19 (per-editor GitHub accounts) is SUPERSEDED by D-04.**
- `.planning/phases/04-enrollment-contact-email-pipeline/04-CONTEXT.md` D-18 - "Phase 4 stays CMS-free and CMS-agnostic"; the form pipeline must survive this migration untouched. Its kill of the "Ustawienia strony" singleton is partially reopened by D-16, for the recruitment switch only.
- `.planning/phases/05-gallery-fees/05-CONTEXT.md` - D-09 (content shape is the durable deliverable, plain JSON), D-11 (this phase, and its open write-path question, now answered by D-06 to D-08). Phase 5 builds the gallery and cennik editors on this panel, so the upload mechanism in D-12 to D-15 must generalise to a photo gallery.

### Code the phase must not break
- `src/lib/server/aktualnosci.ts` - the matured reader and single-validation-boundary precedent (entry typed `unknown`, guarded before property access, built key-by-key never by spreading, malformed entry warns and returns null). Panel output must satisfy it exactly.
- `src/lib/server/dokumenty.ts` - build-time file metadata resolver and path-safety approach.
- `src/lib/content/` - `o-nas.json`, `day-plan.json`, `dokumenty/*.json`, `aktualnosci/*.json` are the files the panel writes. Their shape is frozen by ROADMAP SC5.
- `src/lib/content/site.ts` - holds `recruitmentOpen` (D-16) and the verbatim core message, which 02 D-11 forbids making editable.
- `vite.config.ts` and `src/lib/assets/uploads/` - `enhancedImages()` runs before `sveltekit()`; input glob accepts jpg, jpeg, png, webp and NOT avif.
- `_headers` lines 17-24 - the path-scoped Sveltia `/admin` CSP, removed by D-20 and replaced by whatever the new panel needs.
- `svelte.config.js` - `kit.csp` for site pages, and the prerender configuration the new `/admin` routes must opt out of.
- `wrangler.jsonc` - source of truth for Pages bindings; `FORMS_KV` (id `55f55448fe1345e28a79da5a3e9e9ca9`) is the namespace the login reuses.

### Project intent and constraints
- `.claude/CLAUDE.md` - non-negotiables, the adapter-cloudflare and Tailwind v4 gotchas, the accounts and deploy section. **Its stack line naming Sveltia goes stale with this phase and should be updated.**
- `.planning/PROJECT.md` - Polish-only constraint, near-zero-cost constraint, Key Decisions table (the git-based CMS row changes here).
- `.planning/research/PITFALLS.md` §7 - no Git Gateway on Cloudflare, the reason the OAuth Worker existed; §8 - staff cannot use the CMS, the reason for strict widgets and Polish labels.

### External
- `https://github.com/zlobekstromiec/zlobek-gminny-stromiec` - **PUBLIC repository.** Drives D-02 and D-04.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `src/lib/server/forms/ratelimit.ts` - salted-hash client keys, bucketed KV keys (hour of epoch per client, UTC date site-wide), cleanup-only TTL, and a documented fail-open degrade. The login code endpoint needs exactly this shape of protection and should reuse the module rather than reinvent it. Note the hard-won lessons: a KV write overwrites the stored expiration (which is why the window lives in the key), and it refuses to hash without a salt.
- `src/lib/server/forms/mailer.ts` - the Resend sender, payload builder and the `FROM` identity on `send.zlobekstromiec.pl`. The login code e-mail is a third caller.
- `src/lib/server/forms/sanitize.ts` - `bezpiecznyEmail` (header-injection safe, `MAKS_EMAIL` 254) is directly reusable for the login form.
- `src/lib/server/forms/handle.ts` - `obsluz()` takes every side effect by injection, the pattern that made the second endpoint a thin adapter. The admin endpoints should follow the same testability shape.
- `src/lib/server/aktualnosci.ts` and `dokumenty.ts` - the readers the panel writes for. Reading them defines the exact JSON the panel must emit.
- `src/lib/components/MobileNav.svelte` - the correctness precedent for a hydrated island: bounded focus trap, Escape to close, `prefers-reduced-motion`. The crop and upload island must meet the same bar.
- `src/lib/components/{Seo,SkipLink}.svelte` - reusable in the admin shell.
- `tests/forms.unit.ts` and `tests/forms-copy.unit.ts` - the frozen-clock unit pattern and the explicit-export copy sweep. Admin copy should join a similar sweep so Polish-only and no-em-dash rules are enforced, not hoped for.

### Established Patterns

- **Prerendered zero-JS content routes.** `prerender = true` is set once in `src/routes/+layout.ts` and inherited; `/admin` must opt out explicitly.
- **Exactly two dynamic routes exist today** (`/api/kontakt`, `/api/rekrutacja`), both Pages Functions via adapter-cloudflare. The admin routes become the third group. Secrets are read from `event.platform.env.*`, never `import.meta.env`.
- **No root `.dev.vars`.** `wrangler types` bakes its keys into the committed `worker-configuration.d.ts` as required members, which Pages CI cannot reproduce, so `wrangler types --check` would fail every deploy. Local and Playwright runs pass secrets via `npm run preview:test --binding` flags. Any new admin secret must follow this, and `src/app.d.ts` types them as optional.
- **Content flow:** JSON under `src/lib/content/<collection>/` to a server reader using `import.meta.glob(..., { eager: true, import: 'default' })` to a `+page.server.ts` load.
- **The basename-resolution idiom is duplicated verbatim in three files** (`NewsCard.svelte`, `o-nas/+page.svelte`, `aktualnosci/[slug]/+page.svelte`), and the basename lookup is the path-traversal defence.
- **Tests interpolate from content modules, never retype values.** Test titles are written in Polish. The axe block is inlined per spec.
- Copy rules apply to code comments, test names and commit messages, not just shipped copy.

### Integration Points

- `static/admin/*` - the entire Sveltia surface, deleted by D-20.
- `_headers` `/admin/*` block - replaced; the new panel is SvelteKit routes, so its CSP question is different in kind (it may belong in `kit.csp` rather than `_headers`).
- `package.json` - remove `@sveltia/cms` and the `cms:sync` script; `scripts/cms-sync.mjs` goes with them.
- `wrangler.jsonc` - `FORMS_KV` is reused by the login; decide whether admin sessions share it or get their own namespace (note the scoped `CLOUDFLARE_API_TOKEN` in `.envrc` has NO Workers KV permission, so creating a new namespace needs a widened token or the dashboard).
- `src/lib/content/site.ts` `recruitmentOpen` - becomes panel-writable per D-16; it currently sits in a TypeScript module, so either the boolean moves to JSON or the panel needs a different write strategy for it. **This is the one D-16 detail the planner must resolve deliberately, because the panel cannot safely rewrite a TypeScript module** (the same reasoning that put fee amounts into JSON in 05 D-03).
- GitHub Org `zlobekstromiec` - hosts the OAuth App being removed and the GitHub App being created.
- `docs/instrukcja-cms.md` (or wherever the Phase 2 instrukcja landed) - substantially rewritten.

</code_context>

<specifics>
## Specific Ideas

- The user restated the cost constraint explicitly during this discussion: free if at all possible. Every decision above was checked against it. Cloudflare Access (free to 50 users), KV, GitHub API and the GitHub App are all free, so **cost did not separate any of these options**; Polish UI and preserving the prerender did. The two real free-tier ceilings to respect are 500 Pages builds per month (drove D-11) and roughly 10 ms of Workers CPU per request (drove D-12).
- The repository being public was discovered during this discussion and is not a preference but a constraint on D-02 and D-04.
- The panel should feel like the same product staff already know, not a separate tool (D-19).
- Login should be something a person with no GitHub account and no technical vocabulary can complete unaided: type your e-mail, read the code from your inbox, type it in.
- The recruitment open/closed switch was wanted specifically because it is the one thing the żłobek currently cannot do without a developer.

</specifics>

<deferred>
## Deferred Ideas

- **Full "Ustawienia strony" editing** (contact details, keyFacts) - explicitly out of D-16. It reopens a decision Phase 4 closed, touches shipped and tested surfaces including the fee strings Phase 5 is about to restructure, and is closer to its own phase. Revisit after Phase 5.
- **Editor self-service user management** (allowlist in KV with a „Użytkownicy" screen) - rejected in favour of D-02 for now. Worth revisiting if staff churn makes "set a secret and rebuild" a real burden.
- **Real Cloudflare build-status polling in the panel** - rejected in favour of D-18. Would need a Pages-read API token as a secret plus a failure mode. Genuinely nice for non-technical editors; revisit if staff report uncertainty after handover.
- **Drag-to-position image cropper** - rejected in favour of the automatic centred crop in D-13. Better results for portrait photos, but it is a real interactive component with its own accessibility bar. Candidate for Phase 5 if the gallery makes framing matter more.
- **Gallery and cennik editors** - Phase 5, built on this panel (05 D-09, D-11).
- **Swapping the allowlist to `@ugstromiec.pl` addresses** - one secret update plus a rebuild, once the Gmina provisions the mailboxes. Tracked alongside the existing `zlobek@ugstromiec.pl` form-recipient item.
- **AG-3 test-gate gap** - `tests/*.unit.ts` runs in NO automated gate: `npm run test` is Playwright-only, pre-commit is check plus lint, and there is no CI workflow. Raised at `03-REVIEW.md:99-105` and again in `04-REVIEW.md`, and now inherited by the most security-sensitive code in the project (login, session, repository writes). Offered during this discussion and consciously not taken up; the user chose to proceed. **Flagged for the planner: any unit test this phase writes carries the same blind spot, and the phase's own verification should not lean on a suite nothing runs automatically.**
- **`.claude/CLAUDE.md` stack section names Sveltia** and goes stale with this phase; update as part of D-20 or immediately after.

</deferred>

---

*Phase: 04.1-replace-sveltia-with-custom-polish-cms*
*Context gathered: 2026-08-15*
