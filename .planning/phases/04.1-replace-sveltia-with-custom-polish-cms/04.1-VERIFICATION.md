---
phase: 04.1-replace-sveltia-with-custom-polish-cms
verified: 2026-08-16T12:30:00Z
status: human_needed
score: 2/5 must-haves verified
behavior_unverified: 3
overrides_applied: 0
behavior_unverified_items:
  - truth: "SC1 — a staff member with no GitHub account can sign in to the live admin panel with a one-time e-mail code, and this has actually been done by a real person on the live deployment."
    test: "Have a real staff member, given only an e-mail address and the printed docs/instrukcja-cms.md, open https://<live-host>/admin, request a code, read it from their inbox, and complete login unaided."
    expected: "They land on the authenticated pulpit with no help and no error caused by a missing Cloudflare binding."
    why_human: "The code-request leg and the whole auth gate are code-verified locally (24/24 tests in tests/admin-auth.spec.ts pass, including the 5-attempt burn, tamper rejection, and step-1 timing indistinguishability). What no test can see is a real person, with no developer nearby, actually completing the flow against a real inbox on the real deployment. This is 04.1-UAT.md Block A."
  - truth: "SC3 — staff can create, edit and delete every editable content type from the panel and the change is live on the public site after the Cloudflare rebuild."
    test: "Run 04.1-UAT.md Block B (one save per content type: nabór, aktualność with a phone photo, document PDF, plan dnia, o-nas) and Block C (confirm each change rendered on the public site after the ~2 minute rebuild)."
    expected: "Each save produces exactly one commit (checked via git show --stat) and the public page reflects the change after the build queue drains."
    why_human: "This is CMS-02's ONLY evidence. Every plan from 04.1-05 onward deliberately deferred it — no save through the panel has yet produced a real GitHub commit. The write path (src/lib/server/admin/commit.ts) is proven correct against a mocked fetch (tests/admin-commit.unit.ts, 30/30 passing, including an explicit two-call-count assertion for a two-file save), but a mocked fetch cannot prove the real GitHub Git Data API round-trip, the real Cloudflare Pages build trigger, or real iPhone HEIC decoding (Block B2 note)."
  - truth: "SC4 — the old GitHub OAuth App and the deployed sveltia-cms-auth Worker no longer exist as live service configuration."
    test: "Run 04.1-UAT.md Block F: delete the OAuth App (NOT the GitHub App 'Panel redakcyjny zlobka', App ID 4609238) and the deployed sveltia-cms-auth Worker, then confirm both actions with one more login and one more save."
    expected: "The OAuth App is gone from GitHub org settings; sveltia-cms-auth.devzlobekstromiec.workers.dev no longer serves its old response; the panel still works after both deletions."
    why_human: "The repository-side removal (bundle, config.yml, cms:sync script, package.json entry, _headers block) is code-verified as fully gone. These two remaining objects are dashboard-only state on GitHub and Cloudflare that the scoped CLOUDFLARE_API_TOKEN cannot reach (no Workers permission) and that no grep in this repository can observe or delete."
human_verification:
  - test: "04.1-UAT.md Block A — real staff login with printed instrukcja only, no developer help."
    expected: "Login completes unaided; no server error from a missing binding; staff member reports whether the panel's Polish reads naturally (the human half of CMS-03 that no regex can replace)."
    why_human: "Requires reading a real six-digit code from a real mailbox on the live deployment, and a human judgment of copy clarity."
  - test: "04.1-UAT.md Block B — one real save per content type (nabór, aktualność+photo, document, plan dnia, o-nas), each checked for exactly one commit via git show --stat."
    expected: "Each save yields one atomic commit; the aktualność-with-photo commit touches exactly two files; document add/rename/replace/delete commits match the plan's exact-file-count claims."
    why_human: "This is the sole evidence for CMS-02. No save has produced a real GitHub commit yet — the write path is unit-proven against a mock, not proven against the live GitHub API."
  - test: "04.1-UAT.md Block C — public site reflects each Block B change after the Cloudflare rebuild."
    expected: "Nabór banner text changes, the new aktualność and its photo appear, the document downloads correctly, plan dnia and o-nas edits are visible."
    why_human: "Requires waiting for and observing a real Cloudflare Pages rebuild, which nothing in this repository can simulate."
  - test: "04.1-UAT.md Block D — deletion end to end (aktualność and document)."
    expected: "Deleted entries disappear from the public site after rebuild; document deletion removes both the JSON and the file in one commit."
    why_human: "Same live-commit and live-rebuild dependency as Block B/C."
  - test: "04.1-UAT.md Block E1/E2/E3 — Wyloguj on a shared computer, and the twenty-request login-timing-parity measurement (allowlisted vs non-allowlisted address)."
    expected: "Wyloguj ends the session; the two response-time distributions do not separate in a way that would make the endpoint a timing oracle."
    why_human: "E2/E3 are explicitly the one SC1 property no test in this project can see (local measurement showed ~4.2ms vs ~3.1ms, not conclusively safe); this needs a human to run the curl loop against the live deployment and read the result."
  - test: "04.1-UAT.md Block F — delete the old GitHub OAuth App and the deployed sveltia-cms-auth Worker, verify the Worker URL no longer authorizes, and count exactly eight Pages secrets afterward."
    expected: "Both dead services are gone; panel login and a small save still work after the deletions; `wrangler pages secret list` shows exactly eight secrets and nothing else."
    why_human: "Dashboard-only actions the scoped CLOUDFLARE_API_TOKEN cannot perform (no Workers permission), and the GitHub OAuth App can only be deleted from the GitHub UI."
---

# Phase 04.1: Replace Sveltia with custom Polish CMS Verification Report

**Phase Goal:** A staff member with no GitHub account signs in to a Polish-only admin panel with a one-time code sent to their e-mail, edits every content type the site exposes, and sees it live after the Cloudflare rebuild — with Sveltia CMS, its OAuth Worker and the GitHub OAuth App removed from the project.
**Verified:** 2026-08-16
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1: staff with no GitHub account signs in with an e-mail one-time code; an unauthenticated visitor reaches neither an admin page nor an admin API | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The gate is real and behaviorally proven: `tests/admin-auth.spec.ts` (24/24 passing, re-run live in this verification) exercises unauthenticated GET/POST refusal, tampered-cookie rejection, revocation-on-delist, 5-attempt code burn, and step-1 timing indistinguishability, against a real running SvelteKit + Cloudflare-emulated server. The one thing not exercised: a real person, with no developer present, completing the flow against a real mailbox on the live `*.pages.dev` deployment (04.1-UAT.md Block A). |
| 2 | SC2: every panel surface is Polish, no English chrome anywhere, including third-party UI | ✓ VERIFIED | `tests/admin-polski.spec.ts` (215 lines) sweeps 14 authenticated screens plus login step 2, the validation-refusal state, the not-found state, and both `powod` banner states — ~19 distinct screen checks — against a real banned-word list (`tests/fixtures/angielskie-chrome.ts`) that carries a genuine positive control (`'Save changes'`, `'log out'`, `'CHOOSE FILE'`, `'Delete this entry'`) and a negative control that specifically defeats naive `\b` word-boundary matching on Polish diacritics (`ważność`). The detector-self-test runs first and is asserted before any real screen. Read in full; this is a genuine enforcing gate, not a test that cannot fail. The one carved-out exception (the native `<input type="file">` OS picker chrome) is excluded by construction, documented in the file, and matches UI-SPEC Component Contract 8. |
| 3 | SC3: staff can create, edit and delete all four editable content types (o-nas, plan dnia, dokumenty, aktualności with covers) and the change is live after the Cloudflare rebuild | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | All four editors exist, build, pass `svelte-check` and `eslint`, and their Playwright specs pass locally in `PANEL_DRY_RUN` mode (e.g. `tests/admin-nabor.spec.ts` 11/11, re-run live in this verification, including a no-JS full save and three axe-clean states). The atomic write path (`src/lib/server/admin/commit.ts`) is unit-proven against a mocked fetch with an exact call-count assertion for a two-file save (`tests/admin-commit.unit.ts`, 30/30). What is NOT proven: a real save producing a real commit against the live GitHub API and a real Cloudflare Pages rebuild reaching the public site — this is 04.1-UAT.md Blocks B/C, and REQUIREMENTS.md itself already flags CMS-02 as "Pending (first real save)." |
| 4 | SC4: Sveltia is gone — no bundle, no config.yml, no sveltia-cms-auth Worker (source), no GitHub OAuth App in the login path; `/admin` CSP and the instrukcja describe the new panel | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Repository-side removal is fully verified: `static/admin/`, `sveltia-cms-auth/`, `scripts/cms-sync.mjs` do not exist; `package.json`/`package-lock.json` have zero `sveltia` hits; `_headers` carries zero `/admin/*` path-scoped blocks (replaced by a comment explaining why one is deliberately absent); `svelte.config.js` kit.csp is unchanged/unwidened for the panel (confirmed by reading the file); `docs/instrukcja-cms.md` and `.claude/CLAUDE.md` describe only the new panel (zero github/oauth/sveltia hits outside `tests/`, where two hits are legitimate absence-assertions and two are documented-decision paper-trail links). NOT yet true: the live GitHub OAuth App and the deployed `sveltia-cms-auth` Worker still exist as running services — deletion requires GitHub/Cloudflare dashboard access this agent and the scoped `CLOUDFLARE_API_TOKEN` do not have (04.1-UAT.md Block F, T-04.1-38/39 explicitly left NIEZAMKNIETE in 04.1-11-SUMMARY.md's own threat table). |
| 5 | SC5: content files keep their current shape so the Phase 2-3 readers, the prerender and the existing test suites pass unchanged | ✓ VERIFIED | Re-run live in this verification: `npm run check` → 0 errors/warnings across 4357 files. `npm run lint` → prettier + eslint clean. `node --test tests/*.unit.ts` → 478/478 passing. `npm run build` → exit 0, and `.svelte-kit/cloudflare` has NO `admin/` directory (confirmed by direct `find`), so `/admin` is genuinely excluded from the prerendered output. `npx playwright test --list` → 285 tests across 20 files (unchanged count). Two full admin Playwright specs re-run live in this verification (`admin-auth.spec.ts` 24/24, `admin-nabor.spec.ts` 11/11) both pass. |

**Score:** 2/5 truths verified, 3 present-and-wired-but-behavior-unverified (all three route to the single live UAT session — 04.1-UAT.md).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/hooks.server.ts` | Single, unbypassable admin gate | ✓ VERIFIED | Read in full. Fails closed on missing secret, tampered cookie, expired session, and de-allowlisted address; the allowlist re-check on every request is what makes the cookie revocable without a KV read. `/admin/logowanie` is the only exempt path, matched by whole segment (not prefix). |
| `src/lib/server/admin/sesja.ts` | HMAC-signed, tamper-proof session cookie | ✓ VERIFIED | WebCrypto-only, constant-time signature comparison, `__Host-` cookie prefix, every failure path returns `null` silently (no oracle). |
| `src/lib/server/admin/commit.ts` | One atomic commit per save via Git Data API | ✓ VERIFIED (unit-proven) | Read in full. 7-call sequence (ref → head commit → N blobs in parallel → tree → commit → ref update), conflict detection before any blob is written, non-fast-forward mapped to a Polish "konflikt" outcome, never a partial success. Simple Contents API is never used. |
| `tests/admin-polski.spec.ts` + `tests/fixtures/angielskie-chrome.ts` | Genuine Polish-only enforcement, not a test that cannot fail | ✓ VERIFIED | Read in full; see truth #2 evidence above. |
| `static/admin/`, `scripts/cms-sync.mjs`, `sveltia-cms-auth/`, `@sveltia/cms` in package.json, `config.yml` | Fully removed | ✓ VERIFIED | All confirmed absent by direct filesystem/grep checks. |
| `docs/instrukcja-cms.md` | Describes the panel that actually exists, no Sveltia/GitHub/OAuth mentions | ✓ VERIFIED | Read the opening sections; describes the real e-mail-code login, the real 2-minute publish delay, real Wyloguj behavior. `tests/instrukcja.unit.ts` (12 tests, re-run live, all pass) additionally enforces structural properties (heading levels, screen names quoted from the copy module, no promise that deletion can be undone). |
| `.claude/CLAUDE.md` | Updated to describe the new panel, not Sveltia | ✓ VERIFIED | Stack line, panel-auth gotcha, and panel-write gotcha all describe the new e-mail-code panel; no Sveltia reference remains. |
| `04.1-UAT.md` | Honest, executable live-verification scenario | ✓ VERIFIED (as an artifact) | Exists, is structured into Blocks A–F, and every row is honestly `NIEWYKONANE` (not faked as passed). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `src/hooks.server.ts` | `src/lib/server/admin/sesja.ts` + `allowlist.ts` | `weryfikujSesje` + `naLiscie` on every request | ✓ WIRED | Confirmed by reading the file; re-verified behaviorally by 24/24 passing cases in `admin-auth.spec.ts`, including the revocation and tamper cases. |
| `src/routes/admin/+layout.ts` | prerender opt-out | `export const prerender = false` overriding the site-wide default | ✓ WIRED | Confirmed: `.svelte-kit/cloudflare` build output has no `admin/` directory after a fresh `npm run build`. |
| `src/routes/admin/nabor/+page.server.ts` etc. | `src/lib/server/admin/zapis.ts` → `commit.ts` | form action → `zapiszAtomowo` | ✓ WIRED (locally); NOT proven against live GitHub | Dry-run save flow passes end to end in Playwright; the real network call to the GitHub Git Data API has never executed (this is exactly the CMS-02 gap). |
| `svelte.config.js` kit.csp | `/admin/*` | no path-scoped widening | ✓ WIRED | Confirmed by reading the file: the comment explicitly forbids widening, and the diff history (04.1-11-SUMMARY.md self-report, spot-checked) shows only a comment change, no directive change. |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| CMS-01 | 04.1-01, 02, 03, 11 | Staff sign in with an e-mail one-time code, no GitHub account | ? NEEDS HUMAN | Reworded per D-21 (verified: REQUIREMENTS.md line 65 correctly restates the outcome, not Sveltia/GitHub). Code-side login flow behaviorally proven locally. REQUIREMENTS.md itself honestly leaves this unticked pending 04.1-UAT.md Block A — a judgment this verification agrees with. |
| CMS-02 | 04.1-04, 05, 06, 07, 08, 09, 11 | Panel edits commit to the repo and trigger a rebuild | ? NEEDS HUMAN | Write path unit-proven against a mock; zero real commits produced through the panel to date. REQUIREMENTS.md correctly leaves this Pending — this verification agrees it has no other evidence and cannot be closed without 04.1-UAT.md Block B. |
| CMS-03 | 04.1-02, 03, 06, 08, 10, 11 | Panel is Polish end to end, no English chrome | ? NEEDS HUMAN (automated half VERIFIED) | The automated half is genuinely enforced (see truth #2). The human half — a real staff member confirming the copy reads naturally, not just that it passes a regex — is 04.1-UAT.md Block A2 and is correctly left open in REQUIREMENTS.md. |

No orphaned requirements: `.planning/REQUIREMENTS.md` maps exactly CMS-01, CMS-02, CMS-03 to Phase 04.1, and all three are declared across the phase's plans.

### Anti-Patterns Found

None. Scanned `src/lib/server/admin/`, `src/routes/admin/`, `src/lib/components/admin/`, and `src/lib/content/panel.ts` for `TODO|FIXME|XXX|HACK`, placeholder/not-implemented language, and bare `console.log` — zero hits. The only `console.*` calls present are deliberate `console.warn` diagnostics for missing-binding fail-closed paths in `kod.ts`, none of which log a secret, address, or code.

### Deviations Noted (Approved, Not Gaps)

- **D-20 superseded mid-phase (approved).** Sveltia was deleted in Plan 01, not Plan 11, because as a static asset under `static/admin/` it shadowed the new panel's `/admin` route on Cloudflare Pages (Pages resolves static assets before invoking the Worker). Confirmed via `git log`: commit `6422376` "usun edytor Sveltia" sits inside the Plan 01 commit range, well before Plan 11. The user explicitly approved this at a checkpoint. Not treated as a violation.
- **Requirements ticks deliberately reset (correct, not a regression).** Plan 11 unticked CMS-01/02/03 and retargeted them from Phase 2 to Phase 04.1, because they were ticked against an editor (Sveltia) that no longer exists in the repository, and CMS-02 never had any evidence at all. This verification agrees with that judgment: leaving them ticked would have made REQUIREMENTS.md assert a working login that has never actually been exercised end to end.

### Behavioral Spot-Checks (re-run live during this verification)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `npm run check` | `npm run check` | 0 errors, 0 warnings, 4357 files | ✓ PASS |
| `npm run lint` | `npm run lint` | prettier + eslint clean | ✓ PASS |
| Unit suite | `node --test tests/*.unit.ts` | 478 pass / 0 fail | ✓ PASS |
| Full build | `npm run build` | exit 0; no `admin/` dir in `.svelte-kit/cloudflare` | ✓ PASS |
| Playwright inventory | `npx playwright test --list` | 285 tests / 20 files | ✓ PASS (enumeration) |
| Auth gate, live-run | `npx playwright test tests/admin-auth.spec.ts --project=chromium` | 24 passed | ✓ PASS |
| Nabór save flow, live-run | `npx playwright test tests/admin-nabor.spec.ts --project=chromium` | 11 passed | ✓ PASS |
| Commit-atomicity unit suite | `node --test tests/admin-commit.unit.ts` | 30 passed (includes exact-call-count assertion) | ✓ PASS |
| Prettier-byte-identity unit suite | `node --test tests/admin-serializuj.unit.ts` | 6 passed (real prettier binary invoked) | ✓ PASS |

### Human Verification Required

See `behavior_unverified_items` and `human_verification` in the frontmatter — both point to the single consolidated session in `.planning/phases/04.1-replace-sveltia-with-custom-polish-cms/04.1-UAT.md` (Blocks A through F). Summary:

1. **Block A — live login by a real, unaided staff member**, including the human half of CMS-03 (does the copy actually read as clear Polish).
2. **Block B/C/D — real panel saves and deletions**, each checked for exactly one commit and a corresponding public-site change after rebuild. This is the ONLY evidence CMS-02 will ever have.
3. **Block E — Wyloguj on a shared machine, and the 20-request login-timing-parity measurement** (the one SC1 property no automated test in this project can observe).
4. **Block F — delete the two orphaned live services** (old GitHub OAuth App, deployed `sveltia-cms-auth` Worker) and confirm the panel still works afterward.

None of these can be completed by this verifier: they require reading a real e-mail, waiting for a real Cloudflare rebuild, and dashboard access this session does not have.

### Gaps Summary

No code-level gaps were found. Every must-have that can be checked against the repository — the auth gate, the session cookie, the atomic write path, the Polish-only sweep with real controls, the complete removal of Sveltia's repository footprint, the CSP non-widening, the instrukcja rewrite, and the full local test/build gate (478 unit tests, 285 Playwright tests, clean check/lint, clean build with `/admin` genuinely excluded from prerender) — was independently re-verified in this session, not taken on the SUMMARY's word.

What remains is exactly what the phase's own artifacts (STATE.md `status: awaiting-human`, REQUIREMENTS.md's three Pending rows, and 04.1-UAT.md's five NIEWYKONANE blocks) already say: one live human session that produces the first real GitHub commit through the panel, confirms a real unaided login, measures the one timing property no test can see, and deletes two orphaned dashboard-only services. This is not a shortfall in the executors' work — it is proof that requires a mailbox and dashboard access this agent does not have. The honest, unticked state of CMS-01/02/03 in REQUIREMENTS.md is correct and should not be "fixed" by this verification; it should be closed by the human session itself.

---

_Verified: 2026-08-16_
_Verifier: Claude (gsd-verifier)_
