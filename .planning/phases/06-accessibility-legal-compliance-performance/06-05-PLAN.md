---
phase: 06-accessibility-legal-compliance-performance
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - src/lib/components/NewsCard.svelte
  - src/lib/components/Perks.svelte
  - src/lib/components/KeyFacts.svelte
  - src/lib/components/Hero.svelte
  - src/lib/components/Lightbox.svelte
  - src/lib/components/KontaktForm.svelte
  - src/lib/components/ZgloszenieForm.svelte
  - src/lib/components/ConsentBlock.svelte
  - src/lib/components/Recruitment.svelte
  - src/lib/components/MapPanel.svelte
  - src/lib/components/DayPlan.svelte
  - src/lib/components/AboutTeaser.svelte
  - src/lib/components/KryteriaTable.svelte
  - src/lib/components/FeeBox.svelte
  - src/lib/components/FormField.svelte
  - src/lib/components/FallbackPanel.svelte
  - src/lib/components/NewsPreview.svelte
autonomous: true
requirements: [A11Y-01, A11Y-02]
must_haves:
  truths:
    - "In the mode, every card, chip, panel and form control carries a real edge, because the shadows that used to supply it are replaced by solid borders."
    - "Photographs stay visible in the mode, with strong borders supplying the edge definition the shadows used to give. Not dimmed, not desaturated, not hidden."
    - "No control's state is signalled by colour alone in either theme: every checked, hovered and current state also carries a glyph, a border, a word or an ARIA state."
    - "The one full-width accent band on the public site does not become a yellow field under yellow text."
  artifacts:
    - "Component-scoped high-contrast override blocks in thirteen content components"
    - "A recorded verdict for each of the four components the register does not name"
  key_links:
    - "The duotone icon plates are driven by a per-component custom property, so the plate flips with the surface rather than staying white under yellow line art."
    - "The checkmark glyph is written as a border shorthand, which is why the obvious white-literal grep does not find it and why it must be taken from the register by hand."
  prohibitions:
    - "Photographs must NOT be dimmed, desaturated, filtered or hidden in the mode (D-10)."
    - "No Layer 2 rule may be written as a global rule in app.css."
    - "No base declaration may be reformatted, moved or tidied while an override is added beside it."
    - "A component the register says needs no rule must NOT be given one speculatively; record the verdict instead."
---

<objective>
Finish the component half of the high-contrast override register: cards, decorative surfaces, both
forms, and the remaining named content sites.

Purpose: the token flip reaches roughly 70 percent of the surface. The remaining 30 percent is three
named classes of defect, and this plan closes every instance of all three that lives in a component:
the hardcoded white literals (two of them invisible to the obvious grep), the five dual-role tokens
that cannot be served by one flip, and the soft-depth shadows that carry an edge with no border
fallback. D-10 additionally requires photographs to stay visible with strong borders rather than
being dimmed or hidden, so the gallery remains usable in the mode. **Real photography reached this
repository on 2026-08-18, so D-10 is no longer a rule about placeholder images.** Use the reconciled
hazard list plan 04 recorded rather than the pattern map's original totals.

Output: thirteen components with override blocks, and a recorded verdict on the four the register
does not name.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-accessibility-legal-compliance-performance/06-UI-SPEC.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-PATTERNS.md
@src/app.css
@tests/motyw-bazowy.spec.ts
</context>

<artifacts>
## Artifacts this plan produces

**Modified** — component-scoped `:global(html[data-kontrast='wysoki'])` blocks in:
`NewsCard.svelte`, `Perks.svelte`, `KeyFacts.svelte`, `Hero.svelte`, `Lightbox.svelte`,
`KontaktForm.svelte`, `ZgloszenieForm.svelte`, `ConsentBlock.svelte`, `Recruitment.svelte`,
`MapPanel.svelte`, `DayPlan.svelte`, `AboutTeaser.svelte`, `KryteriaTable.svelte`.

**Recorded verdicts (no code change expected)** — `FeeBox.svelte`, `FormField.svelte`,
`FallbackPanel.svelte`, `NewsPreview.svelte`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: cards, chips, photographs and decorative surfaces (D-09, D-10)</name>
  <files>src/lib/components/NewsCard.svelte, src/lib/components/Perks.svelte, src/lib/components/KeyFacts.svelte, src/lib/components/Hero.svelte, src/lib/components/Lightbox.svelte</files>
  <read_first>
    - `06-UI-SPEC.md` § "4. High contrast, Layer 2" registers 4f NewsCard, 4h Lightbox, 4i Perks, 4j KeyFacts, 4k Hero. Every row gives the line, the current declaration and the required override, including the four rows marked NEW that `06-CONTEXT.md` D-09 does not list.
    - `06-PATTERNS.md` § H1 rows 8, 9, 10, 11 (the three duotone plate properties and the lightbox close button) and § H2 (which of these shadows convert and which are kept).
    - `06-UI-SPEC.md` § "Discretion Decisions Recorded", the rows for photographs, the Perks accent band and full-colour brand assets.
  </read_first>
  <action>
    Implement registers 4f, 4h, 4i, 4j and 4k verbatim, each inside its own component's style block.

    Four judgements in this group carry reasoning that must survive into the code comments, because
    each of them looks like a mistake to a reader who has not read the register:

    - The one full-width accent band on the public site is THE ONE PLACE THE ACCENT FLIP IS WRONG. The
      accent token flips to the text value, which would put a full-width yellow field under yellow
      text. It gets an explicit override to the background value plus a border on both block edges so
      the band keeps its structural role. Every OTHER accent surface is a small chip or button where
      a yellow fill with a black label is correct, and those need no rule.
    - The three duotone icon plates are driven by a per-component custom property holding a white
      value. Flip it to the background value so the plate goes black and the chip reads as yellow line
      art. Two of the three are among the sites the decision list does not name.
    - Photographs (D-10): the hero image slot, the news cover, the gallery tiles and the lightbox's
      enlarged image all take a 2px border in the text value. Not dimmed, not desaturated, not
      filtered, not hidden. The border supplies exactly the edge the removed shadow used to give.
    - A card whose ONLY hover cue was a shadow needs a replacement, not a deletion: the shadow goes
      and an outline in the hover value takes its place at zero offset. A white ring is unmistakable
      and motionless.

    Two rows in this group explicitly need NO rule and the register says so, so that nobody adds one:
    the hero's background gradient has both of its stops flip to the background value and renders flat
    on its own; and the hero status pill arrives correct from Layer 1 as a black surface with a yellow
    border and white text. State both verdicts in the SUMMARY.

    Change no base declaration. Re-run the plan-01 visual baseline after each file.
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/motyw-bazowy.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lc 'data-kontrast' src/lib/components/{NewsCard,Perks,KeyFacts,Hero,Lightbox}.svelte | wc -l` returns 5.
    - `npx playwright test tests/motyw-bazowy.spec.ts` green.
    - `npm run check` reports zero errors and zero unused-selector warnings.
    - The SUMMARY records the two explicit no-rule-needed verdicts for the hero gradient and the hero status pill.
  </acceptance_criteria>
  <done>Cards, chips and photographs are legible in the mode, and every removed shadow left a border behind.</done>
</task>

<task type="auto">
  <name>Task 2: both forms, the consent checkbox and the recruitment steps</name>
  <files>src/lib/components/KontaktForm.svelte, src/lib/components/ZgloszenieForm.svelte, src/lib/components/ConsentBlock.svelte, src/lib/components/Recruitment.svelte</files>
  <read_first>
    - `06-UI-SPEC.md` § "4. High contrast, Layer 2" registers 4l (both forms), 4m (ConsentBlock) and 4n (Recruitment and the rekrutacja route).
    - `06-PATTERNS.md` § H1 rows 7, 12, 13, 14, 16 and § H2 (the two form-card shadows convert; the eleven three-dimensional submit bars are KEPT and merely recoloured).
    - `06-UI-SPEC.md` § Color, the measured ratio table: the danger token on black is 3.25 to 1 and FAILS, which is why the third high-contrast value exists and why error text is pinned to it.
  </read_first>
  <action>
    Implement registers 4l, 4m and 4n verbatim.

    Three judgements in this group:

    - The submit button's rest state sets its label to the ink token on an accent fill. Both flip to
      the text value, so the label would sit on itself. Pin the label to the background value. The
      same defect and the same fix apply to the primary call-to-action variant, and the pressed state
      of both forms carries a hardcoded white label, which is white on white after the fill inverts.
    - The consent checkmark glyph is written as a BORDER SHORTHAND, not as a colour declaration, which
      is precisely why the obvious white-literal grep does not find it. A white tick on a yellow box
      measures 1.07 to 1. Pin the glyph to the background value. Take this site from the register
      rather than from a grep.
    - A step circle filled with the brand-blue token becomes a black circle on a black page, which is
      invisible. Fill it with the text value and pin its numeral to the background value. The same
      treatment applies to the numbered-step circle in the recruitment route stylesheet, which plan 06
      handles with the rest of that route.

    The eleven three-dimensional submit bars are KEPT: they become white and remain the only depth cue
    in the mode, and they are decorative rather than informational. Do not convert them to borders.

    Errors keep their icon and their instructional wording, so nothing rests on colour in either theme;
    the colour change is a legibility fix, not a semantics change.

    Change no base declaration. Re-run the plan-01 visual baseline after each file.
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/motyw-bazowy.spec.ts tests/kontakt.spec.ts tests/rekrutacja.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lc 'data-kontrast' src/lib/components/{KontaktForm,ZgloszenieForm,ConsentBlock,Recruitment}.svelte | wc -l` returns 4.
    - `npx playwright test tests/motyw-bazowy.spec.ts tests/kontakt.spec.ts tests/rekrutacja.spec.ts` green.
    - The consent checkmark override is present and was taken from the register rather than from a grep (state this in the SUMMARY).
    - `npm run check`, `npm run lint`, `npm run test:unit` clean.
  </acceptance_criteria>
  <done>Both forms are operable and legible in the mode, including every state a parent actually reaches.</done>
</task>

<task type="auto">
  <name>Task 3: remaining named content sites, and a recorded verdict on the four unnamed components</name>
  <files>src/lib/components/MapPanel.svelte, src/lib/components/DayPlan.svelte, src/lib/components/AboutTeaser.svelte, src/lib/components/KryteriaTable.svelte, src/lib/components/FeeBox.svelte, src/lib/components/FormField.svelte, src/lib/components/FallbackPanel.svelte, src/lib/components/NewsPreview.svelte</files>
  <read_first>
    - `06-UI-SPEC.md` § "4o. Remaining named sites" — the whole table, including the three rows that explicitly need no rule.
    - `06-PATTERNS.md` § H3 closing note: two components appear in the font-size sweep but in NO register row, and the plan must confirm they need no override or add one.
    - The plan-03 SUMMARY, which records the executor's first-hand finding on those two components.
  </read_first>
  <action>
    (a) Implement the four rows in register 4o that require an override:
    - the map panel's static raster gains a 2px border in the text value (D-10);
    - the day-plan panel gains a 2px border, because its tint background and dashed subtle separators
      both flip to the background value and the panel would otherwise have no edge at all;
    - the about-teaser blockquote's left rule is drawn in an expressive token, which flips to the
      background value, so the quote loses its rule entirely and must take the text value;
    - the criteria table's zebra stripe alternates two surfaces that both flip to the background
      value, so the rows fuse into one block. Give every row a 1px bottom border in the text value.
      That stripe is decorative in the default theme and STRUCTURAL here, which is the reason the rule
      cannot be skipped as cosmetic.

    (b) Confirm and RECORD the verdict on the four components the register does not require a rule for.
    Two are named in the register as needing none (the fee box and the form field both arrive correct
    from Layer 1). Two appear in the font-size sweep but in no register row at all. For each of the
    four, open the file and check three things: a hardcoded white literal in any notation including a
    border shorthand and an alpha value; a soft-depth shadow with no border fallback; and an edge that
    exists only because of a tint background. Write the verdict for each into the SUMMARY with the
    evidence. Add an override only where one of the three is actually present. A speculative override
    is a rule nobody can later justify or safely delete.

    Change no base declaration. Re-run the plan-01 visual baseline after each file.
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/motyw-bazowy.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lc 'data-kontrast' src/lib/components/{MapPanel,DayPlan,AboutTeaser,KryteriaTable}.svelte | wc -l` returns 4.
    - The SUMMARY carries a named verdict, with evidence, for each of the four unnamed components.
    - `npx playwright test tests/motyw-bazowy.spec.ts` green.
    - `npm run check` reports zero errors and zero unused-selector warnings.
  </acceptance_criteria>
  <done>Every component-level override the register requires exists, and the four the register is silent about have a recorded answer rather than an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor preference to rendered contrast | the attribute repaints every content surface, including the two forms a parent submits personal data through |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Denial of Service | both public forms in the mode | **high** | mitigate | An invisible submit label or an unreadable validation error in the mode makes the enrolment form unusable for exactly the visitor the mode exists for, and the site stores nothing, so a lost enquiry is lost for good. Mitigation: registers 4l and 4m implemented in full, plus the existing form specs re-run, plus plan 06's high-contrast axe sweep over both form routes. |
| T-06-05-02 | Tampering | seventeen component stylesheets | high | mitigate | Same default-theme regression risk as plan 04, at greater volume. Mitigation: every rule gated on an absent attribute, plus the plan-01 visual baseline after each file. |
| T-06-05-03 | Information Disclosure | the consent checkmark and the two-value pairs | medium | mitigate | A glyph at 1.07 to 1 is invisible, so a parent cannot tell whether the RODO consent box is ticked. Mitigation: the glyph is pinned to the background value, and the native checked state carries the semantics for assistive technology regardless of paint. |
| T-06-05-04 | Repudiation | the four unnamed components | medium | mitigate | A component nobody checked is a component nobody knows about. Mitigation: a recorded verdict with evidence for each, rather than silence. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan; this phase installs zero packages. Carried per the phase security contribution. |
</threat_model>

<verification>
1. `npm run check && npm run lint && npm run test:unit` green.
2. `npx playwright test tests/motyw-bazowy.spec.ts` green.
3. `npx playwright test tests/kontakt.spec.ts tests/rekrutacja.spec.ts tests/galeria.spec.ts tests/home.spec.ts` green.
4. Manual observation with the attribute set from the console, recorded with screenshots in the
   SUMMARY: homepage, `/o-nas` with the gallery and one photo opened in the lightbox, `/kontakt` with
   a validation error rendered, `/rekrutacja`, `/cennik`. Name anything still wrong; plan 06 owns the
   six route stylesheets and needs the list.
</verification>

<success_criteria>
- Thirteen components carry their register rows.
- The four components the register does not name carry a recorded verdict with evidence.
- Photographs are visible with borders; nothing is dimmed, desaturated or hidden.
- The default theme is provably unchanged across seventeen edited files.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-legal-compliance-performance/06-05-SUMMARY.md` when done.
Record: every register row implemented, the no-rule-needed verdicts with reasons, the four unnamed
components' verdicts with evidence, and the observed worklist for plan 06.
</output>
