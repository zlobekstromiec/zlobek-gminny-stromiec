---
phase: 06-accessibility-legal-compliance-performance
plan: 12
type: execute
wave: 10
depends_on: ["06-10", "06-11"]
files_modified:
  - src/lib/server/admin/walidacja/polityka.ts
  - src/lib/pola-strony.ts
  - src/routes/admin/polityka-prywatnosci/+page.server.ts
  - src/routes/admin/polityka-prywatnosci/+page.svelte
  - src/lib/content/panel.ts
  - src/routes/admin/+page.svelte
  - tests/admin-walidacja-polityka.unit.ts
  - tests/admin-polityka.spec.ts
  - tests/fixtures/trasy-panelu.ts
  - tests/admin-copy.unit.ts
  - tests/admin-pulpit.spec.ts
  - docs/instrukcja-cms.md
  - tests/instrukcja.unit.ts
autonomous: true
requirements: [LEGAL-02]
must_haves:
  truths:
    - "A logged-in editor can open the polityka screen from the panel dashboard, change the four site-wide prose blocks and the last-updated date, press Zapisz once, and see the change committed."
    - "The twelve klauzula blocks appear on the screen READ-ONLY, rendered as text with a hint naming where they live and why they are shared, never as a greyed-out control."
    - "The validator refuses an empty required prose block, because that section renders on the public page and cannot be blank."
    - "The panel route list reaches nineteen entries, so both new screens are swept for Polish."
  artifacts:
    - "src/lib/server/admin/walidacja/polityka.ts"
    - "src/routes/admin/polityka-prywatnosci/+page.server.ts and +page.svelte"
    - "tests/admin-walidacja-polityka.unit.ts"
    - "tests/admin-polityka.spec.ts"
  key_links:
    - "The read-only hint names the shared source and the reason: editing the blocks here would desync the two RODO texts, which is exactly what D-15 exists to prevent."
    - "The panel route list goes from eighteen to nineteen; the on-disk route walk is what makes a missing entry red rather than silent."
  prohibitions:
    - "The twelve klauzula blocks must NOT be editable on this screen, and must NOT be rendered as disabled inputs."
    - "This screen must NOT join the panel navigation; it is reached from a dashboard tile only, and the navigation stays at nine items."
    - "Nothing in the validator logs a field value."
    - "Neither new panel route may import anything carrying node:fs."
    - "LEGAL-02 stays UNTICKED."
---

<objective>
Give the privacy policy an editor, and take the panel route list to its final count for this phase.

Purpose: D-14 makes both legal pages panel-editable stores. This is the smaller of the two screens on
purpose. The twelve klauzula blocks are read-only here because D-15 makes them shared with both forms,
so the only editable content is the four site-wide sections plus the last-updated date.

**After this plan a real editor can do something they could not do before**: update the hosting,
cookie, browser-storage and bulletin sections of the privacy policy without a developer.

Output: the validator, the screen, the enumeration entries that take the route list to nineteen, and
the staff-manual section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-accessibility-legal-compliance-performance/06-UI-SPEC.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-PATTERNS.md
@src/lib/server/admin/walidacja/cennik.ts
@src/routes/admin/cennik/+page.server.ts
@src/routes/admin/cennik/+page.svelte
@src/routes/admin/deklaracja-dostepnosci/+page.svelte
@src/lib/content/forms.ts
@src/lib/content/panel.ts
@tests/fixtures/trasy-panelu.ts
</context>

<artifacts>
## Artifacts this plan produces

**New files**
- `src/lib/server/admin/walidacja/polityka.ts`
- `src/routes/admin/polityka-prywatnosci/+page.server.ts`
- `src/routes/admin/polityka-prywatnosci/+page.svelte`
- `tests/admin-walidacja-polityka.unit.ts`
- `tests/admin-polityka.spec.ts`

**New exported symbols**
- `src/lib/server/admin/walidacja/polityka.ts`: `SCIEZKA_POLITYKA` (`'src/lib/content/polityka.json'`), `walidujPolityke(zrodlo)`, `interface PolitykaDane` in the committed key order, plus the named length caps.
- `src/lib/pola-strony.ts`: the field-name constants for this screen and `wartosciPolityki(dane)`.
- `src/lib/content/panel.ts`: `KOPIA_POLITYKA`, `POLA_POLITYKA`, the dashboard-tile strings and the page title.
- `src/routes/admin/polityka-prywatnosci/+page.server.ts`: `interface WynikPolityki`.

**New panel route**
- `/admin/polityka-prywatnosci` — session-gated, no prerender, dashboard tile only.

**Modified**
- `tests/fixtures/trasy-panelu.ts` — 18 entries become 19, the final count for this phase.
- `src/routes/admin/+page.svelte` — ten dashboard cards become eleven.
- `docs/instrukcja-cms.md` and `tests/instrukcja.unit.ts` — one new manual section.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the validator and its unit suite</name>
  <files>src/lib/server/admin/walidacja/polityka.ts, src/lib/pola-strony.ts, tests/admin-walidacja-polityka.unit.ts</files>
  <read_first>
    - `src/lib/server/admin/walidacja/cennik.ts` in full, and `src/lib/server/admin/walidacja/deklaracja.ts` as landed by plan 10 (the closer sibling: same phase, same conventions, same date control).
    - `tests/admin-walidacja-cennik.unit.ts` — the suite shape including the byte-for-byte serialization assertion against the real store file.
    - `06-UI-SPEC.md` § "11. Panel: `/admin/polityka-prywatnosci`", the field-group table and the two-row validation copy table.
  </read_first>
  <behavior>
    - An absent or empty required prose block is refused with the message that names the consequence: the section appears on the page and cannot be blank.
    - An incomplete date is refused with the date message.
    - A value longer than its named cap is refused with a message quoting the cap the server enforced.
    - The result is constructed key by key in the committed key order, never spread from the source, so a forged extra key cannot reach the store.
    - A valid source serializes byte-identically to the committed store file.
    - The exported store path points at a file that exists on disk.
    - Nothing in the module writes to the console on any path.
  </behavior>
  <action>
    Write `src/lib/server/admin/walidacja/polityka.ts` following the fees validator and the declaration
    validator verbatim in shape. Same four load-bearing properties: relative imports carry an explicit
    file extension so bare `node --test` can load the module; the store path is exported and pinned
    against the filesystem, because a save writing an unread path reports success, produces a real
    commit and a real build and changes nothing visible; the interface IS the committed key order; every
    field is read before anything is refused and the result is built key by key.

    There is NO klauzula field in this validator. The twelve blocks are read-only on the screen and live
    in the forms content module, so a field for them would be the first step toward the drift D-15
    exists to prevent. If a request arrives carrying one, it is simply not read, which is what a closed
    allowlist means.

    Nothing logs.

    Add the field-name constants and the value-capture helper to `src/lib/pola-strony.ts`.

    A red-first commit is impossible in this repository. Observe the suite red before the module exists,
    RECORD it in the SUMMARY, then land both together. Never use `--no-verify`.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` green, including the byte-for-byte serialization assertion.
    - `grep -c 'console\.' src/lib/server/admin/walidacja/polityka.ts` returns 0.
    - `grep -ci 'klauzul' src/lib/server/admin/walidacja/polityka.ts` returns hits only inside comments explaining the omission, and no field is read for it.
    - Mutation check recorded and reverted: make one required prose block optional and confirm the suite goes red.
    - `npm run check` and `npm run lint` clean.
  </acceptance_criteria>
  <done>The site-wide half of the policy has a closed allowlist in front of it, and the shared half is structurally out of reach.</done>
</task>

<task type="auto">
  <name>Task 2: the panel screen, its save path and its read-only klauzula block</name>
  <files>src/routes/admin/polityka-prywatnosci/+page.server.ts, src/routes/admin/polityka-prywatnosci/+page.svelte, src/lib/content/panel.ts</files>
  <read_first>
    - `src/routes/admin/cennik/+page.server.ts` and `src/routes/admin/cennik/+page.svelte` in full.
    - `src/routes/admin/deklaracja-dostepnosci/+page.svelte` as landed by plan 10 — the closest sibling and the one to stay consistent with.
    - `06-UI-SPEC.md` § "11", the field-group table and the whole panel copy table for this screen.
    - `src/routes/admin/cennik/+page.svelte:16-19` — the read-only presentation ruling, verbatim, which is the exact sentence to reuse.
    - `src/lib/content/forms.ts` — the twelve blocks this screen displays.
  </read_first>
  <action>
    (a) `src/lib/content/panel.ts` gains this screen's Polish strings verbatim from the copy table:
    page title, dashboard-tile heading and description, heading, lead, the six group legends, every
    label and hint, the read-only hint and the saved link. The lead must say what the editor changes
    here AND that the klauzula under the forms is shared and therefore changes together with the forms.

    Every new export joins the explicit sweep list in `tests/admin-copy.unit.ts`, whose length assertion
    against the module's export count makes an omission loud.

    (b) `+page.server.ts`: the singleton screen template verbatim. Validate, serialize, save, redirect;
    head hash read on load; secrets from the platform environment only; nothing logs; the authentication
    boundary is the shared server hook. The named save action carries the same four branches in the same
    cheapest-first order, with the underlying detail of a save error deliberately not rendered.

    (c) `+page.svelte`: the screen skeleton in order, matching the declaration screen from plan 10.

    Six field groups:
    - the klauzula group, READ-ONLY;
    - browser settings, one textarea at the standard minimum height;
    - hosting and server logs, one textarea;
    - cookies, one textarea;
    - the public-information bulletin, one textarea;
    - the last-updated date, a three-select date control, never a native date input.

    THE READ-ONLY GROUP IS THE ONE THING THAT MAKES THIS SCREEN DIFFERENT AND IT HAS A RULING. The
    twelve blocks render as TEXT plus a hint. NEVER as a disabled input: a control nobody may type into
    looks like a control somebody forgot to enable, and it is skipped by keyboard navigation with no
    explanation. That exact sentence already exists in the fees screen; reuse it rather than inventing
    a variant. The hint names why and where: the blocks are shared with both forms and this page, the
    copy sweep pins three of their sentences, and editing them here would desync the two RODO texts,
    which is precisely what D-15 exists to prevent.

    The screen works with JavaScript disabled end to end. It has no repeatable group, so it avoids two
    of the three framework traps the declaration screen had to handle, but the named-action rule still
    applies: the save is a named action, not a default one.

    (d) Do NOT import anything carrying `node:fs` into this route.
  </action>
  <verify>
    <automated>npm run check && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` reports zero errors; `npm run test:unit` green including the extended copy sweep.
    - `grep -rn "node:fs\|server/dokumenty\|lib/zastepcze" src/routes/admin/polityka-prywatnosci/` returns nothing.
    - `grep -c 'type="date"' src/routes/admin/polityka-prywatnosci/+page.svelte` returns 0.
    - The klauzula group renders as text: no input, textarea or select element exists inside it, and no element inside it carries a disabled attribute.
    - `grep -c 'console\.' src/routes/admin/polityka-prywatnosci/+page.server.ts` returns 0.
    - `npm run lint` clean.
  </acceptance_criteria>
  <done>An editor can maintain the site-wide half of the policy, and can see the shared half without being able to break it.</done>
</task>

<task type="auto">
  <name>Task 3: enumeration surfaces to nineteen, the staff manual, and the acceptance suite</name>
  <files>tests/fixtures/trasy-panelu.ts, src/routes/admin/+page.svelte, tests/admin-copy.unit.ts, tests/admin-pulpit.spec.ts, tests/admin-polityka.spec.ts, docs/instrukcja-cms.md, tests/instrukcja.unit.ts</files>
  <read_first>
    - `06-PATTERNS.md` § "The five enumeration surfaces a new panel screen must join".
    - `06-UI-SPEC.md` § "12. Panel: pulpit and the enumeration surfaces", including the two surfaces deliberately NOT touched.
    - The plan-10 SUMMARY, for the counted route-list length after the first new screen.
    - `tests/admin-cennik.spec.ts` — the panel screen acceptance idiom.
  </read_first>
  <action>
    (a) The route list gains this screen's URL, after the declaration entry and before the help entry,
    with the same explanatory comment shape. The list holds EIGHTEEN after plan 10 and reaches NINETEEN
    here, which is its final count for this phase. Count the array; do not trust a naive count of the
    file, which also matches the type declaration line.

    (b) The dashboard grows from ten cards to eleven, inserted after the declaration card and before the
    help card, written out rather than looped. No counter: there is no count worth showing.

    (c) The dashboard test's tile assertions go from ten to eleven.

    (d) The panel navigation and the panel path list stay untouched. The navigation stays at NINE items.
    Say so in the SUMMARY.

    (e) `docs/instrukcja-cms.md` gains one section for this screen, gated by `tests/instrukcja.unit.ts`.
    It must state plainly, in Polish, that the klauzula under the forms is shared and is changed by the
    person who looks after the site, not on this screen, and that the four sections here are the ones
    the staff member owns.

    (f) `tests/admin-polityka.spec.ts`: renders for a signed-in editor and redirects for one who is not;
    the validation summary links to the offending control; a refused save preserves every typed value;
    the klauzula group renders as text with no form control and no disabled attribute inside it; axe
    reports zero violations on a clean screen and with an invalid state rendered.
  </action>
  <verify>
    <automated>npm run test:unit && npx playwright test tests/admin-polityka.spec.ts tests/admin-enumeracja.spec.ts tests/admin-polski.spec.ts tests/admin-pulpit.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - The four named Playwright files are green and `npm run test:unit` is green.
    - The route list array holds exactly 19 entries, counted from the array.
    - The navigation list and the panel path list are unchanged in `git diff`.
    - The manual section states that the klauzula is shared and is not edited on this screen.
    - Mutation check recorded and reverted: remove the new route-list entry and confirm the on-disk route walk goes red.
  </acceptance_criteria>
  <done>Both new screens are swept for Polish, reachable from the dashboard, and documented for the staff member who will use them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated editor to committed content | four prose blocks and one date travel into a commit and onto a public RODO page |
| shared copy to per-page copy | the twelve klauzula blocks are visible on this screen but owned elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12-01 | Tampering | the four prose blocks | high | mitigate | Untrusted input despite an authenticated sender, rendering on a public legal page. Mitigation: closed-allowlist validator, absent is a refusal, key-by-key construction, and public rendering through the already-hardened renderer. |
| T-06-12-02 | Tampering | drift between the two RODO texts | high | mitigate | An editable klauzula on this screen would let the page's copy diverge from the copy shown at the moment of collection. Mitigation: no field exists for it in the validator and no control exists for it on the screen; it renders as text with a hint stating the reason. |
| T-06-12-03 | Information Disclosure | validation logging | medium | mitigate | Nothing in the validator or the server file writes to the console, asserted by grep. |
| T-06-12-04 | Repudiation | a screen with zero Polish coverage | medium | mitigate | The route-list entry lands in the same commit as the route, and the mutation check proves the on-disk walk turns the gap red. |
| T-06-12-05 | Denial of Service | a disabled control as read-only presentation | low | mitigate | A greyed-out control is skipped by keyboard navigation with no explanation, which on an accessibility-certification phase's own panel would be an embarrassing defect. Mitigation: text plus hint, asserted by the suite. |
| T-06-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan. Carried per the phase security contribution. |
</threat_model>

<verification>
1. `npm run check && npm run lint && npm run test:unit && npm run test` all green.
2. The three mutation checks (required prose block, route-list entry, and a manual no-JavaScript save), recorded then reverted.
3. `npm run gate:launch` re-run and diffed against `06-BRAMA.md`; unchanged by this plan.
4. Confirm LEGAL-02 is still unticked in the requirements traceability table.
</verification>

<success_criteria>
- The screen exists, saves, refuses an empty required block in Polish, and works with scripting disabled.
- The twelve klauzula blocks are visible, read-only, rendered as text, with a hint naming the shared source.
- The panel route list reaches nineteen; the navigation stays at nine.
- The staff manual covers both new screens.
- LEGAL-02 stays UNTICKED, by design, until Phase 7 names the data-protection officer.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-legal-compliance-performance/06-12-SUMMARY.md` when done.
Record: the counted route-list length, the mutation observations, the no-JavaScript save result, and
the explicit statement that the navigation was deliberately not touched.
</output>
