---
phase: 06-accessibility-legal-compliance-performance
plan: 14
type: execute
wave: 11
depends_on: ["06-12", "06-13"]
files_modified:
  - .planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md
  - .planning/phases/06-accessibility-legal-compliance-performance/06-BRAMA.md
  - .planning/phases/06-accessibility-legal-compliance-performance/06-UAT.md
  - docs/dev-env.md
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SITE-05, A11Y-01, A11Y-03, LEGAL-02]
must_haves:
  truths:
    - "The phase's performance change is measured rather than assumed, and the record says plainly which numbers are emulated proxies and which are the live before-numbers they are being compared against."
    - "npm run gate:launch is run one last time, is RED, and every red item is reconciled against the expected list written when the gate was built."
    - "The stale panel route counts in every project document are corrected to the real number in one sweep, so no document says one number while the file says another."
    - "Every one of the ten manual-only verifications, plus the four carried human sessions, exists as a row in a single acceptance sheet with instructions somebody else could follow."
  artifacts:
    - ".planning/phases/06-accessibility-legal-compliance-performance/06-UAT.md"
    - "The PO section of 06-WYDAJNOSC.md"
    - "The final reconciliation section of 06-BRAMA.md"
  key_links:
    - "The acceptance sheet is what carries A11Y-03's held-out oracle, SITE-05's live after-measurement, and the two human sessions this phase absorbed from earlier phases."
    - "The gate's final red item list is what Phase 7's definition of done is written against."
  prohibitions:
    - "npm run gate:launch must NOT be made green, and no acceptance criterion here may require it."
    - "A11Y-03 and LEGAL-02 must NOT be ticked. Both are authored in full and stay unmarked until Phase 7 supplies the five outstanding content classes."
    - "The noindex guard must NOT be flipped, robots.txt must NOT be changed, the sitemap must NOT be rewritten, the share card must NOT be regenerated, no stub document must be replaced, and the repository must NOT be made private. Every one of those is Phase 7."
    - "No placeholder content may be replaced with a real value."
---

<objective>
Close the phase: measure what changed, reconcile the gate against its own expected list, correct the
stale counts, and write the acceptance sheet.

Purpose: this phase makes four claims that only evidence can support. It says the site is AA
conformant, which the audit record and the automated suites support. It says the mobile numbers did
not regress unacceptably, which needs an after-measurement against the before-numbers taken in plan 01.
It says the launch gate is red ON PURPOSE, which is only meaningful if the red items match a list
written before anyone knew what the gate would find. And it defers thirteen properties to human
sessions, which are only real if they are written down where somebody else can run them.

Output: the performance record's second half, the gate reconciliation, one sweep of stale counts, and
`06-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-accessibility-legal-compliance-performance/06-VALIDATION.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-CONTEXT.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-RESEARCH.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-BRAMA.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-AUDYT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/dev-env.md
</context>

<artifacts>
## Artifacts this plan produces

**New file**
- `.planning/phases/06-accessibility-legal-compliance-performance/06-UAT.md` — the phase acceptance sheet: ten manual-only verifications plus the four carried human sessions (D-24 parts B and C, D-28 rows B4 and B2), each with instructions, an expected outcome and a result column.

**Modified**
- `06-WYDAJNOSC.md` — the PO section, with the emulated proxy numbers and an explicitly empty live row for the acceptance session.
- `06-BRAMA.md` — the final reconciliation: the gate's last red output against the expected list, with every difference explained.
- `docs/dev-env.md`, `.claude/CLAUDE.md`, `.planning/REQUIREMENTS.md` — the stale panel route count corrected in one sweep.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: the performance after-measurement proxy, and the honest gap (D-25)</name>
  <files>.planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md</files>
  <read_first>
    - `06-WYDAJNOSC.md` as written in plan 01: the instrument, the throttling settings, the three routes, the five-run median method, the discarded warm-up, and the named LCP element per route.
    - `06-RESEARCH.md` § "4. Lighthouse methodology" — the variance expectation, the reason the first cold run is discarded, and the closing paragraph on what this phase adds.
    - `06-UI-SPEC.md` § "Performance contract (SITE-05)" — the seven constraints the user interface was not allowed to violate.
    - The plan-07 SUMMARY for the widget's measured client weight, and the plan-08 SUMMARY confirming the funding strip ships no image.
  </read_first>
  <action>
    Measure the after-state with the SAME instrument, the SAME throttling settings, the SAME three
    routes and the SAME five-run median method as plan 01, so the comparison is a comparison.

    THE HONEST CONSTRAINT, AND IT MUST BE STATED RATHER THAN GLOSSED: plan 01's before-numbers were
    taken against the LIVE deployment, as D-25 requires. This phase's code is committed but not yet
    deployed, so a like-for-like after-measurement cannot be taken here. Do therefore both of the
    following and label them differently:

    (a) Run the measurement against a LOCAL PRODUCTION BUILD served by the project's own preview
    command, under the same throttled mobile profile. Record it under a heading that names it as an
    emulated proxy, not as the live after-number. Its job is to catch a gross regression BEFORE anybody
    pushes, which is worth having and is not worth pretending is more than it is.

    (b) Leave the live after-measurement as an explicitly empty section with the instrument, the
    settings and the route list already filled in, so the acceptance session has only to run it and
    write the numbers. It is a row in `06-UAT.md` from task 3.

    Then write the analysis, and let it be allowed to say the numbers got slightly worse. This phase
    ADDS work to every route: a fifth island, a funding strip in the footer, two rebuilt pages and 167
    rewritten type declarations. The performance contract is what keeps the regression bounded, not the
    hope that it will not happen. Check each of its seven constraints against what actually shipped and
    record a verdict for each: zero new render-blocking assets, zero layout shift from the widget, the
    widget's client weight against its budget, no runtime cost for the type scale at multiplier one, no
    style recalculation from the contrast mechanism when it is off, no unsized image in the funding
    strip, and no image on either legal page.

    D-25 forbids a performance budget as a test, so nothing here becomes an assertion. The record is the
    deliverable.
  </action>
  <verify>
    <automated>grep -c 'PO' .planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md && grep -c 'LCP' .planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-WYDAJNOSC.md` carries a proxy after-section with three route tables in the same shape as the before-section.
    - It carries a clearly separate, explicitly empty live after-section with the instrument and settings pre-filled.
    - It carries a verdict for each of the seven performance-contract constraints.
    - It states in one sentence which comparison is like-for-like and which is not.
    - No timing assertion was added to any test suite.
  </acceptance_criteria>
  <done>The phase's performance change has a number and an honest label, and the live comparison is set up rather than skipped.</done>
</task>

<task type="auto">
  <name>Task 2: the final gate reconciliation and the stale-count sweep</name>
  <files>.planning/phases/06-accessibility-legal-compliance-performance/06-BRAMA.md, docs/dev-env.md, .claude/CLAUDE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `06-BRAMA.md` as written in plan 02: the verbatim red output at gate-creation time, the expected item list with a reason per item, and the note predicting the count would grow in plans 08, 09 and 11.
    - `06-RESEARCH.md` § "Pitfall 6" and § "State of the Art", the deprecated-in-our-own-documents list: four documents state a panel route count that is wrong, and the reason a naive count of the file overcounts by one.
    - `.claude/CLAUDE.md` — the sentence stating a panel route count.
    - `.planning/REQUIREMENTS.md` CMS-03 — the same count in its note.
    - `docs/dev-env.md` — the verify gate and the launch-gate section added in plan 02.
    - The plan-10 and plan-12 SUMMARY entries recording the counted array length.
  </read_first>
  <action>
    (a) Run `npm run gate:launch` one final time and append its verbatim output to `06-BRAMA.md` under
    a closing heading, with the exit code.

    RECONCILE, item by item, against the expected list written when the gate was built. Every
    difference must be explained by a change this phase made and nothing else:
    - four marker hits ADDED by the funding strip's placeholder chips;
    - two marker hits REMOVED with the two legal stubs;
    - several content flags ADDED by the two new legal stores;
    - the go-live checklist items unchanged, because this phase touched none of them.

    ANY ITEM THAT CANNOT BE EXPLAINED THAT WAY IS A DEFECT IN THIS PHASE, NOT A PHASE 7 ITEM, and it is
    fixed here. This reconciliation is the whole reason the expected list was written before the phase
    ran; without it, "red on purpose" is indistinguishable from "red and nobody looked".

    Then write the sentence Phase 7's definition of done is written against: the exact list of items
    that must go from red to green, in the gate's own wording.

    THE GATE MUST STILL BE RED AND MUST NOT BE MADE GREEN. That is the deliverable.

    (b) The stale-count sweep. The panel route list holds a specific number of entries, and several
    project documents state a different one, all of them wrong, because a naive count of that file also
    matches its type declaration line. A document saying one number while the file says another gets
    reconciled by somebody deleting a route, which is exactly the silent failure that list exists to
    prevent. Correct every one of them in a single sweep to the counted number after plans 10 and 12,
    and record where each correction was made. Sweep at minimum: the project instructions file, the
    requirements note under the Polish-panel requirement, and the developer environment document. Check
    the planning state document and the earlier phase specification too; where a correction would be
    rewriting a historical record rather than a live claim, note it instead of editing it.

    (c) `docs/dev-env.md` gets its final pass for this phase: the four-command verify chain named in
    full, the launch-gate section stating it is red by design until Phase 7, and the counted route
    number wherever it appears.
  </action>
  <verify>
    <automated>npm run gate:launch; echo "exit=$?"; grep -c 'CZERWONE' .planning/phases/06-accessibility-legal-compliance-performance/06-BRAMA.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run gate:launch` exits non-zero and its final output is appended verbatim to `06-BRAMA.md`.
    - Every red item is reconciled against the expected list, with the four additions and two removals named.
    - `06-BRAMA.md` ends with the exact red-to-green list Phase 7 is accountable to.
    - `grep -rn "18 panel\|osiemnast" .claude/CLAUDE.md .planning/REQUIREMENTS.md docs/dev-env.md` returns nothing stating a stale count as a live claim.
    - No file under `static/`, no noindex default, no robots file, no sitemap and no stub document was modified by this plan.
  </acceptance_criteria>
  <done>The gate's red state is evidenced against a list written in advance, and no document in the project states a route count the file disagrees with.</done>
</task>

<task type="auto">
  <name>Task 3: the phase acceptance sheet (D-24, D-27 tier 3, D-28)</name>
  <files>.planning/phases/06-accessibility-legal-compliance-performance/06-UAT.md</files>
  <read_first>
    - `06-VALIDATION.md` § "Manual-Only Verifications" — all ten rows, each with its behaviour, its requirement, why it is manual and its instructions. Every one is carried into this sheet; the validation contract says so.
    - `06-CONTEXT.md` D-24, D-27 and D-28.
    - `.planning/STATE.md` "Blockers/Concerns", the verification debt entry describing exactly what parts B and C of the rate-limit re-check still owe and why part A passing is not evidence.
    - `.planning/phases/04.1-replace-sveltia-with-custom-polish-cms/04.1-UAT.md` — the sheet format this project uses, and row B4.
    - `.planning/phases/05-gallery-fees/05-UAT.md` — the most recent example.
    - The plan-07, 09, 11 and 13 SUMMARY entries, for the specific things each deferred to this sheet.
  </read_first>
  <action>
    Write `06-UAT.md` in this project's established sheet format: an identifier, what to do, what to
    expect, and a result column, per row. Polish prose. Somebody who did not build this phase must be
    able to run every row from the sheet alone.

    Twelve rows, and none of them may be merged away:

    From the validation contract's manual-only list:
    1. High-contrast legibility as rendered: screenshot all nine public routes in the mode and review.
       The automated halves are necessary and not sufficient; passing a ratio and being legible are
       different claims.
    2. Screen-reader announcement quality (D-27 tier 3): one recorded session covering the widget
       trigger's name and expanded state, the segmented control announcing its full Polish suffix rather
       than the bare glyph, the reset status line firing, and the declaration's headings reading as
       meaningful sections.
    3. The perceived size of the pre-hydration flash: once, on a phone, on mobile data, against the live
       deployment. This is the only evidence that can retire the accepted cost recorded in D-12; pair it
       with the millisecond figure plan 07 measured.
    4. Core Web Vitals on real hardware: one session on an actual mid-range Android phone over mobile
       data, all three routes. Emulation is a model; if the phone contradicts it, the emulated numbers
       were the wrong ones. THIS ROW ALSO CARRIES THE LIVE AFTER-MEASUREMENT plan 14 task 1 set up.
    5. Is the declaration comprehensible Polish: one reading by a Polish-speaking non-developer. The
       statutory phrasing is prescribed, but the architecture, communication and additional-information
       blocks are ours.
    6. Independent structural validation of the declaration: run the DEPLOYED page through the public
       validator. Expected in Phase 6: it flags the outstanding content classes as empty, which is
       correct, and passes everything else. ANY OTHER FAILURE IS A REAL DEFECT. This is the held-out
       oracle for A11Y-03's structural half and it was not used to build the page.
    7. Default theme did not regress: compare against the plan-01 visual baseline. Note in the row that
       the baseline was re-based four times, each authorised by a named contract, and list them, so the
       reviewer knows which differences are expected.
    8. Tap-target measurement: performed in plan 13. Carry the row with its recorded result rather than
       repeating it, and note any second breach found and its disposition.
    9. The third-party challenge widget's own accessibility: manual contrast and keyboard-reachability
       check. Automated tooling cannot see into a cross-origin frame and a real managed widget refuses
       to be driven.

    From the absorbed debts:
    10. D-24, the rate-limit reset re-check, part B: from the SAME device, submit once at or after the
        top of the next clock hour with no site silence in between, and confirm it is ACCEPTED. That
        reset is the only behaviour that distinguishes the fix from the bug; part A passing is not
        evidence, because the pre-fix build refused inside the bucket too.
    11. D-24 part C: on a new date in coordinated universal time, confirm a fresh daily counter key is
        used rather than the previous date's continuing to climb.
    12. D-28, the carried row B4: the stale-save conflict panel, which needs only a second editor in a
        second browser tab and no client input at all.
    13. D-28, the carried row B2, RETURNED TO THIS PHASE on 2026-08-18: upload a photograph taken on a
        phone through the panel and confirm `git show --stat` reports exactly two files sharing one
        basename. This row was routed to Phase 7 on the ground that it needed a real photograph from
        the zlobek and none existed; quick task `260818-i5t` then landed the director's first batch of
        seven, so the blocking input now exists and the row comes home. **Record provenance when
        running it:** if those files are processed exports rather than camera originals from an
        iPhone, the HEIC assumption open since 04.1 is still untested, and this row must say so
        rather than be ticked.

    Add a short closing section stating plainly what this phase proved and what it did not, in the style
    this project already uses for its two blocked form requirements:
    - A11Y-03 and LEGAL-02 are AUTHORED IN FULL and deliberately UNTICKED, because a declaration missing
      its accessibility coordinator is non-conformant and separately fineable, and a privacy page
      missing its data-protection officer is incomplete. Phase 7 supplies both names and closes them.
      This is the same shape as Phase 4 closing complete with one requirement blocked and one pending,
      not a new pattern.
    - The launch gate is RED ON PURPOSE and its red items are the Phase 7 work list.
    - Five content classes on the declaration ship as marked placeholders because no source for them
      exists in this project.

    That is tracked debt with a named owner, not a silent descope, and saying so in the phase's own
    acceptance sheet is what keeps it that way.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-accessibility-legal-compliance-performance/06-UAT.md && grep -c '^|' .planning/phases/06-accessibility-legal-compliance-performance/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-UAT.md` exists and carries thirteen rows, each with instructions, an expected outcome and an empty result column.
    - Every one of the ten manual-only verifications from the validation contract appears; none is merged into another.
    - The two rate-limit rows state explicitly why part A passing is not evidence.
    - The closing section states what is authored-and-unticked, what is red on purpose, and what is a marked placeholder, with the reason for each.
    - The validation contract's sign-off list can be checked against this sheet row by row.
  </acceptance_criteria>
  <done>Everything this phase could not prove itself is written down where somebody else can prove it, with the reason it could not be automated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase evidence to phase close | this plan decides what the phase claims to have proven |
| Phase 6 to Phase 7 | the gate's final red list is the handover contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14-01 | Repudiation | the launch gate reconciliation | **high** | mitigate | "Red on purpose" is indistinguishable from "red and nobody looked" unless every red item is matched to an expectation written before the phase ran. Mitigation: item-by-item reconciliation against the plan-02 expected list, with any unexplained item treated as a defect in this phase rather than as Phase 7 work. This is the closing act on the open threat T-05-09-05. |
| T-06-14-02 | Repudiation | a like-for-like performance claim that is not like-for-like | medium | mitigate | Comparing a local emulated after-number against a live before-number and presenting them as a comparison would be a false claim. Mitigation: the proxy is labelled as a proxy, the live section is left explicitly empty, and the acceptance sheet carries the row that fills it. |
| T-06-14-03 | Repudiation | ticking a requirement the phase did not close | **high** | mitigate | Two requirements are authored in full and look finished. Ticking either would put a false claim in the traceability table, exactly as happened once before on this project with the panel requirements. Mitigation: an explicit prohibition, an acceptance criterion, and a closing section in the sheet stating the reason each stays unmarked. |
| T-06-14-04 | Tampering | the stale route counts | medium | mitigate | A document saying one number while the file says another gets reconciled by somebody deleting a route, and that route then has zero Polish coverage. Mitigation: one sweep, one number, recorded per site. |
| T-06-14-05 | Elevation of Privilege | Phase 7 work performed here | high | mitigate | Every remaining gate item is a one-line change and all of them are tempting at phase close. Mitigation: an explicit prohibition list naming each, plus an acceptance criterion asserting no such file was modified. |
| T-06-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan. This phase's total supply-chain movement is nil, which is load-bearing rather than incidental: the entire commercial accessibility-overlay category D-01 refuses is a third-party script registry by another name. Carried per the phase security contribution. |
</threat_model>

<verification>
1. `npm run check && npm run lint && npm run test:unit && npm run test` all green.
2. `npm run gate:launch` exits non-zero and every red item is reconciled.
3. `06-WYDAJNOSC.md`, `06-BRAMA.md`, `06-AUDYT.md` and `06-UAT.md` all exist and are internally consistent.
4. `06-VALIDATION.md`'s sign-off checklist is walked row by row against the artefacts and the result recorded.
5. A11Y-03 and LEGAL-02 are confirmed still unticked; SITE-05 and A11Y-01 tick only if their acceptance rows pass.
</verification>

<success_criteria>
- The performance change is recorded with an honest label on every number.
- The gate is red, reconciled, and its remaining items are Phase 7's written work list.
- No project document states a panel route count the file disagrees with.
- Twelve acceptance rows exist, runnable by somebody who did not build the phase.
- A11Y-03 and LEGAL-02 are authored in full and deliberately unticked, with the reason recorded.
- Nothing belonging to Phase 7 was done early.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-legal-compliance-performance/06-14-SUMMARY.md` when done.
It is also the phase's closing summary, so it must carry: the final gate output with its reconciliation,
the performance before-and-proxy-after comparison, the list of properties proven and the list deferred
to `06-UAT.md`, and the explicit statement that A11Y-03 and LEGAL-02 remain unticked by design.
</output>
