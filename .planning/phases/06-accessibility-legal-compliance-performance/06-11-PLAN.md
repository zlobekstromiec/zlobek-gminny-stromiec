---
phase: 06-accessibility-legal-compliance-performance
plan: 11
type: execute
wave: 9
depends_on: ["06-09"]
files_modified:
  - src/lib/content/polityka.json
  - src/lib/content/polityka.ts
  - src/lib/polityka.ts
  - src/routes/polityka-prywatnosci/+page.svelte
  - tests/polityka-prywatnosci.spec.ts
  - tests/responsive.spec.ts
  - tests/zastepcze.unit.ts
  - tests/motyw-bazowy.spec.ts
autonomous: true
requirements: [LEGAL-02, A11Y-01]
must_haves:
  truths:
    - "A visitor can open /polityka-prywatnosci and read one complete RODO statement: what the forms collect and who receives it, what is stored in their own browser and why, who hosts the site, what cookies exist, where the public-information bulletin is, and when the policy last changed."
    - "The twelve klauzula blocks render from the SAME source the two forms use, so the two RODO texts can never drift apart."
    - "The klauzula remains inline in a collapsed disclosure under both forms; it is NOT moved to this page with the forms linking out."
    - "The session-storage preference introduced by the accessibility widget is disclosed honestly: what it holds, that it is not a cookie, that it contains nothing about the visitor, and that it disappears when the tab closes."
  artifacts:
    - "src/lib/content/polityka.json"
    - "src/lib/content/polityka.ts"
    - "src/lib/polityka.ts"
    - "src/routes/polityka-prywatnosci/+page.svelte"
  key_links:
    - "The page imports the klauzula blocks from the forms content module; the existing copy sweep already pins three of their sentences and keeps guarding them."
    - "The page adopts Seo.svelte, which deletes its hand-rolled title and robots tags."
  prohibitions:
    - "LEGAL-02 must NOT be ticked in this phase. The page is authored in full and stays unmarked until Phase 7 names the data-protection officer."
    - "The twelve klauzula blocks must NOT be copied into the new store. One source, imported."
    - "The klauzula must NOT be removed from under either form (04 D-03 stays intact)."
    - "The store must NOT be space-indented."
    - "The twelve blocks must render at the third heading level under a second-level heading that scopes them to forms; hoisting them would claim a scope they do not have and would break the page's heading order."
    - "No image ships on this page."
---

<objective>
Rebuild `/polityka-prywatnosci` from a 56-line stub into the full RODO policy.

Purpose: LEGAL-02 requires a privacy page, and this phase also introduces the first client-side
storage this project has ever had, which must be disclosed. D-15 makes the page REUSE the twelve
klauzula blocks that already sit under both forms rather than restating them, so the two RODO texts
are one text and cannot drift. Everything the forms do not cover, which is the browser storage, the
host, the cookies and the public-information bulletin link, is new and is panel-editable.

**After this plan a visitor can do something they could not do before**: read, in one place, what this
site does with their data.

Output: the store, the reader, the copy module and the rebuilt page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-accessibility-legal-compliance-performance/06-UI-SPEC.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-RESEARCH.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-PATTERNS.md
@src/lib/content/forms.ts
@src/routes/cennik/+page.svelte
@src/lib/content/cennik.json
@src/lib/markdown.ts
@tests/polityka-prywatnosci.spec.ts
@tests/forms-copy.unit.ts
</context>

<artifacts>
## Artifacts this plan produces

**New files**
- `src/lib/content/polityka.json` — the seed store: four prose blocks and one date, tab-indented with a trailing newline, with per-block placeholder flags where a block is not yet confirmed.
- `src/lib/content/polityka.ts` — the page's fixed Polish strings.
- `src/lib/polityka.ts` — the build-time reader.
- `src/routes/polityka-prywatnosci/+page.svelte` — replaced in full.

**New exported symbols**
- `src/lib/content/polityka.ts`: `META_POLITYKA`, `NAGLOWEK_POLITYKA`, `NAGLOWKI_POLITYKA` (the seven section headings in locked order), `WSTEP_KLAUZULI`, `LINK_BIP`.
- `src/lib/polityka.ts`: `POLITYKA`, `type PolitykaDane`.

**Modified**
- `tests/polityka-prywatnosci.spec.ts` — extended from its current shape to cover the rebuilt page.
- `tests/responsive.spec.ts` — the route list gains this route.
- `tests/zastepcze.unit.ts` — the new nested flags pinned by name.
- `tests/motyw-bazowy.spec.ts` — this route's snapshots re-baselined ONCE, authorised by UI-SPEC Contract 8.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: the seed store, the reader and the copy module</name>
  <files>src/lib/content/polityka.json, src/lib/content/polityka.ts, src/lib/polityka.ts</files>
  <read_first>
    - `06-UI-SPEC.md` § "8. `/polityka-prywatnosci`", the locked section order table, and § Copywriting Contract, the polityka table. Every Polish string there is authoritative and implemented verbatim.
    - `src/lib/content/forms.ts:291-381` — the twelve klauzula blocks, their type and the fact that no markup is permitted in their strings. Verify the count is twelve rather than trusting it.
    - `src/lib/content/cennik.json` — the exact byte shape.
    - `src/lib/cennik.ts` and `src/lib/deklaracja.ts` (plan 09) — the reader idiom, including the fail-the-build-on-a-missing-required-field decision.
    - `06-CONTEXT.md` D-11 (the session-storage trade and its stated cost) and D-15.
    - The plan-07 SUMMARY, for the actual storage key names and values the widget writes.
  </read_first>
  <action>
    (a) `src/lib/content/polityka.ts`: the fixed strings verbatim from the copy table. The seven section
    headings in their locked order, the page heading and lead, the one-sentence introduction that scopes
    the klauzula to the two forms, and the bulletin link with its visually hidden new-tab suffix.

    (b) `src/lib/content/polityka.json`: FOUR prose blocks and one date. The browser-settings block, the
    hosting block, the cookies block and the bulletin block, plus the last-updated date. Seed each block
    with the approved copy from the contract.

    The browser-settings block is the one that must be exactly right, because it describes something
    this very phase introduced. Seed it with the approved sentence: the accessibility control's choice
    is remembered in the browser's session memory, it is not a cookie, it contains nothing about the
    visitor, it disappears when the tab closes, and it is never sent to any server. Cross-check that
    sentence against what plan 07 actually implemented, using the key names and values recorded in that
    plan's SUMMARY. If the implementation and the sentence disagree, the SENTENCE is what is wrong only
    if the implementation matches D-11; otherwise stop and report, because a privacy policy that
    describes behaviour the code does not have is the one defect on this page that matters most.

    The cookies block states the honest position rather than the convenient one: the public site uses no
    cookie for tracking or statistics, and the ONLY cookie in the service is created when a staff member
    signs in to the editorial panel and exists solely to hold that session.

    Per-block placeholder flags where the block is not yet confirmed, following the same per-block
    granularity as the declaration store. TAB-INDENTED WITH A TRAILING NEWLINE.

    (c) `src/lib/polityka.ts`: the build-time reader, same guarded discipline and the same
    fail-the-build-on-a-missing-required-field decision as the declaration reader, for the same reason.
  </action>
  <verify>
    <automated>npm run check && npm run test:unit && node -e "const s=require('node:fs').readFileSync('src/lib/content/polityka.json','utf8');process.exit(s.startsWith('{\n\t')&&s.endsWith('\n')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/content/polityka.json` is tab-indented and ends with a newline; `npx prettier --check src/lib/content/polityka.json` passes.
    - The store holds four prose blocks and one date, and no copy of any klauzula block.
    - The browser-settings sentence was cross-checked against the plan-07 implementation and the result is recorded in the SUMMARY.
    - `npm run check` and `npm run test:unit` clean.
  </acceptance_criteria>
  <done>The site-wide half of the policy exists as editable leaf values, and the storage disclosure was checked against the code rather than against the plan.</done>
</task>

<task type="auto">
  <name>Task 2: the public page, the reused klauzula, and the acceptance suite</name>
  <files>src/routes/polityka-prywatnosci/+page.svelte, tests/polityka-prywatnosci.spec.ts, tests/responsive.spec.ts, tests/zastepcze.unit.ts, tests/motyw-bazowy.spec.ts</files>
  <read_first>
    - `src/routes/cennik/+page.svelte` — the composition analog, the Seo adoption and the header-comment shape.
    - `src/routes/polityka-prywatnosci/+page.svelte` as it stands: the container rules ARE the inherited idiom and are kept; the two off-scale type sizes are deleted with the stub.
    - `06-UI-SPEC.md` § "8", the section table and the heading-level ruling for the twelve blocks.
    - `tests/polityka-prywatnosci.spec.ts` as it stands — it already exists and already carries an axe pass, contrary to one upstream claim. Extend it rather than replacing it.
    - `tests/forms-copy.unit.ts` — the three klauzula sentences it already pins, which keep guarding themselves.
  </read_first>
  <action>
    (a) Replace the stub in full. Adopt `Seo.svelte` and DELETE the hand-rolled title and robots tags,
    the same deletion plan 09 made, which together leave exactly one noindex flip site for Phase 7.

    (b) Seven sections in the locked order. Section 2 holds a one-sentence introduction scoping the
    klauzula to the two forms, then the twelve blocks IMPORTED from the forms content module and
    rendered directly: their type carries an optional heading and a paragraph list with no markup
    permitted in the strings, so no renderer is involved at all. Do not copy them, do not paraphrase
    them, do not reorder them.

    The twelve blocks render at the THIRD heading level, under the second-level heading at section 2,
    and that is deliberate: they describe form data specifically, so hoisting them would state a scope
    they do not have and would break the heading order on a page this phase certifies as conformant.

    Sections 3 to 6 render the four store prose blocks through `renderPost`, the same hardened renderer
    plan 09 uses, with the same lint suppression carrying a reason. Section 7 renders the date.

    (c) 04 D-03 STAYS INTACT AND IS EASY TO BREAK HERE: the klauzula remains inline in a collapsed
    disclosure under BOTH forms. It is not moved to this page with the forms linking out. Add an
    assertion to the suite that proves it did not move, rather than trusting the diff.

    (d) The page's own block styles for what the renderer can emit, and the composition rules, exactly as
    plan 09 did for the declaration. Type roles on the scale tokens from plan 03. No image.

    (e) The header comment per D-07: prerendered, one island, the accessibility widget in the shared top
    bar. No zero-JavaScript claim.

    (f) The suite. Extend the existing spec: the seven headings in order (RETYPED, they are ours and their
    shape matters), the twelve klauzula headings rendered in order at the third level (INTERPOLATED from
    the forms module, because that module is the source of truth and a retyped copy here would be a
    second place to maintain them), the four store blocks (INTERPOLATED), the bulletin link's exact
    target and its new-tab suffix, one heading at the top level, no skipped heading levels, an axe pass
    in the default theme and in high contrast, and the not-moved assertion from (c).

    (g) Enumeration surfaces: the responsive route list gains this route; the inventory suite pins the new
    nested flags by name; the high-contrast sweep already covers the route through the shared public route
    list, so verify rather than duplicate.

    (h) Re-baseline `tests/motyw-bazowy.spec.ts` for this route ONLY. Fourth and last authorised
    re-baseline point, authorised by UI-SPEC Contract 8. Any other route moving is a regression.
  </action>
  <verify>
    <automated>npx playwright test tests/polityka-prywatnosci.spec.ts tests/kontakt.spec.ts tests/rekrutacja.spec.ts tests/kontrast.spec.ts tests/responsive.spec.ts tests/motyw-bazowy.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test` over the six named files is green.
    - `npm run test:unit` green, including the newly pinned flags and the untouched klauzula sentence assertions.
    - `npm run build` succeeds; the prerendered page carries no hand-rolled robots meta authored by this route.
    - `grep -rEn 'font-size:[[:space:]]*[0-9]' src/routes/polityka-prywatnosci/+page.svelte | wc -l` returns 0.
    - The suite asserts the twelve blocks render at the third level, and asserts the klauzula is STILL present under both forms.
    - Only this route's snapshots changed; the review is recorded in the SUMMARY.
    - Mutation check recorded and reverted: reorder two klauzula blocks in the forms module and confirm this suite goes red, proving the page really reads that source.
  </acceptance_criteria>
  <done>One RODO text serves the two forms and this page, and the new browser storage is disclosed on the page that has to disclose it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor-writable store to public page | four prose blocks travel into a JSON file, into a commit, through a build-time reader, onto a page that is a legal statement about data processing |
| implementation to disclosure | the page describes what the code does with a visitor's browser storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11-01 | Tampering | store prose rendered on a public page | **high** | mitigate | Same stored cross-site-scripting boundary as the declaration. Mitigation: the already-hardened renderer, reused not reimplemented, with a reasoned lint suppression at every site, plus `script-src 'self'` as the second layer. |
| T-06-11-02 | Repudiation | a disclosure that does not match the code | **high** | mitigate | A privacy policy describing behaviour the site does not have is a false statement by a public body, and this page is the first thing a regulator reads. Mitigation: the browser-storage sentence is cross-checked against the plan-07 implementation using that plan's recorded key names, and the check result is recorded. |
| T-06-11-03 | Tampering | drift between the two RODO texts | high | mitigate | Copying the twelve blocks into this page would create a second copy that silently diverges the first time either is edited. Mitigation: one source, imported; a mutation check proves the page really reads it; the existing copy sweep keeps pinning three of its sentences. |
| T-06-11-04 | Repudiation | the klauzula silently leaving the forms | medium | mitigate | Moving the klauzula to this page and linking out would break a prior decision and would remove the notice from the moment of collection. Mitigation: an explicit not-moved assertion in the suite rather than trust in the diff. |
| T-06-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists in this plan; no new renderer and no new dependency. Carried per the phase security contribution. |
</threat_model>

<verification>
1. `npm run check && npm run lint && npm run test:unit && npm run test` all green.
2. `npm run build` and inspect the prerendered page for the seven headings and the twelve third-level blocks.
3. `npm run gate:launch` diffed against `06-BRAMA.md`: new content flags appear, one marker entry disappears with the stub.
4. The klauzula reorder mutation check, recorded then reverted.
5. Manual reading by a Polish-speaking non-developer is deferred to the phase UAT alongside the declaration.
</verification>

<success_criteria>
- The stub is gone; the full RODO policy renders in this site's own markup on the locked type scale.
- The twelve klauzula blocks come from one source and render at the third level under a scoping heading.
- The new browser storage is disclosed accurately, cross-checked against the implementation.
- The klauzula is still inline under both forms.
- LEGAL-02 remains UNTICKED, as designed.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-legal-compliance-performance/06-11-SUMMARY.md` when done.
Record: the storage-disclosure cross-check result, the mutation observation, the gate diff and the
snapshot review.
</output>
