---
phase: 06-accessibility-legal-compliance-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md
  - tests/motyw-bazowy.spec.ts
  - src/lib/zastepcze.ts
  - tests/zastepcze.unit.ts
  - package.json
  - docs/dev-env.md
  - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
autonomous: true
requirements: [SITE-05, A11Y-01]
must_haves:
  truths:
    - "The SITE-05 before-numbers for the three D-25 routes exist as a committed record, measured against the live deployment before any Phase 6 code landed."
    - "A default-theme visual baseline for all nine public routes exists as a Playwright snapshot suite that goes red on an unintended repaint."
    - "npm run build fails when npm run test:unit fails, so every unit suite this phase writes is gated by the Cloudflare Pages build."
    - "One placeholder walker serves two consumers: the inventory suite and (from plan 02) the launch gate."
  artifacts:
    - ".planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md"
    - "tests/motyw-bazowy.spec.ts + its committed snapshot directory"
    - "src/lib/zastepcze.ts"
    - "package.json build script containing npm run test:unit"
    - "01-UI-SPEC.md Amendment v1.8 section"
  key_links:
    - "tests/zastepcze.unit.ts imports plikiTresci/znaczniki from src/lib/zastepcze.ts instead of declaring them."
    - "playwright.config.ts webServer runs npm run build, so the unit tier now also gates every browser run."
  prohibitions:
    - "src/lib/zastepcze.ts must NOT be imported from any .svelte file or any route under src/routes/admin (it carries node:fs and the panel is the Cloudflare Worker)."
    - "No .dev.vars file is created at the repository root."
    - "npm run test is NOT added to the Pages build command (D-21 names only test:unit)."
---

<objective>
Wave 0. Capture the two pieces of evidence that cease to exist the moment Phase 6 code lands,
then wire the two mechanisms every later plan depends on.

Purpose: two of this phase's claims (SITE-05 improved-or-bounded, and "the default theme cannot
regress") can only be evidenced against a state that this phase destroys. Both baselines are
irrecoverable. Separately, D-21 puts `npm run test:unit` into the Cloudflare Pages build command,
so the eight new unit suites this phase writes land in a tier that actually runs, and the
placeholder walker is extracted so plan 02's gate and the existing inventory suite cannot drift.

Output: `06-WYDAJNOSC.md` with the before-numbers, `tests/motyw-bazowy.spec.ts` with committed
snapshots, `src/lib/zastepcze.ts`, the amended `build` script, and Amendment v1.8 appended to the
locked design contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-CONTEXT.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-RESEARCH.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-VALIDATION.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-UI-SPEC.md
@.planning/phases/06-accessibility-legal-compliance-performance/06-PATTERNS.md
@tests/zastepcze.unit.ts
@package.json
@playwright.config.ts
@docs/dev-env.md
</context>

<artifacts>
## Artifacts this plan produces

**New files**
- `.planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md` — the SITE-05 measurement record, before and (in plan 14) after.
- `tests/motyw-bazowy.spec.ts` — default-theme visual baseline over nine public routes at two viewports.
- `tests/motyw-bazowy.spec.ts-snapshots/` — 18 committed PNG baselines.
- `src/lib/zastepcze.ts` — the shared placeholder walker.

**New exported symbols**
- `src/lib/zastepcze.ts`: `KLUCZ_ZASTEPCZY`, `interface Znacznik`, `plikiTresci(katalog?, prefiks?)`, `znaczniki(wartosc, gdzie, zebrane?)`, `KATALOG_TRESCI`.
- `tests/motyw-bazowy.spec.ts`: `TRASY_PUBLICZNE` (exported so later plans extend one list, not three).

**Modified**
- `package.json` — `build` becomes `wrangler types --check && npm run test:unit && vite build` (D-21).
- `tests/zastepcze.unit.ts` — declares nothing, imports the walker.
- `docs/dev-env.md` — records that the Pages build now runs the unit tier.
- `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md` — Amendment v1.8 appended (D-13).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: SITE-05 before-measurement against the live deployment (D-25)</name>
  <files>.planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md</files>
  <read_first>
    - `.planning/phases/06-accessibility-legal-compliance-performance/06-RESEARCH.md` § "4. Lighthouse methodology that is reproducible" (the five-run median, the warm-up run, the INP caveat, the edge-static variance correction).
    - `.planning/phases/06-accessibility-legal-compliance-performance/06-CONTEXT.md` D-25.
    - `docs/dev-env.md` for the live URL.
  </read_first>
  <action>
    THIS TASK RUNS FIRST AND COMMITS NOTHING UNDER src/. It is a measurement, and its window closes
    the moment any Phase 6 change reaches the live deployment.

    Per D-25, drive the `web-perf` skill over the chrome-devtools MCP server. The server IS
    available in this environment under a plugin-namespaced tool prefix
    (`mcp__plugin_chrome-devtools-mcp_chrome-devtools__*`); the research body's claim that it is
    absent is overturned by the Orchestrator Addendum, so no MCP configuration change and no
    human-verify checkpoint is owed. Enumerate the available tool names at run time rather than
    hard-coding one: the throttling tool is `emulate` in 1.7.0 but was `emulate_cpu` plus
    `emulate_network` in older releases (Assumption A5).

    Target the LIVE deployment, never localhost and never desktop broadband. Three routes per D-25:
    the homepage, `/o-nas` (gallery populated), and one `/aktualnosci/<slug>` post that has a cover
    image. Per route: resize to a mobile viewport, apply CPU throttling 4x and network throttling
    Slow 4G, run ONE warm-up load and DISCARD it (Cloudflare Pages serves the first request for an
    asset in a colo from origin), then five traced cold loads with `reload: true`. Record LCP, CLS,
    FCP, TBT and TTFB for each run and take the MEDIAN on LCP.

    Run `performance_analyze_insight` on `LCPBreakdown` and `CLSCulprits` for the median run of each
    route and record what the LCP element actually is.

    INP does not appear in a passive cold-load trace. State in the record which of the three D-25
    targets was measured directly and which was approximated by TBT. Do not claim a measured INP.

    Optionally cross-check with `npx lighthouse <url> --only-categories=performance --output=json`,
    five runs, median. Its fixed mobile profile is lower variance; if the two instruments disagree by
    more than the expected spread (roughly plus or minus 10 to 25 percent on LCP with applied
    throttling), the disagreement is itself a finding and goes in the record.

    Write `06-WYDAJNOSC.md` with: the instrument and its version, the exact throttling settings, the
    live URL and the deployment id measured, a per-route table of five runs plus the median, the
    named LCP element, and an explicit "PRZED" heading. Leave a "PO" heading empty for plan 14.
    Add one sentence stating plainly that this phase ADDS work (a fifth island, an EU strip on every
    route, two rebuilt pages, 167 rewritten declarations) and that the after-numbers are permitted to
    be slightly worse, because D-25 forbids a performance budget as a test.

    Polish prose in the document body, per the project copy rules: no emoji, no em dashes, en dash
    only inside numeric ranges.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md && grep -c 'LCP' .planning/phases/06-accessibility-legal-compliance-performance/06-WYDAJNOSC.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-WYDAJNOSC.md` exists and contains a `PRZED` section with three route tables.
    - Each route table carries five numbered runs plus a median row; the warm-up run is named and
      marked discarded.
    - The record names the instrument, its version and the exact throttling settings.
    - The record states explicitly which metric was approximated rather than measured.
    - No file under `src/`, `static/`, `tests/` or `package.json` was modified by this task.
  </acceptance_criteria>
  <done>The before-numbers exist as a committed record and the measurement window is closed honestly.</done>
</task>

<task type="auto">
  <name>Task 2: default-theme visual baseline for all nine public routes</name>
  <files>tests/motyw-bazowy.spec.ts</files>
  <read_first>
    - `tests/responsive.spec.ts` lines 1-60 (the `VIEWPORTS` map and the `ROUTES` idiom this file mirrors).
    - `tests/galeria.spec.ts` lines 1-59 (the header-comment shape every spec in this repository carries).
    - `06-RESEARCH.md` § "Not Inferable From Unit Tests" item 7, and § Pitfall 4.
  </read_first>
  <action>
    Create `tests/motyw-bazowy.spec.ts`, a full-page screenshot suite over the nine public routes at
    two viewports, and commit the generated snapshots.

    Export the route list as `TRASY_PUBLICZNE` from this file so plans 06 to 12 extend ONE list
    rather than three: `/`, `/o-nas`, `/aktualnosci`, `/dokumenty`, `/rekrutacja`, `/cennik`,
    `/kontakt`, `/deklaracja-dostepnosci`, `/polityka-prywatnosci`. Add one news post route resolved
    from the committed content rather than retyped, following the `slugi()` helper idiom in
    `tests/fixtures/trasy-panelu.ts`.

    Two viewports: 375px (the project's smallest tested tier) and 1280px. Use
    `expect(page).toHaveScreenshot(..., { fullPage: true, maxDiffPixelRatio: 0.002, animations: 'disabled' })`.
    Disable animations and wait for fonts (`document.fonts.ready`) before each shot, or the suite is
    flaky for reasons that have nothing to do with the theme.

    Generate the baselines with `npx playwright test tests/motyw-bazowy.spec.ts --update-snapshots`
    and COMMIT the resulting snapshot directory.

    The header comment must state, in Polish, exactly what this suite is accountable to and when it
    is allowed to be re-baselined, because a snapshot suite nobody may re-baseline becomes a suite
    somebody deletes. Its job is to catch an UNINTENDED default-theme repaint while plans 03 to 06
    edit seventeen component stylesheets. It is authorised to be re-baselined exactly four times, and
    only in the plan that lands the authorising contract: plan 07 (the widget wrapper joins the
    TopBar on every route, UI-SPEC Contract 1), plan 08 (the EU-funding strip joins the Footer on
    every route, Contract 9), plan 09 and plan 11 (both legal stubs are replaced in full, Contracts 7
    and 8). Any other red is a regression, not a re-baseline.

    Do NOT add this file to `tests/responsive.spec.ts` or fold it into another suite. It is its own
    gate with its own re-baseline rules.
  </action>
  <verify>
    <automated>npx playwright test tests/motyw-bazowy.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/motyw-bazowy.spec.ts` passes with the committed snapshots.
    - The snapshot directory contains at least 18 PNG files and is committed (not gitignored).
    - `TRASY_PUBLICZNE` is exported and holds the nine public routes.
    - The header comment names the four authorised re-baseline points by plan number and contract.
    - Re-running the suite twice in a row with no source change is green both times (no flake from
      fonts or animation).
  </acceptance_criteria>
  <done>An unintended repaint during the Layer 2 sweep now turns a test red instead of shipping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-21 build chain, the shared placeholder walker, and Amendment v1.8</name>
  <files>package.json, src/lib/zastepcze.ts, tests/zastepcze.unit.ts, docs/dev-env.md, .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md</files>
  <read_first>
    - `tests/zastepcze.unit.ts` in full (the walker being extracted, its non-vacuity guard, its Polish ASCII-folded test names).
    - `src/lib/cennik.ts` lines 1-20 (the header-comment reasoning style to copy for a module with an unusual constraint).
    - `06-PATTERNS.md` § "8. `src/lib/zastepcze.ts` (utility) — an EXTRACTION, not a new file" (both named hazards).
    - `06-UI-SPEC.md` § "Amendment v1.8 block (append verbatim to `01-UI-SPEC.md`)" — the block is written out there and is appended byte for byte.
    - The tail of `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md` (the v1.7 append precedent).
  </read_first>
  <behavior>
    - `plikiTresci()` with no argument returns every `.json` path under `src/lib/content`, recursively, sorted, relative to that directory.
    - `plikiTresci(katalog)` with an explicit directory walks that directory instead, so a fixture tree can be passed in (plan 02 needs this).
    - `znaczniki(obiekt, 'plik.json')` collects every key named `placeholder` at any nesting depth, including inside arrays, and reports each as a dotted path.
    - `tests/zastepcze.unit.ts` still asserts every collected marker is a boolean, still refuses to pass on an empty inventory, and still pins the two nested `w-skrocie.json` flags by name.
    - `npm run build` fails when `npm run test:unit` fails.
  </behavior>
  <action>
    Three changes, one commit each, in this order.

    (a) D-21. Change the `build` script in `package.json` to
    `wrangler types --check && npm run test:unit && vite build`. Do NOT add `npm run test`
    (Playwright stays a local gate, D-21 says so). Do NOT add a third pre-commit hook: pre-commit
    already runs svelte-check plus prettier plus eslint over the whole tree slowly enough that the
    GSD commit wrapper times out on it, which is why D-21 chose the build command instead. Note that
    `playwright.config.ts` runs `npm run build` as its `webServer`, so the unit tier now also gates
    every browser run, and record that consequence in `docs/dev-env.md` beside the four-command
    verify gate at :64. This pays the AG-3 debt raised three times (03-REVIEW.md:99-105, 04-REVIEW
    WR-01, T-05-09-05).

    (b) Extract the walker. Move `plikiTresci()` and `znaczniki()` verbatim out of
    `tests/zastepcze.unit.ts` into a new `src/lib/zastepcze.ts`, together with the marker-key
    constant and the `Znacznik` interface, and have the suite import them with an explicit `.ts`
    extension (`import { ... } from '../src/lib/zastepcze.ts';`) so bare `node --test` type stripping
    still resolves it on the pinned Node 22.23.2. Two hazards named by the pattern map, both
    load-bearing: the content directory constant is currently resolved relative to `tests/`, so from
    `src/lib/` it becomes `new URL('./content', import.meta.url)` AND it must be a PARAMETER with
    that default rather than a hard-coded module constant, because plan 02's gate script runs from a
    third location and plan 02's unit suite passes fixture trees. Second, this module carries
    `node:fs`, which no other `$lib` module outside the server-only subtree does. Its header comment
    must say, in Polish, that it is build and tooling tier only and must never be imported from a
    `.svelte` file or from any route under `src/routes/admin`, because the panel is the Cloudflare
    Worker and has no filesystem (the 04.1-08 lesson). Leave the three `test()` blocks and the
    non-vacuity guard behind in the suite unchanged. Do not take the opportunity to add an assertion
    that a flag is `false`: the existing suite deliberately never does that, and it is plan 02's job.

    A red-first cycle is impossible in this repository: pre-commit runs svelte-check over the whole
    working tree, so a suite importing a module that does not exist yet is a type error that blocks
    even an unrelated commit. Observe the rewired suite failing before the module exists, RECORD that
    observation in the SUMMARY, then land the module and the rewired suite in one commit. Never reach
    for `--no-verify`.

    (c) D-13. Append Amendment v1.8 to `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md`,
    copying the block from `06-UI-SPEC.md` § "Amendment v1.8 block" byte for byte, at the end of the
    file, following the v1.7 append precedent (05 D-16, D-20). This plan lands the first Phase 6 code
    change, so this is the plan that owes the amendment. Do not restate the contracts; the block is
    deliberately a pointer.
  </action>
  <verify>
    <automated>npm run test:unit && node -e "const p=require('./package.json');process.exit(p.scripts.build.includes('npm run test:unit')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` is green and `tests/zastepcze.unit.ts` declares neither walker function.
    - `node -e "..."` above exits 0, proving the build script gates the unit tier.
    - `grep -rn "zastepcze" src/routes/admin src/lib/components | wc -l` returns 0.
    - `npm run check` and `npm run lint` are both clean.
    - `01-UI-SPEC.md` ends with a section headed `## Amendment v1.8 (2026-08-18): Dostępność, zgodność prawna, wydajność`.
    - `docs/dev-env.md` states that the Pages build now runs `npm run test:unit`.
  </acceptance_criteria>
  <done>Every unit suite this phase writes is gated by the build, one walker serves two consumers, and the locked contract carries its amendment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree to build tooling | `src/lib/zastepcze.ts` reads the repository off disk with `node:fs` |
| build tooling to Cloudflare Pages | the `build` script now decides whether a deployment is created at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | `src/lib/zastepcze.ts` reaching the client or Worker bundle | high | mitigate | `node:fs` in a `$lib` module breaks the Worker bundle if imported by a panel route or a component. Header comment states the prohibition and the acceptance criterion greps for it. |
| T-06-01-02 | Denial of Service | `build` script | medium | accept | A red unit suite now blocks every deployment. That is the intent: `svelte.config.js:16-18` already documents "a failed build leaves the previous deployment live" as this project's preferred failure mode. |
| T-06-01-03 | Repudiation | the SITE-05 record | medium | mitigate | A before-number taken after code landed would be a false comparison that nobody could detect later. Task 1 runs first, commits nothing under `src/`, and names the deployment id it measured. |
| T-06-01-04 | Tampering | committed snapshot baselines | low | mitigate | A baseline re-generated to make a red suite green would erase the regression evidence. The header comment names the only four authorised re-baseline points, by plan and by contract. |
| T-06-01-SC | Tampering | npm installs | high | mitigate | **This phase installs ZERO packages.** `06-RESEARCH.md` § Package Legitimacy Audit records one `[SUS]` entry, `chrome-devtools-mcp`, flagged only for `too-new` on its latest release date, with 2.3M weekly downloads and an official `ChromeDevTools` org repository. Per the Orchestrator Addendum it is already present in this environment and nothing is being installed or configured, so the human-verify checkpoint the researcher recommended is not owed. No package-manager install task exists in this plan. |
</threat_model>

<verification>
1. `npm run check && npm run lint && npm run test:unit` green.
2. `npx playwright test tests/motyw-bazowy.spec.ts` green twice in a row.
3. `06-WYDAJNOSC.md` carries three route tables with five runs and a median each.
4. `npm run build` observed failing when a unit assertion is deliberately broken, then green again
   (mutation check, recorded in the SUMMARY, change reverted).
5. `grep -rn "zastepcze" src/routes/admin src/lib/components` returns nothing.
</verification>

<success_criteria>
- The two irrecoverable baselines exist and were captured before any `src/` change in this phase.
- `npm run test:unit` runs inside the Cloudflare Pages build command and inside every Playwright run.
- One placeholder walker exists, in `src/lib/`, imported by the inventory suite.
- `01-UI-SPEC.md` carries Amendment v1.8.
- No new npm dependency. No root `.dev.vars`.
</success_criteria>

<output>
Create `.planning/phases/06-accessibility-legal-compliance-performance/06-01-SUMMARY.md` when done.
Record in it: the measured before-numbers, the observed red of the rewired inventory suite before the
module existed, and the observed red of `npm run build` under a deliberately broken unit assertion.
</output>
