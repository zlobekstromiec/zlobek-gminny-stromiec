---
phase: 01-live-homepage-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - svelte.config.js
  - vite.config.ts
  - eslint.config.js
  - src/app.d.ts
  - src/app.html
  - src/app.css
  - src/routes/+layout.ts
  - src/routes/+layout.svelte
  - src/routes/+page.svelte
  - static/fonts/
  - .tool-versions
  - .pre-commit-config.yaml
  - docs/dev-env.md
  - playwright.config.ts
  - tests/home.spec.ts
autonomous: true
requirements: [SITE-01, SITE-02, SITE-04, SITE-06]
user_setup: []

must_haves:
  truths:
    - "npm run build exits 0 and produces the .svelte-kit/cloudflare output directory (SITE-01 build half)"
    - "The homepage document is served with <html lang=\"pl\"> (SITE-06, D-10)"
    - "The two-tier accessible palette tokens exist in app.css exactly as locked in 01-UI-SPEC.md (SITE-04, highest project risk)"
    - "Fonts are self-hosted (no Google Fonts CDN reference) — RODO"
    - "A visible focus ring is defined globally and prefers-reduced-motion is honored at the base layer (A11Y baseline)"
    - "tests/home.spec.ts encodes the HOME-01/HOME-02/axe-AA acceptance criteria and runs (RED until Plan 03 fills the homepage)"
    - "npm run check exits 0 (svelte-check: types + compiler a11y)"
  artifacts:
    - path: "svelte.config.js"
      provides: "SvelteKit configured with @sveltejs/adapter-cloudflare"
      contains: "adapter-cloudflare"
    - path: "vite.config.ts"
      provides: "Tailwind v4 Vite plugin + sveltekit plugin"
      contains: "@tailwindcss/vite"
    - path: "src/app.css"
      provides: "Tailwind v4 @theme two-tier design token seed + self-hosted @font-face + global a11y base"
      contains: "@theme"
    - path: "src/app.html"
      provides: "HTML shell with Polish lang attribute"
      contains: "lang=\"pl\""
    - path: "src/app.d.ts"
      provides: "App.Platform.env typing stub (empty in Phase 1)"
      contains: "interface Platform"
    - path: "src/routes/+layout.ts"
      provides: "prerender flag inherited by all routes"
      contains: "prerender = true"
    - path: "playwright.config.ts"
      provides: "E2E config with webServer building + previewing the app"
    - path: "tests/home.spec.ts"
      provides: "Failing homepage e2e/a11y acceptance test (RED)"
    - path: ".tool-versions"
      provides: "asdf Node pin (even LTS 22.x)"
      contains: "nodejs"
    - path: ".pre-commit-config.yaml"
      provides: "Local pre-commit hook running npm run check + npm run lint"
  key_links:
    - from: "vite.config.ts"
      to: "src/app.css"
      via: "Tailwind v4 Vite plugin compiles @theme tokens to CSS variables + utilities"
      pattern: "tailwindcss\\(\\)"
    - from: "svelte.config.js"
      to: ".svelte-kit/cloudflare"
      via: "adapter-cloudflare writes the Pages build output"
      pattern: "adapter-cloudflare"
---

<objective>
Establish the Walking Skeleton spine: a buildable, deployable SvelteKit 2 + Svelte 5 app with Tailwind v4, the Cloudflare adapter, self-hosted fonts, the locked two-tier accessible design-token system (SITE-04 — the single highest project risk), the a11y/test quality gate (D-09), the dev-env conventions (D-08), and a minimal homepage that renders `<html lang="pl">` and builds to `.svelte-kit/cloudflare`.

This plan is greenfield (empty repo, only `.gitignore`). It creates the foundation every later plan and phase inherits.

Purpose: Make the stack real and prove `npm run build` works before any component building, and lock the AA-verified palette tokens verbatim so no downstream drift re-introduces the contrast risk the UI-SPEC already solved.
Output: Scaffolded SvelteKit app + `app.css` design foundation + Playwright/axe + svelte-check gate + dev-env files + the failing homepage acceptance test (RED).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md
@.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
@.planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md
@.planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md
</context>

<scope_note>
This plan touches ~16 files but is a one-time greenfield walking-skeleton spine. Most files are **scaffold-generated boilerplate**, not hand-authored logic — the hand-authored surface is small:
- **Scaffold-generated (Task 2, by `npx sv create .` + `npx sv add tailwindcss`, then lightly edited):** `package.json`, `package-lock.json`, `svelte.config.js`, `vite.config.ts`, `eslint.config.js`, `src/app.d.ts`, `src/app.html`, `src/routes/+layout.svelte`, `src/routes/+page.svelte`, `src/routes/+layout.ts` (edits: adapter-cloudflare in `svelte.config.js`, `lang="pl"` in `app.html`, `prerender = true` in `+layout.ts`, empty `App.Platform.env` stub in `app.d.ts`).
- **Hand-authored (Tasks 1 & 3):** `playwright.config.ts`, `tests/home.spec.ts`, `src/app.css` (UI-SPEC `@theme` token seed + a11y base), `static/fonts/*`, `.tool-versions`, `.pre-commit-config.yaml`, `docs/dev-env.md`.
</scope_note>

<artifacts_this_phase_produces>
Symbols/files this plan creates (exclude from drift/source-grounding checks):
- Files: `package.json`, `package-lock.json`, `svelte.config.js`, `vite.config.ts`, `eslint.config.js`, `src/app.d.ts`, `src/app.html`, `src/app.css`, `src/routes/+layout.ts`, `src/routes/+layout.svelte`, `src/routes/+page.svelte`, `static/fonts/*`, `.tool-versions`, `.pre-commit-config.yaml`, `docs/dev-env.md`, `playwright.config.ts`, `tests/home.spec.ts`
- Design tokens (CSS custom properties via `@theme`): `--color-surface`, `--color-surface-warm`, `--color-band`, `--color-ink`, `--color-muted`, `--color-brand-blue`, `--color-accent`, `--color-accent-hover`, `--color-accent-active`, `--color-danger`, `--color-danger-surface`, `--color-focus-ring`, `--color-border-strong`, `--color-border-subtle`, `--color-expr-blue`, `--color-expr-yellow`, `--color-expr-orange`, `--font-display`, `--font-body`, `--radius-sm`, `--radius-md`, `--radius-lg`, `--radius-pill`
- TypeScript namespace: `App.Platform` (env stub)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1: Failing homepage e2e/a11y acceptance test + Playwright config</name>
  <files>playwright.config.ts, tests/home.spec.ts</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Code Examples "Homepage smoke test with axe"; §Validation Architecture — test map + Wave 0 gaps)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (tests/home.spec.ts pattern assignment — copy verbatim, extend)
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Copywriting Contract — authoritative Polish strings; §Hero — verbatim message)
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-02 — verbatim core message is final client copy)
    - .planning/PROJECT.md (line 47 — the exact verbatim core message text)
  </read_first>
  <behavior>
    - Homepage responds at "/" and the served document has html lang="pl"
    - Hero contains the verbatim core-message substring "będziemy czuwać nad każdym krokiem" (final copy per D-02)
    - Primary CTA labelled "Zapisz dziecko" exists and links to /rekrutacja (HOME-02)
    - Aktualności empty-state heading "Wkrótce pojawią się aktualności" is present (HOME-02)
    - Quick-contact exposes a mailto link to zlobek@ugstromiec.pl (HOME-02)
    - axe scan with tags wcag2a, wcag2aa, wcag21a, wcag21aa returns zero violations (SITE-04 / A11Y baseline)
  </behavior>
  <action>
    Author `playwright.config.ts` with a `webServer` that builds then previews the app (command `npm run build && npm run preview`, url `http://localhost:4173`, `reuseExistingServer: !process.env.CI`) and `use.baseURL` pointing at it. Author `tests/home.spec.ts` using `@playwright/test` + `@axe-core/playwright` `AxeBuilder`, following the RESEARCH §Code Examples smoke-test pattern and extending it with the six assertions in the behavior block above. Reference the verbatim substring and Polish strings exactly from 01-UI-SPEC.md §Copywriting Contract and PROJECT.md line 47. This test is RED now (no app yet) and turns GREEN in Plan 03 when the homepage is composed. Do NOT stub or weaken the assertions to make it pass early.
  </action>
  <acceptance_criteria>
    - `playwright.config.ts` exists with a `webServer` entry and a `baseURL`
    - `tests/home.spec.ts` imports `AxeBuilder` from `@axe-core/playwright` and asserts `results.violations` is empty for the four WCAG tag families
    - `tests/home.spec.ts` asserts the verbatim substring `będziemy czuwać nad każdym krokiem`, the CTA `Zapisz dziecko` → `/rekrutacja`, the empty-state heading `Wkrótce pojawią się aktualności`, and a `mailto:zlobek@ugstromiec.pl` link
    - Running `npx playwright test tests/home.spec.ts` executes and reports failing content assertions (RED expected) once dependencies from Task 2 are installed
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/home.spec.ts || echo "RED as expected (homepage composed in Plan 03)"</automated>
  </verify>
  <done>Playwright config + homepage acceptance test authored, encoding HOME-01/HOME-02/axe-AA criteria; RED until Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold SvelteKit + Tailwind v4 + Cloudflare adapter + minimal buildable homepage</name>
  <files>package.json, package-lock.json, svelte.config.js, vite.config.ts, eslint.config.js, src/app.d.ts, src/app.html, src/routes/+layout.ts, src/routes/+layout.svelte, src/routes/+page.svelte</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Standard Stack + Installation block; §Code Examples "Cloudflare adapter config", "Tailwind v4 Vite plugin", "Platform env typing stub"; §Recommended Project Structure; Open Questions 1 & 3)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (svelte.config.js / vite.config.ts / src/app.d.ts / +layout.ts pattern assignments — copy verbatim)
    - .planning/research/STACK.md (versions + scaffold command)
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-04 adapter, D-08 dev-env conventions; Claude's Discretion — SvelteKit file layout)
    - .gitignore (preserve; do not overwrite)
  </read_first>
  <action>
    Scaffold in place with `npx sv create .` choosing the SvelteKit minimal template, TypeScript, ESLint, Prettier, and Playwright (Vitest optional) — preserve the existing `.git` and `.envrc`; delete any stray empty `main.html` if present. Add Tailwind v4 with `npx sv add tailwindcss` (wires `@tailwindcss/vite` + the `app.css` import automatically). Install `@sveltejs/adapter-cloudflare` (dev), `@lucide/svelte` (runtime), and `@axe-core/playwright` + `eslint-plugin-svelte` (dev). Use `@lucide/svelte` — NOT the deprecated `lucide-svelte` the UI-SPEC names (RESEARCH stack correction). Set `svelte.config.js` to use `adapter()` from `@sveltejs/adapter-cloudflare` (per RESEARCH code example). Confirm `vite.config.ts` has `tailwindcss()` before `sveltekit()`. Create `src/app.d.ts` with the empty `App.Platform.env` stub (secrets land Phase 4 — leave a comment noting RESEND_API_KEY / TURNSTILE_SECRET_KEY arrive then). Set `<html lang="pl">` in `src/app.html`. Create `src/routes/+layout.ts` with `export const prerender = true`. Create a minimal `src/routes/+layout.svelte` (renders `<main id="main"><slot /></main>` for now — the full semantic shell lands in Plan 02) and a minimal `src/routes/+page.svelte` placeholder (a single heading marked with a `PLACEHOLDER` token; the real homepage is Plan 03) so the build succeeds. Do NOT create `tailwind.config.js` (Tailwind v4 is CSS-first). Do NOT hand-author a `/functions` directory (adapter-cloudflare makes server routes the Pages Functions). Ensure `package.json` scripts include `dev`, `build`, `preview`, `check`, `lint`, `test`. Commit the lockfile.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and `.svelte-kit/cloudflare` exists after build
    - `package.json` dependencies include `@sveltejs/adapter-cloudflare`, `@lucide/svelte`, `@axe-core/playwright`, `eslint-plugin-svelte`, `tailwindcss`, `@tailwindcss/vite`; and NOT `lucide-svelte`
    - `svelte.config.js` imports `adapter` from `@sveltejs/adapter-cloudflare`
    - `src/app.html` contains `lang="pl"`
    - `src/routes/+layout.ts` contains `export const prerender = true`
    - No `tailwind.config.js` file exists; no `functions/` directory exists
    - `src/app.d.ts` declares `namespace App { interface Platform { env: {...} } }`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -d .svelte-kit/cloudflare && test ! -f tailwind.config.js && grep -q 'lang="pl"' src/app.html && grep -q 'adapter-cloudflare' svelte.config.js</automated>
  </verify>
  <done>App scaffolded and builds to `.svelte-kit/cloudflare`; Cloudflare adapter + Tailwind v4 + `@lucide/svelte` wired; Polish `lang`; prerender enabled; no `tailwind.config.js` / no `/functions`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Design-token foundation (app.css @theme + self-hosted fonts + a11y base) + dev-env quality gates</name>
  <files>src/app.css, static/fonts/, .tool-versions, .pre-commit-config.yaml, docs/dev-env.md</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Appendix `@theme` token seed — paste verbatim; §Color two-tier hard rules; §Font diacritic coverage; §Interaction — focus ring; §Motion — reduced motion)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (src/app.css pattern assignment; Shared Patterns — Design tokens, Accessibility, Motion)
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Pattern 2 tokens; §Pattern 4 self-hosted fonts; §Environment Availability — pre-commit missing; Open Q1 Node pin)
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-08 dev-env conventions, D-09 a11y gate)
    - .planning/research/STACK.md
  </read_first>
  <behavior>
    - `--color-brand-blue` resolves to #0369A1 and `--color-accent` to #F59E0B (AA-verified pairings from UI-SPEC)
    - Every text/icon/border/focus/button-label color draws from the accessible tier; `expr-*` tokens are documented decorative-only
    - A visible focus ring (`outline: 3px solid var(--color-focus-ring); outline-offset: 2px`) applies to interactive elements; `outline: none` is never used without a visible replacement
    - Under `prefers-reduced-motion: reduce`, transitions/transforms/smooth-scroll are neutralized at the base layer
  </behavior>
  <action>
    Paste the 01-UI-SPEC.md Appendix `@theme{}` token seed into `src/app.css` verbatim (surfaces, accessible tier, expressive tier, fonts, radius) after `@import "tailwindcss";`. Self-host Baloo 2 (weights 400 + 700) and Nunito (weights 400 + 700) subset to `latin,latin-ext` with `font-display: swap` — install and import `@fontsource/baloo-2` + `@fontsource/nunito` (self-hosted, bundled from our own origin) OR place subset WOFF2 files in `static/fonts/` with hand-authored `@font-face`; either way the fonts MUST be served from our own origin, never the Google Fonts CDN (RODO). Add a global base layer: a `:focus-visible` rule using `outline: 3px solid var(--color-focus-ring); outline-offset: 2px`, and a `@media (prefers-reduced-motion: reduce)` block that disables transforms, animation, and smooth scrolling. Add a short CSS comment recording the two-tier hard rule (expressive tokens are decorative surfaces only; CTA label is ink on amber except the active state; body links are brand-blue and underlined) so downstream executors do not drift. Pin an even Node LTS in `.tool-versions` (nodejs 22.x — do NOT build on local Node 25). Install `pre-commit` if missing (`brew install pre-commit`, fallback `pipx install pre-commit`), author `.pre-commit-config.yaml` with a `repo: local` hook that runs `npm run check` and a second that runs `npm run lint` on commit, then run `pre-commit install`. Author `docs/dev-env.md` as the machine-contract onboarding doc (asdf `.tool-versions`, direnv `.envrc`, pre-commit, the Cloudflare Pages build settings: build command `npm run build`, output dir `.svelte-kit/cloudflare`, NODE_VERSION 22, and `npm run check && npm run lint && npm run test` as the verify-before-commit gate).
  </action>
  <acceptance_criteria>
    - `src/app.css` contains `@theme` and the tokens `--color-brand-blue: #0369A1`, `--color-accent: #F59E0B`, `--color-ink: #1E293B`, plus the expressive tier `--color-expr-yellow`
    - `src/app.css` defines a `:focus-visible` outline using `--color-focus-ring` and a `prefers-reduced-motion: reduce` block
    - Fonts are self-hosted: either `@fontsource` is imported OR `static/fonts/` contains `.woff2` files; no reference to the Google Fonts CDN host
    - `.tool-versions` pins `nodejs 22.` (even LTS)
    - `.pre-commit-config.yaml` exists with a local hook running `npm run check`; `docs/dev-env.md` documents the Cloudflare Pages build settings + the verify-before-commit gate
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && grep -q "@theme" src/app.css && grep -q "#0369A1" src/app.css && grep -q "#F59E0B" src/app.css && grep -q "prefers-reduced-motion" src/app.css && grep -Eq "nodejs (22|22\.)" .tool-versions && test -f .pre-commit-config.yaml && test -f docs/dev-env.md && { grep -Rq "@fontsource" src/app.css package.json || ls static/fonts/*.woff2 >/dev/null 2>&1; }</automated>
  </verify>
  <done>Two-tier `@theme` tokens locked in `app.css` matching the UI-SPEC; fonts self-hosted (no Google CDN); global focus-ring + reduced-motion base present; Node pinned to 22.x LTS; pre-commit gate + `docs/dev-env.md` in place; `npm run check` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → git repo | Committed files (including any accidental secret) cross into version control |
| npm registry → build | Third-party packages are installed and executed at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Info Disclosure | Repo / `.envrc` / `.dev.vars` | high | mitigate | No real secrets exist in Phase 1; `.envrc`/`.dev.vars` are gitignored (verify `.gitignore` covers them); confirm no secret is introduced by the scaffold. `App.Platform.env` stub stays empty. |
| T-01-SC | Tampering | npm installs (SvelteKit/Tailwind/adapter/lucide/axe/eslint) | high | mitigate | Package Legitimacy Audit in 01-RESEARCH.md is clean (all first-party repos, 195k–120M dl/wk, no postinstall scripts). Use `@lucide/svelte` (NOT deprecated `lucide-svelte`). Commit the lockfile. No `[ASSUMED]`/`[SUS]` packages → no blocking human checkpoint required (researcher explicit). |
| T-01-04 | Info Disclosure (reputational) | Placeholder content indexing | low | mitigate | Handled in Plan 04 (`robots.txt` Disallow) + Plan 03 (`Seo.svelte` noindex meta) while on `*.pages.dev` (D-11). |

Block threshold: high. Both high-severity threats (secret leak, supply chain) are mitigated → no blocking condition.
</threat_model>

<verification>
- `npm run build` succeeds and emits `.svelte-kit/cloudflare`
- `npm run check` passes (types + compiler a11y)
- `app.css` contains the AA-verified two-tier tokens verbatim; fonts self-hosted; focus ring + reduced-motion base present
- `tests/home.spec.ts` runs (RED — content assertions fail until Plan 03)
- No `tailwind.config.js`, no `/functions` dir, no `lucide-svelte` dependency
</verification>

<success_criteria>
The SvelteKit app builds to the Cloudflare output, serves `<html lang="pl">`, carries the locked accessible design tokens and self-hosted fonts, has the a11y/test gate and dev-env conventions wired, and the failing homepage acceptance test is authored (RED). This is the deployable spine for Plans 02–05.
</success_criteria>

<output>
Create `.planning/phases/01-live-homepage-design-foundation/01-01-SUMMARY.md` when done.
</output>
