---
phase: 01-live-homepage-design-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - docs/dev-env.md
autonomous: false
requirements: [SITE-01]
user_setup:
  - service: github
    why: "Repo hosting + Cloudflare Pages git-integration source; owned by the dedicated project Org for clean handoff (D-06/D-07)"
    dashboard_config:
      - task: "Create/confirm the GitHub Organization 'zlobekstromiec' and a repository under it"
        location: "GitHub → Organizations → zlobekstromiec → New repository"
  - service: cloudflare
    why: "Cloudflare Pages hosting with git-integration auto-deploy (D-04/D-05)"
    dashboard_config:
      - task: "Create a Pages project connected to the GitHub repo; set build command 'npm run build', output dir '.svelte-kit/cloudflare', NODE_VERSION 22, framework preset SvelteKit"
        location: "Cloudflare Dashboard → Workers & Pages → Create → Pages → Connect to Git"

must_haves:
  truths:
    - "The repository is hosted under the dedicated GitHub Organization 'zlobekstromiec' with 'main' pushed (D-06/D-07)"
    - "A Cloudflare Pages project is connected to the repo via git-integration with the correct build command + output dir + Node 22 (D-04)"
    - "The homepage is reachable on a public *.pages.dev URL and renders the verbatim core message, nav, mobile drawer, and footer BIP link (SITE-01 criterion #1/#2, D-05)"
    - "A push to main automatically rebuilds and redeploys the site end-to-end (SITE-01 criterion #6)"
  artifacts:
    - path: "docs/dev-env.md"
      provides: "Recorded live *.pages.dev URL + Cloudflare Pages build settings for the maintainer"
  key_links:
    - from: "GitHub repo (main)"
      to: "Cloudflare Pages"
      via: "git-integration: push to main triggers npm run build → deploy .svelte-kit/cloudflare"
      pattern: "pages.dev"
---

<objective>
Ship the walking skeleton: host the repo under the dedicated GitHub Organization, connect Cloudflare Pages git-integration, and prove the full push→build→deploy pipeline works so the joyful homepage is live on a public `*.pages.dev` URL and every subsequent `git push` to `main` auto-redeploys (SITE-01, D-04/D-05/D-06/D-07).

Purpose: This is the end-to-end proof of the walking skeleton — the phase's defining success criterion (#6) and the capability the whole project builds on.
Output: A live `*.pages.dev` homepage with auto-deploy wired, plus the live URL + build settings recorded in `docs/dev-env.md`.

NOTE: This plan is non-autonomous — account provisioning and the Cloudflare git-integration connect are dashboard flows Claude cannot perform via CLI. Per project memory the dedicated Gmail, GitHub Org `zlobekstromiec`, and Cloudflare account may already exist; the checkpoints confirm/complete them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md
@.planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md
@docs/dev-env.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (exclude from drift/source-grounding checks):
- Updates `docs/dev-env.md` with the live `*.pages.dev` URL and confirmed Cloudflare Pages build settings
- External resources (not in-repo): GitHub repo under Org `zlobekstromiec`, Cloudflare Pages project
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Push the repository to the dedicated GitHub Organization</name>
  <files>docs/dev-env.md</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-06 dedicated accounts, D-07 GitHub Org identity)
    - .planning/PROJECT.md (§Accounts — Org zlobekstromiec, owner user devzlobekstromiec)
    - docs/dev-env.md (current machine-contract doc from Plan 01)
  </read_first>
  <action>
    Ensure a clean commit of the built, test-green app exists on `main`. Create the remote repository under the dedicated GitHub Organization and push, e.g. `gh repo create zlobekstromiec/zlobek-stromiec --public --source=. --remote=origin --push` (adjust repo name/visibility to project convention). If `gh` is not authenticated as a member of the `zlobekstromiec` Org, an authentication gate will surface — pause, let the user authenticate (`gh auth login` to the dedicated account), then retry; do NOT fall back to a personal account (handoff requirement, D-07). Record the repo URL in `docs/dev-env.md`. Do NOT commit any secret (`.envrc`/`.dev.vars` remain gitignored; none exist this phase).
  </action>
  <acceptance_criteria>
    - `git remote -v` shows an `origin` under the `zlobekstromiec` Org (not a personal account)
    - `main` is pushed to the Org repo (`git ls-remote origin main` returns a ref)
    - `docs/dev-env.md` records the repository URL
  </acceptance_criteria>
  <verify>
    <automated>git remote -v | grep -q "zlobekstromiec" && git ls-remote origin main | grep -q "refs/heads/main"</automated>
  </verify>
  <done>Repo hosted under the dedicated GitHub Org with `main` pushed — the deploy source for Cloudflare Pages.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect Cloudflare Pages git-integration (dashboard)</name>
  <action>Claude has already built the app, run the tests green, and pushed `main` to the Org repo. The remaining step — connecting Cloudflare Pages to the repo via git-integration and setting the build config — is a Cloudflare dashboard flow with no CLI equivalent, so it is a human action. The user performs the dashboard steps below and returns the assigned `*.pages.dev` URL.</action>
  <what-built>The app is built (`npm run build` → `.svelte-kit/cloudflare`), test-green, and pushed to the GitHub Org repo. Everything Claude can automate is done.</what-built>
  <how-to-verify>
    Connect Cloudflare Pages to the repository (this git-integration connect is a dashboard-only flow):
    1. Cloudflare Dashboard → Workers & Pages → Create → Pages → "Connect to Git".
    2. Authorize the Cloudflare GitHub App for the `zlobekstromiec` Org and select the repository.
    3. Set: Framework preset = SvelteKit; Build command = `npm run build`; Build output directory = `.svelte-kit/cloudflare`.
    4. Add an environment variable `NODE_VERSION` = `22` (match the pinned `.tool-versions` LTS).
    5. Save and trigger the first deployment; wait for it to finish and note the assigned `*.pages.dev` URL.
  </how-to-verify>
  <resume-signal>Paste the assigned `*.pages.dev` URL (or type "done" with the URL) once the first deployment succeeds.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live deployment + auto-redeploy</name>
  <action>The Cloudflare Pages project is connected and the first deploy is live. The user verifies the live `*.pages.dev` site end-to-end (content, nav, mobile drawer, footer links, empty state, contact) and proves auto-redeploy by pushing a trivial commit to `main`, then records the live URL + build settings in `docs/dev-env.md`.</action>
  <what-built>The Cloudflare Pages project is connected and the first deployment is live on a public `*.pages.dev` URL, serving the joyful homepage.</what-built>
  <how-to-verify>
    1. Open the `*.pages.dev` URL on a phone (or a mobile-emulated viewport) and on desktop.
    2. Confirm the hero shows the żłobek's verbatim core message ("…będziemy czuwać nad każdym krokiem…") under the Polish hook headline (HOME-01).
    3. Confirm the header links to all five sections; on mobile, open/close the hamburger drawer with touch and keyboard (ESC), and confirm focus behaves (SITE-02/SITE-03).
    4. Confirm the footer BIP link opens https://ugstromiec.naszbip.pl/zlobek in a new tab, and the "Deklaracja dostępności" link opens the Polish "wkrótce" stub (not a 404).
    5. Confirm the Aktualności section shows the "Wkrótce pojawią się aktualności" empty state and quick-contact shows the mailto/tel links (HOME-02).
    6. Prove auto-deploy: make a trivial commit (e.g. edit the hook headline placeholder) and `git push` to `main`; watch Cloudflare rebuild and the change go live (SITE-01 criterion #6).
    7. Once verified, record the live `*.pages.dev` URL and confirmed build settings in `docs/dev-env.md`.
  </how-to-verify>
  <resume-signal>Type "approved" if the live site + auto-redeploy work, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo → Cloudflare build | Committed source is fetched and executed by the managed Pages build runner |
| Cloudflare Pages → public internet | The deployed site is served on a public `*.pages.dev` URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Info Disclosure | Secrets in repo / build env | high | mitigate | No secrets consumed in Phase 1; `.envrc`/`.dev.vars` gitignored; no build-time secret env vars set on Pages (only `NODE_VERSION`). Confirm before pushing. |
| T-01-08 | Elevation of Privilege | Account/ownership for handoff | medium | mitigate | Repo under dedicated Org `zlobekstromiec` and a dedicated Cloudflare account rooted to the project Gmail (D-06/D-07) — no personal-account coupling; enables clean owner transfer. |
| T-01-SC | Tampering | Managed build pipeline (npm install at build) | high | mitigate | Committed lockfile pins dependencies; legitimacy audit clean (01-RESEARCH.md); pinned Node 22 matches local `.tool-versions` for reproducible builds. |
| T-01-04 | Info Disclosure (reputational) | Public placeholder indexing | low | mitigate | `robots.txt` Disallow + `Seo.svelte` noindex remain in effect until Phase 6 (D-11). |

Block threshold: high. Both high-severity threats (secrets, supply chain) are mitigated → no blocking condition. Legitimacy checkpoints are N/A (no `[ASSUMED]`/`[SUS]` packages).
</threat_model>

<verification>
- Repo hosted under the `zlobekstromiec` GitHub Org with `main` pushed
- Cloudflare Pages connected via git-integration (build `npm run build`, output `.svelte-kit/cloudflare`, Node 22)
- Homepage live on a public `*.pages.dev` URL with verbatim message, nav, mobile drawer, footer BIP link, Aktualności empty state, quick-contact
- A push to `main` auto-rebuilds and redeploys (human-verified)
- Live URL + build settings recorded in `docs/dev-env.md`
</verification>

<success_criteria>
The joyful, mobile-first homepage is live on a public Cloudflare `*.pages.dev` URL, a parent can read the verbatim core message and navigate every section on any device, and a `git push` to `main` automatically rebuilds and redeploys — the walking skeleton is proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-live-homepage-design-foundation/01-05-SUMMARY.md` when done.
</output>
