---
phase: 01-live-homepage-design-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/lib/components/Hero.svelte
  - src/lib/components/Cta.svelte
  - src/lib/components/NewsPreview.svelte
  - src/lib/components/QuickContact.svelte
  - src/lib/components/Seo.svelte
  - src/routes/+page.svelte
  - tests/home.spec.ts
  - tests/responsive.spec.ts
autonomous: true
requirements: [SITE-02, SITE-06, HOME-01, HOME-02]

must_haves:
  truths:
    - "The hero shows the żłobek's core message VERBATIM (final client copy) beneath a short Polish hook headline (HOME-01, D-01, D-02)"
    - "A primary Rekrutacja CTA labelled 'Zapisz dziecko' links to /rekrutacja (HOME-02)"
    - "The Aktualności preview renders its REQUIRED empty state (no posts exist until Phase 3) (HOME-02)"
    - "A quick-contact block surfaces address, phone (tel:), email (mailto:zlobek@ugstromiec.pl), and opening hours (HOME-02)"
    - "The page emits per-route Polish SEO metadata (title, description, canonical, OG/Twitter) and noindex while on *.pages.dev (D-10, D-11)"
    - "The homepage owns exactly one <h1>; layout is responsive across phone/tablet/desktop (SITE-02)"
    - "All visitor-facing copy is Polish; every non-final placeholder carries a greppable PLACEHOLDER token (SITE-06, Pitfall 12)"
  artifacts:
    - path: "src/lib/components/Hero.svelte"
      provides: "Hero: hook h1 (PLACEHOLDER) + verbatim core message (final) + CTA slot + image placeholder"
      contains: "będziemy czuwać nad każdym krokiem"
    - path: "src/lib/components/Cta.svelte"
      provides: "CTA button with primary (amber/ink) and secondary (blue outline) variants"
    - path: "src/lib/components/NewsPreview.svelte"
      provides: "Aktualności preview with required empty state"
      contains: "Wkrótce pojawią się aktualności"
    - path: "src/lib/components/QuickContact.svelte"
      provides: "Quick-contact block (adres/telefon/e-mail/godziny)"
      contains: "zlobek@ugstromiec.pl"
    - path: "src/lib/components/Seo.svelte"
      provides: "Reusable per-route head: title/description/canonical/OG/Twitter + noindex on pages.dev"
    - path: "src/routes/+page.svelte"
      provides: "Composed homepage owning the single h1"
    - path: "tests/responsive.spec.ts"
      provides: "Viewport-matrix responsive test (phone/tablet/desktop)"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/Hero.svelte"
      via: "homepage composes Hero + Cta + NewsPreview + QuickContact + Seo"
      pattern: "Hero"
    - from: "src/lib/components/Hero.svelte"
      to: "src/lib/components/Cta.svelte"
      via: "hero renders the primary Rekrutacja CTA"
      pattern: "Cta"
---

<objective>
Compose the homepage content slice that turns the acceptance test GREEN: the hero leading with the żłobek's core message VERBATIM under a short Polish hook (HOME-01, D-01/D-02), the primary Rekrutacja CTA, the Aktualności preview in its required empty state, quick-contact, and the reusable Polish SEO/head mechanism with noindex while on `*.pages.dev` (D-10/D-11). This is the user-facing payload of the walking skeleton.

Purpose: After this plan a parent lands on the homepage and immediately reads the warm core message, sees how to enrol (CTA), where news will appear, and how to make contact — the phase's core value.
Output: Hero/Cta/NewsPreview/QuickContact/Seo components + composed `+page.svelte` + GREEN home.spec.ts + responsive.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
@.planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md
@.planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md
@.planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (exclude from drift/source-grounding checks):
- Files: `src/lib/components/Hero.svelte`, `Cta.svelte`, `NewsPreview.svelte`, `QuickContact.svelte`, `Seo.svelte`; finalizes `src/routes/+page.svelte`; extends `tests/home.spec.ts`; creates `tests/responsive.spec.ts`
- Svelte components: `Hero`, `Cta` (variant prop: `primary` | `secondary`), `NewsPreview`, `QuickContact`, `Seo` (props: `title`, `description`, `canonical`, `image`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hero (verbatim core message) + Cta variants</name>
  <files>src/lib/components/Hero.svelte, src/lib/components/Cta.svelte</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Hero; §Rekrutacja CTA; §Color hard rules — CTA label ink on amber, white only on active; §Copywriting Contract)
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-01 hook+verbatim lead, D-02 verbatim is final copy, D-03 image placeholder non-identifiable + WebP/AVIF + OG reuse)
    - .planning/PROJECT.md (line 47 — the exact verbatim core message to paste)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (Hero.svelte / Cta.svelte pattern assignments; RESEARCH Pitfall 9 image bloat)
    - src/app.css (tokens: accent, accent-hover, accent-active, brand-blue, ink, focus-ring, radius-pill, radius-lg)
  </read_first>
  <behavior>
    - Hero h1 is a short Polish hook (marked PLACEHOLDER); directly beneath, the verbatim 4-sentence core message renders as a styled lead quote (final copy, NOT a placeholder)
    - The verbatim text contains "będziemy czuwać nad każdym krokiem Twojej pociechy" exactly as in PROJECT.md
    - Cta primary: amber #F59E0B fill with ink label by default; hover #D97706; active #B45309 with WHITE label; focus-visible 3px ring offset 2px; pill shape; min-height 44px
    - Cta secondary: transparent, 2px brand-blue border, brand-blue label, hover fill #E0F2FE; never amber
    - Hero image slot is a non-identifiable warm placeholder (no child faces) shipped as WebP/AVIF with explicit width/height and fetchpriority="high"
  </behavior>
  <action>
    Create `src/lib/components/Cta.svelte` with a `variant` prop (`primary` | `secondary`), pill shape (`radius-pill`), padding 12px 24px, min-height 44px, 16px/700, optional `@lucide/svelte` `ArrowRight` icon `aria-hidden`. Primary: default fill `accent` `#F59E0B` + label `ink` `#1E293B`; hover `accent-hover` `#D97706` + slight raise; focus-visible 3px `focus-ring` outline offset 2px; active `accent-active` `#B45309` + label WHITE (the ONLY white-on-amber state — never white on the default fill). Secondary: transparent fill, 2px `brand-blue` border, `brand-blue` label, hover fill `band` `#E0F2FE`; never amber. Create `src/lib/components/Hero.svelte` per D-01: an `<h1>` short Polish hook headline (`font-display`) marked with a `PLACEHOLDER` token, and directly beneath it the żłobek's verbatim 4-sentence core message from PROJECT.md line 47 as a styled lead quote (`font-body`, `muted`) — this is FINAL client copy (D-02), do NOT mark it PLACEHOLDER and do NOT alter a single character. Render the primary `Cta` (`Zapisz dziecko` → `/rekrutacja`) and an optional secondary `Cta` (`Poznaj żłobek` → `/o-nas`). Add the hero image slot: `radius-lg`, a non-identifiable warm placeholder (environment/toys/hands or clearly-synthetic — NO real child faces per D-03), shipped as WebP/AVIF with explicit `width`/`height` (no CLS) and `fetchpriority="high"`, marked `PLACEHOLDER`, `aria-hidden` while decorative (real `alt` when a photo lands in Phase 6); it doubles as the OG share image (D-03/D-10). Place decorative `expr-*` blobs behind at low z-index, `aria-hidden`. Layout: stacked (base) → 2-col at `lg`; section `py` 48→96. Do NOT put any `text-expr-*` / bright yellow or orange on text.
  </action>
  <acceptance_criteria>
    - `Hero.svelte` contains the verbatim substring `będziemy czuwać nad każdym krokiem Twojej pociechy` and an `<h1>` with a `PLACEHOLDER` token on the hook headline only
    - `Cta.svelte` supports `primary` and `secondary` variants; primary default is amber fill with ink label (white label only on the active state)
    - The hero image tag has explicit `width` and `height` and `fetchpriority="high"`
    - No `text-expr-` utility or bright yellow/orange applied to any text
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && grep -q "będziemy czuwać nad każdym krokiem Twojej pociechy" src/lib/components/Hero.svelte && grep -q "Zapisz dziecko" src/lib/components/Hero.svelte && grep -q "fetchpriority" src/lib/components/Hero.svelte && grep -Eq "variant" src/lib/components/Cta.svelte</automated>
  </verify>
  <done>Hero leads with the verbatim (final) core message under a Polish hook, with the primary Rekrutacja CTA and a consent-safe placeholder image; CTA variants honor the AA color rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aktualności preview (empty state) + quick-contact block</name>
  <files>src/lib/components/NewsPreview.svelte, src/lib/components/QuickContact.svelte</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Latest-Aktualności preview — REQUIRED empty state; §Quick-contact block; §Copywriting Contract — empty-state heading + body)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (NewsPreview.svelte / QuickContact.svelte pattern assignments)
    - src/app.css (tokens: surface-warm, band, brand-blue, muted, radius-md)
  </read_first>
  <behavior>
    - NewsPreview renders the header row (h2 "Aktualności" + secondary link "Zobacz wszystkie" → /aktualnosci) and, because no posts exist until Phase 3, the REQUIRED empty state panel with heading "Wkrótce pojawią się aktualności" and the UI-SPEC body text
    - QuickContact renders four items with Polish labels: Adres (PLACEHOLDER), Telefon (tel: link, PLACEHOLDER value), E-mail (mailto:zlobek@ugstromiec.pl), Godziny otwarcia
    - Tel/email are brand-blue underlined links with ≥44px targets; icons are aria-hidden
  </behavior>
  <action>
    Create `src/lib/components/NewsPreview.svelte` per UI-SPEC §Latest-Aktualności: header row with an `<h2>` `Aktualności` and a secondary link `Zobacz wszystkie` → `/aktualnosci`; render the REQUIRED empty state (no posts until Phase 3) as a friendly panel (`surface-warm` `#FBFAF7`, `radius-md`, centered) with a `@lucide/svelte` `Newspaper` icon `aria-hidden`, heading `Wkrótce pojawią się aktualności`, and body `Nie opublikowaliśmy jeszcze żadnych wpisów. Zajrzyj tu wkrótce — będziemy informować o wydarzeniach i nowościach z życia żłobka.` Create `src/lib/components/QuickContact.svelte` per UI-SPEC §Quick-contact: four items each with a `@lucide/svelte` icon `aria-hidden` + Polish label + value — `Adres` = `ul. [do uzupełnienia], 26-804 Stromiec` (PLACEHOLDER), `Telefon` = `+48 00 000 00 00` rendered as a `tel:` link (PLACEHOLDER, 44px target), `E-mail` = `zlobek@ugstromiec.pl` rendered as a `mailto:` link (44px target), `Godziny otwarcia` = `pon.–pt. 6:30–16:30`. Tel/email links are `brand-blue` + underline. Mark every non-final value with a greppable `PLACEHOLDER` token (the email address is final — do not mark it). All copy Polish (SITE-06).
  </action>
  <acceptance_criteria>
    - `NewsPreview.svelte` contains the exact empty-state heading `Wkrótce pojawią się aktualności` and the secondary link to `/aktualnosci`
    - `QuickContact.svelte` contains a `mailto:zlobek@ugstromiec.pl` link and a `tel:` link, with Polish labels Adres/Telefon/E-mail/Godziny
    - Placeholder values carry a `PLACEHOLDER` token; the email is not marked placeholder
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && grep -q "Wkrótce pojawią się aktualności" src/lib/components/NewsPreview.svelte && grep -q "mailto:zlobek@ugstromiec.pl" src/lib/components/QuickContact.svelte && grep -q "tel:" src/lib/components/QuickContact.svelte</automated>
  </verify>
  <done>Aktualności preview shows the required friendly empty state; quick-contact surfaces address/phone/email/hours with accessible Polish links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SEO/head component + compose homepage (turn acceptance test GREEN)</name>
  <files>src/lib/components/Seo.svelte, src/routes/+page.svelte, tests/home.spec.ts, tests/responsive.spec.ts</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Pattern 5 SEO/head noindex; §Validation Architecture — SITE-02/HOME-01/HOME-02 test map; Pitfall 11)
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-10 metadata mechanism, D-11 noindex on pages.dev, D-12 JSON-LD/GSC DEFERRED to Phase 6)
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Hero/CTA/News/Quick-contact contracts — composition order + single h1)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (Seo.svelte / +page.svelte pattern assignments; tests/home.spec.ts)
    - src/lib/components/Hero.svelte, Cta.svelte, NewsPreview.svelte, QuickContact.svelte (from Tasks 1-2)
    - tests/home.spec.ts (the RED test authored in Plan 01 — extend, do not weaken)
  </read_first>
  <behavior>
    - Seo emits a per-route Polish <title>, meta description, canonical link, and Open Graph + Twitter card tags using a placeholder share image, plus a noindex robots meta while on *.pages.dev (D-11)
    - Homepage composes Seo + Hero + Cta + NewsPreview(empty) + QuickContact and owns exactly one <h1> (in Hero)
    - home.spec.ts (extended) passes: verbatim message, CTA, empty state, mailto, and zero axe wcag21aa violations (GREEN)
    - responsive.spec.ts passes across phone/tablet/desktop viewports (SITE-02)
  </behavior>
  <action>
    Create `src/lib/components/Seo.svelte` (RESEARCH Pattern 5): props `title`, `description`, `canonical`, `image`; render into `<svelte:head>` a Polish `<title>`, `meta name="description"`, `link rel="canonical"`, and Open Graph (`og:title`/`og:description`/`og:image`/`og:type`/`og:locale=pl_PL`) + Twitter card tags using the placeholder share image `/og-placeholder` (authored in Plan 04). Add `<meta name="robots" content="noindex">` while on `*.pages.dev` (D-11) — this flips at Phase 6. Do NOT add JSON-LD structured data or Google Search Console tags (D-12 — deferred to Phase 6). Finalize `src/routes/+page.svelte` to compose, in order, `Seo` (Polish homepage title/description) + `Hero` + `NewsPreview` (empty state) + `QuickContact`, ensuring exactly one `<h1>` (owned by Hero). Extend `tests/home.spec.ts` (the RED test from Plan 01) with the HOME-01/HOME-02 assertions if not already fully covered, and confirm the axe scan passes — do NOT weaken any assertion to force a pass. Create `tests/responsive.spec.ts` asserting the homepage renders without horizontal overflow and the header/nav adapts across a viewport matrix (e.g. 375×667 phone, 768×1024 tablet, 1280×800 desktop) — at phone width the hamburger is visible; at desktop width the inline nav links are visible.
  </action>
  <acceptance_criteria>
    - `Seo.svelte` emits `<title>`, `meta[name=description]`, `link[rel=canonical]`, OG + Twitter tags, and a `noindex` robots meta; contains NO JSON-LD (`application/ld+json`)
    - `+page.svelte` composes Seo + Hero + NewsPreview + QuickContact and the rendered page has exactly one `<h1>`
    - `npx playwright test tests/home.spec.ts` passes (GREEN — verbatim message, CTA, empty state, mailto, zero axe AA violations)
    - `npx playwright test tests/responsive.spec.ts` passes across the three viewports
    - `npm run check && npm run lint` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npm run lint && npx playwright test tests/home.spec.ts tests/responsive.spec.ts && ! grep -q "application/ld+json" src/lib/components/Seo.svelte</automated>
  </verify>
  <done>Homepage composed and the full acceptance test is GREEN: verbatim core message, Rekrutacja CTA, Aktualności empty state, quick-contact, Polish SEO metadata with noindex, single h1, responsive, zero axe AA violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Prerendered homepage HTML/CSS + one hydrated island are served to the visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Info Disclosure (reputational) | Placeholder homepage content on public `*.pages.dev` | low | mitigate | `Seo.svelte` emits `noindex` while on `*.pages.dev` (D-11), paired with Plan 04 `robots.txt` Disallow. Every non-final placeholder carries a `PLACEHOLDER` token for the Phase 6 pre-launch grep gate. |
| T-01-06 | Info Disclosure | `mailto:` / `tel:` links (exposed contact address) | low | accept | `zlobek@ugstromiec.pl` is the public institutional address by design (public-body contact); no personal data exposed. Accepted. |

Block threshold: high. No high-severity threats in this plan.
</threat_model>

<verification>
- `npm run check && npm run lint` pass
- `npx playwright test tests/home.spec.ts tests/responsive.spec.ts` pass (GREEN)
- Hero shows the verbatim core message (final copy) under a Polish hook
- Primary Rekrutacja CTA + Aktualności empty state + quick-contact present
- Per-route Polish SEO metadata emitted with noindex; no JSON-LD (deferred)
- Exactly one `<h1>`; responsive across phone/tablet/desktop
</verification>

<success_criteria>
A parent opening the homepage reads the żłobek's warm core message verbatim, sees a clear Rekrutacja call-to-action, a friendly "news coming soon" state, and quick contact — all Polish, responsive, AA-clean, with professional share-preview metadata and no placeholder indexing.
</success_criteria>

<output>
Create `.planning/phases/01-live-homepage-design-foundation/01-03-SUMMARY.md` when done.
</output>
