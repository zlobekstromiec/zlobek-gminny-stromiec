---
phase: 01-live-homepage-design-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - static/robots.txt
  - static/sitemap.xml
  - static/_headers
  - static/favicon.svg
  - static/favicon.png
  - static/apple-touch-icon.png
  - static/site.webmanifest
  - static/og-placeholder.png
  - src/routes/deklaracja-dostepnosci/+page.svelte
autonomous: true
requirements: [SITE-01, SITE-06]

must_haves:
  truths:
    - "robots.txt disallows all crawling while on *.pages.dev, and sitemap.xml is scaffolded noindex-aligned (D-11)"
    - "Security headers are shipped via static/_headers: CSP, HSTS, X-Content-Type-Options nosniff, Referrer-Policy, X-Frame-Options (ASVS L1 V14 config)"
    - "A favicon set and a branded 1200x630 placeholder OG share image exist so a shared *.pages.dev link looks professional (D-10)"
    - "A Polish /deklaracja-dostepnosci stub route exists so the footer's legally-sensitive link does not 404 before Phase 6 (RESEARCH A4/Open Q2)"
  prohibitions:
    - statement: "The site must NOT be indexable while on the *.pages.dev placeholder (no allow-all robots, no JSON-LD, no GSC verification)"
      status: resolved
      verification: "grep static/robots.txt for 'Disallow: /'; confirm no 'application/ld+json' and no google-site-verification introduced this phase"
  artifacts:
    - path: "static/robots.txt"
      provides: "Disallow-all crawl directive for the placeholder deployment"
      contains: "Disallow: /"
    - path: "static/_headers"
      provides: "Cloudflare Pages security headers baseline"
      contains: "X-Content-Type-Options"
    - path: "static/og-placeholder.png"
      provides: "Branded 1200x630 placeholder social share image"
    - path: "static/site.webmanifest"
      provides: "PWA/manifest metadata for favicon set"
    - path: "src/routes/deklaracja-dostepnosci/+page.svelte"
      provides: "Polish 'wkrótce' accessibility-declaration stub route"
  key_links:
    - from: "static/_headers"
      to: ".svelte-kit/cloudflare"
      via: "adapter-cloudflare copies static/_headers into the Pages deploy output"
      pattern: "_headers"
    - from: "src/routes/deklaracja-dostepnosci/+page.svelte"
      to: "src/lib/components/Footer.svelte"
      via: "resolves the footer 'Deklaracja dostępności' link (created in Plan 02)"
      pattern: "deklaracja-dostepnosci"
---

<objective>
Ship the deployment-hygiene and metadata artifacts that make the placeholder site professional and safe: `robots.txt` + `sitemap.xml` set to noindex/disallow (D-11), a security-headers baseline via `static/_headers` (ASVS L1 config controls), a favicon set + branded placeholder OG share image (D-10), and a Polish `/deklaracja-dostepnosci` stub route so the footer's legally-sensitive accessibility link never 404s before Phase 6 (resolves RESEARCH A4/Open Q2 — planner decision: STUB it).

Purpose: A shared `*.pages.dev` link looks intentional, search engines never index placeholder content, the public-body site carries a security-header baseline from day one, and no footer link is dead.
Output: `robots.txt`, `sitemap.xml`, `_headers`, favicon set, `og-placeholder.png`, and the accessibility-declaration stub route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md
@.planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md
@.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
@.planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (exclude from drift/source-grounding checks):
- Files: `static/robots.txt`, `static/sitemap.xml`, `static/_headers`, `static/favicon.svg`, `static/favicon.png`, `static/apple-touch-icon.png`, `static/site.webmanifest`, `static/og-placeholder.png`, `src/routes/deklaracja-dostepnosci/+page.svelte`
- Route: `/deklaracja-dostepnosci` (prerendered Polish stub)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: robots.txt + sitemap.xml (noindex-aligned for the placeholder)</name>
  <files>static/robots.txt, static/sitemap.xml</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-11 robots/sitemap noindex on *.pages.dev, flip at Phase 6; D-12 JSON-LD/GSC deferred)
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (Pitfall 11 Polish SEO/metadata; §Security Domain — indexing of placeholder content)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (static/robots.txt, static/sitemap.xml pattern assignments)
  </read_first>
  <action>
    Create `static/robots.txt` that disallows all crawling while on the `*.pages.dev` placeholder: a `User-agent: *` group with `Disallow: /` (D-11). Add a short comment noting this flips to an allow + real sitemap reference at Phase 6 when the custom domain goes live. Create `static/sitemap.xml` scaffolded with the homepage URL entry (using a relative or `*.pages.dev` placeholder host), consistent with the noindex posture — it exists as the mechanism but is not advertised to crawlers yet. Do NOT add a Google Search Console verification token or JSON-LD (D-12 — Phase 6).
  </action>
  <acceptance_criteria>
    - `static/robots.txt` contains a `User-agent: *` group and `Disallow: /`
    - `static/sitemap.xml` is well-formed XML with at least the homepage `<url>` entry
    - No `google-site-verification` token present
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Disallow: /" static/robots.txt && grep -q "<urlset" static/sitemap.xml && ! grep -rq "google-site-verification" static/</automated>
  </verify>
  <done>Placeholder deployment is crawl-disallowed; sitemap scaffolded noindex-aligned; no premature GSC/JSON-LD.</done>
</task>

<task type="auto">
  <name>Task 2: Security-headers baseline + favicon set + placeholder OG image</name>
  <files>static/_headers, static/favicon.svg, static/favicon.png, static/apple-touch-icon.png, static/site.webmanifest, static/og-placeholder.png</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Security Domain — recommended static/_headers CSP/HSTS/nosniff/Referrer-Policy; Known Threat Patterns table)
    - .planning/phases/01-live-homepage-design-foundation/01-CONTEXT.md (D-10 favicon set + branded placeholder share image)
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Color — brand palette for the OG card; wordmark)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (static/_headers, favicon set + og-placeholder pattern assignments)
    - src/app.css (self-hosted fonts served from own origin — CSP font-src 'self')
  </read_first>
  <action>
    Create `static/_headers` (Cloudflare Pages format) applying to all routes: a conservative Content-Security-Policy allowing `default-src 'self'`, `style-src 'self' 'unsafe-inline'` (Svelte scoped styles), `font-src 'self'` (self-hosted WOFF2), `img-src 'self' data:`, `script-src 'self'`, `base-uri 'self'`, `frame-ancestors 'none'`; `Strict-Transport-Security: max-age=31536000; includeSubDomains`; `X-Content-Type-Options: nosniff`; `Referrer-Policy: strict-origin-when-cross-origin`; `X-Frame-Options: DENY`. Note in a comment that Phases 2–4 will extend the CSP (CMS `/admin`, Turnstile script, Resend). Create the favicon set: `favicon.svg` + `favicon.png` + `apple-touch-icon.png` + `site.webmanifest` (Polish `name`/`short_name`, brand `theme_color` `#0369A1`, `background_color` `#FFFFFF`) using the two-tier brand palette. Create `static/og-placeholder.png` at 1200×630 — a branded placeholder share card (wordmark `Żłobek Gminny Stromiec` on a warm brand-palette background, using the accessible tier for any text) referenced by `Seo.svelte` (Plan 03). Keep images optimized (Pitfall 9). Ensure `src/app.html` references the favicon/manifest if the scaffold default did not.
  </action>
  <acceptance_criteria>
    - `static/_headers` contains `Content-Security-Policy`, `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, and `X-Frame-Options`
    - The favicon set exists (`favicon.svg`, `favicon.png`, `apple-touch-icon.png`, `site.webmanifest`) and the manifest has Polish name + brand theme color
    - `static/og-placeholder.png` exists at 1200×630
    - `npm run build` copies `static/_headers` into `.svelte-kit/cloudflare`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "X-Content-Type-Options" static/_headers && grep -q "Content-Security-Policy" static/_headers && grep -q "Strict-Transport-Security" static/_headers && test -f static/og-placeholder.png && test -f static/site.webmanifest && npm run build && test -f .svelte-kit/cloudflare/_headers</automated>
  </verify>
  <done>Security-header baseline shipped, favicon set + branded 1200×630 OG placeholder in place, all copied into the Pages build output.</done>
</task>

<task type="auto">
  <name>Task 3: Accessibility-declaration stub route (no dead footer link)</name>
  <files>src/routes/deklaracja-dostepnosci/+page.svelte</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (Open Q2 + Assumption A4 — stub vs accept 404; Pitfall 14 note; §Architectural Responsibility Map — prerender)
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Footer — Deklaracja dostępności link; §Accessibility Contract — landmarks, single h1 per page)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (deklaracja-dostepnosci stub pattern assignment)
  </read_first>
  <action>
    Create `src/routes/deklaracja-dostepnosci/+page.svelte` as a minimal Polish "wkrótce" stub so the footer's `Deklaracja dostępności` link (created in Plan 02) resolves instead of 404ing before Phase 6 (planner decision resolving RESEARCH A4/Open Q2). Render a single `<h1>` `Deklaracja dostępności` and a short body `Deklaracja dostępności zostanie opublikowana wkrótce.` marked with a `PLACEHOLDER` token; the full conformant declaration is authored in Phase 6. Set a page `<title>` via `<svelte:head>` (Polish) — do NOT import `Seo.svelte` (it belongs to Plan 03, a later wave; keep this route self-contained). The route inherits `prerender = true` from the root layout. Draw colors from the accessible tier only.
  </action>
  <acceptance_criteria>
    - `src/routes/deklaracja-dostepnosci/+page.svelte` renders an `<h1>` `Deklaracja dostępności` and a Polish "wkrótce" body carrying a `PLACEHOLDER` token
    - The route does not import `Seo.svelte`
    - `npm run build` prerenders `/deklaracja-dostepnosci` (no 404)
  </acceptance_criteria>
  <verify>
    <automated>npm run check && grep -q "Deklaracja dostępności" src/routes/deklaracja-dostepnosci/+page.svelte && npm run build && { test -f .svelte-kit/cloudflare/deklaracja-dostepnosci.html || test -f .svelte-kit/cloudflare/deklaracja-dostepnosci/index.html; }</automated>
  </verify>
  <done>Polish accessibility-declaration stub exists and prerenders, so the footer's legally-sensitive link resolves before the Phase 6 declaration is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Static assets + response headers are served to the visitor |
| search engine → site | Crawlers request `/robots.txt` and page content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Info Disclosure / Tampering | Missing security headers on a public-body site | medium | mitigate | `static/_headers` ships CSP, HSTS, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `X-Frame-Options: DENY`; verified copied into the build output. Extended by Phases 2–4. |
| T-01-04 | Info Disclosure (reputational) | Search-engine indexing of placeholder content | low | mitigate | `robots.txt` `Disallow: /` on `*.pages.dev` (D-11), paired with the `Seo.svelte` noindex meta (Plan 03). |
| T-01-07 | Spoofing (visual) | Blank/ugly social share preview of the placeholder link | low | mitigate | Branded 1200×630 `og-placeholder.png` + favicon set so shared links look intentional (D-10). |

Block threshold: high. No high-severity threats in this plan.
</threat_model>

<verification>
- `robots.txt` disallows all; `sitemap.xml` well-formed; no GSC/JSON-LD
- `static/_headers` carries CSP/HSTS/nosniff/Referrer-Policy/X-Frame-Options and is copied into `.svelte-kit/cloudflare`
- Favicon set + 1200×630 `og-placeholder.png` present
- `/deklaracja-dostepnosci` prerenders (footer link not dead)
</verification>

<success_criteria>
The placeholder deployment is crawl-safe, ships a security-header baseline, presents a professional favicon + share preview, and has no dead footer link — the deployment-hygiene foundation Phases 2–6 extend.
</success_criteria>

<output>
Create `.planning/phases/01-live-homepage-design-foundation/01-04-SUMMARY.md` when done.
</output>
