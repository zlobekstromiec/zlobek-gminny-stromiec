---
phase: 01-live-homepage-design-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/nav.ts
  - src/lib/components/SkipLink.svelte
  - src/lib/components/Header.svelte
  - src/lib/components/MobileNav.svelte
  - src/lib/components/Footer.svelte
  - src/routes/+layout.svelte
  - tests/nav.spec.ts
autonomous: true
requirements: [SITE-02, SITE-03, SITE-06]

must_haves:
  truths:
    - "A persistent header links to all five sections: Aktualności, O nas, Rekrutacja, Dokumenty, Kontakt (SITE-03)"
    - "The active section is indicated by a brand-blue bottom bar AND aria-current=page (not color alone)"
    - "Below the md breakpoint the nav collapses to a keyboard-operable mobile drawer: hamburger toggles a role=dialog aria-modal panel with focus trap, ESC-to-close, restore focus, and body scroll lock (SITE-02, A11Y baseline)"
    - "The footer links to BIP (exact label, external, rel=noopener noreferrer, new-tab suffix), Deklaracja dostępności, and Kontakt (SITE-03)"
    - "The layout renders exactly one <header>, one <nav aria-label>, one <main id=main>, one <footer>; the skip link is the first focusable element"
    - "All navigation copy is Polish (SITE-06)"
  artifacts:
    - path: "src/lib/nav.ts"
      provides: "Single source of the five nav links (label + href)"
      exports: ["navLinks"]
    - path: "src/lib/components/Header.svelte"
      provides: "Sticky header with wordmark + 5 desktop links + active state"
    - path: "src/lib/components/MobileNav.svelte"
      provides: "Hydrated hamburger drawer island (focus trap, ESC, restore focus, scroll lock)"
    - path: "src/lib/components/Footer.svelte"
      provides: "Footer with BIP external link, Deklaracja dostępności, Kontakt, org line"
    - path: "src/lib/components/SkipLink.svelte"
      provides: "Skip-to-content link, first focusable, visually hidden until focused"
    - path: "src/routes/+layout.svelte"
      provides: "Semantic landmark shell wiring SkipLink + Header + main + Footer"
    - path: "tests/nav.spec.ts"
      provides: "E2E test for header links, footer links/BIP, and drawer keyboard behavior"
  key_links:
    - from: "src/lib/components/Header.svelte"
      to: "src/lib/nav.ts"
      via: "imports the five nav links from the single source"
      pattern: "from '\\$lib/nav'"
    - from: "src/lib/components/MobileNav.svelte"
      to: "src/lib/nav.ts"
      via: "renders the same five links in the drawer"
      pattern: "from '\\$lib/nav'"
    - from: "src/routes/+layout.svelte"
      to: "src/lib/components/Header.svelte"
      via: "layout renders the persistent header + footer around <main>"
      pattern: "Header"
---

<objective>
Build the persistent navigation shell (SITE-03): a sticky header linking to all five sections with an accessible active state, the mobile hamburger drawer island (the walking skeleton's one real hydrated UI interaction — focus trap + ESC + restore focus + scroll lock), a skip link, and a footer linking to BIP (correct label + external-link safety), Deklaracja dostępności, and Kontakt. Finalize the semantic landmark layout shell around `<main id="main">`.

Purpose: A visitor can navigate to every section on any device, keyboard-operably, with the AA landmark structure baked into the shared shell so conformance is the default for every later page.
Output: `nav.ts` + Header/MobileNav/Footer/SkipLink components + finalized `+layout.svelte` + `tests/nav.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
@.planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md
@.planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (exclude from drift/source-grounding checks):
- Files: `src/lib/nav.ts`, `src/lib/components/SkipLink.svelte`, `src/lib/components/Header.svelte`, `src/lib/components/MobileNav.svelte`, `src/lib/components/Footer.svelte`, `tests/nav.spec.ts`; finalizes `src/routes/+layout.svelte`
- Exports: `navLinks` (from `src/lib/nav.ts`) — array of `{ label: string; href: string }`
- Svelte components: `SkipLink`, `Header`, `MobileNav`, `Footer`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nav source + SkipLink + desktop Header</name>
  <files>src/lib/nav.ts, src/lib/components/SkipLink.svelte, src/lib/components/Header.svelte</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Header/Nav; §Interaction — skip link, nav link states; §Copywriting Contract — nav labels)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (Header.svelte / nav.ts pattern assignments; Shared Patterns — Accessibility)
    - src/app.css (design tokens available: brand-blue, ink, border-subtle, shadow-sm, focus-ring)
    - src/routes/+layout.svelte (current minimal shell from Plan 01)
  </read_first>
  <behavior>
    - navLinks exports exactly five entries: Aktualności→/aktualnosci, O nas→/o-nas, Rekrutacja→/rekrutacja, Dokumenty→/dokumenty, Kontakt→/kontakt
    - Header renders the wordmark and all five links; each link has a ≥44px hit area
    - Active section shows a 3px brand-blue bottom bar AND sets aria-current="page"
    - Skip link is visually hidden until focused and targets #main
  </behavior>
  <action>
    Create `src/lib/nav.ts` exporting `navLinks` as the single source of the five Polish nav links (label + href) exactly: `Aktualności` → `/aktualnosci`, `O nas` → `/o-nas`, `Rekrutacja` → `/rekrutacja`, `Dokumenty` → `/dokumenty`, `Kontakt` → `/kontakt`. Create `src/lib/components/SkipLink.svelte` rendering an anchor to `#main` with the label `Przejdź do treści`, visually hidden until `:focus` (then visible, positioned top-left) — it must be the first focusable element. Create `src/lib/components/Header.svelte` per UI-SPEC §Header/Nav: sticky top bar, wordmark left (`Żłobek Gminny Stromiec`, `font-display`/700, `brand-blue`), the five links right consumed from `$lib/nav`, label style 14px/700, horizontal gap 24px, each link padded to a 44px hit area; wrap the links in `<nav aria-label="Główna nawigacja">`; default link color `ink`, hover `brand-blue` + underline; active link gets a 3px `brand-blue` bottom bar AND `aria-current="page"` derived from `$page.url.pathname` (Svelte 5 runes); header gains `shadow-sm` + hairline `border-subtle` bottom once scrolled. Reserve a slot/placeholder to render `MobileNav` below the `md` breakpoint (built in Task 2) and hide the desktop link list below `md`. All copy Polish (SITE-06). Draw all colors from the accessible tier only.
  </action>
  <acceptance_criteria>
    - `src/lib/nav.ts` exports `navLinks` with the five exact label/href pairs above
    - `Header.svelte` imports from `$lib/nav`, wraps links in `<nav aria-label="Główna nawigacja">`, and sets `aria-current="page"` on the active link
    - `SkipLink.svelte` targets `#main` with label `Przejdź do treści` and is visually hidden until focused
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && grep -q "aria-label=\"Główna nawigacja\"" src/lib/components/Header.svelte && grep -q "aria-current" src/lib/components/Header.svelte && grep -q "/aktualnosci" src/lib/nav.ts && grep -q "Przejdź do treści" src/lib/components/SkipLink.svelte</automated>
  </verify>
  <done>Single nav source + skip link + accessible sticky desktop header with correct active state and Polish labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mobile nav drawer island (hydrated, keyboard-operable)</name>
  <files>src/lib/components/MobileNav.svelte</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Header/Nav Mobile drawer contract; §Motion — reduced motion)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (MobileNav.svelte pattern assignment; Shared Patterns — Motion, Accessibility; RESEARCH Pitfall 10, Don't-Hand-Roll focus trap)
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Architectural Responsibility Map — this is the only hydrated island)
    - src/lib/nav.ts (links to render)
    - src/lib/components/Header.svelte (integration point from Task 1)
  </read_first>
  <behavior>
    - Hamburger button (44×44) has aria-label "Otwórz menu" when closed / "Zamknij menu" when open, plus aria-expanded and aria-controls pointing at the drawer
    - Opening sets the drawer role="dialog" aria-modal="true", moves focus to the close button, traps Tab focus within the drawer, locks body scroll
    - ESC closes the drawer and returns focus to the hamburger
    - The five links render stacked, each ≥44px full-width tap target, from $lib/nav
    - Under prefers-reduced-motion the slide animation is replaced by instant show/hide
  </behavior>
  <action>
    Create `src/lib/components/MobileNav.svelte` as the single hydrated island (Svelte 5 runes: `$state` for open/closed). Render a 44×44 hamburger `<button>` with `aria-label` toggling `Otwórz menu` / `Zamknij menu`, `aria-expanded`, and `aria-controls` referencing the drawer element id. When open, render a slide-in panel from the right over a scrim with `role="dialog"` `aria-modal="true"`: move initial focus to the close button, implement a Tab/Shift+Tab focus trap over the drawer's focusable elements, close on `Escape` and on scrim click, restore focus to the hamburger on close, and lock body scroll while open. Render the five links stacked (from `$lib/nav`), each a full-width native `<a>` with ≥44px height. Build from native `<button>`/`<a>` semantics (do not hand-roll click-divs). Wrap the slide transition so `@media (prefers-reduced-motion: reduce)` swaps it for instant show/hide. Integrate into `Header.svelte` so it appears only below the `md` breakpoint.
  </action>
  <acceptance_criteria>
    - `MobileNav.svelte` hamburger sets `aria-expanded`, `aria-controls`, and toggles `aria-label` between `Otwórz menu` and `Zamknij menu`
    - Open drawer element has `role="dialog"` and `aria-modal="true"`
    - Escape closes the drawer and focus returns to the hamburger (covered by tests/nav.spec.ts in Task 3)
    - Body scroll is locked while open and released on close
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && grep -q "aria-modal=\"true\"" src/lib/components/MobileNav.svelte && grep -q "Otwórz menu" src/lib/components/MobileNav.svelte && grep -q "aria-expanded" src/lib/components/MobileNav.svelte</automated>
  </verify>
  <done>Keyboard-operable mobile drawer island with focus trap, ESC-close, restore focus, scroll lock, and reduced-motion support — the walking skeleton's real hydrated interaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Footer + finalized semantic layout shell + nav E2E test</name>
  <files>src/lib/components/Footer.svelte, src/routes/+layout.svelte, tests/nav.spec.ts</files>
  <read_first>
    - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md (§Footer; §Accessibility Contract — landmarks; §Copywriting Contract — footer org line, BIP suffix)
    - .planning/phases/01-live-homepage-design-foundation/01-PATTERNS.md (Footer.svelte + +layout.svelte pattern assignments; RESEARCH Pitfall 14 BIP link)
    - .planning/phases/01-live-homepage-design-foundation/01-RESEARCH.md (§Validation Architecture — SITE-03 test map; §Security Domain — external-link safety)
    - src/routes/+layout.svelte (current minimal shell from Plan 01)
    - src/lib/components/Header.svelte, src/lib/components/SkipLink.svelte (to wire)
  </read_first>
  <behavior>
    - Footer BIP link labelled exactly "Biuletyn Informacji Publicznej (BIP)" → https://ugstromiec.naszbip.pl/zlobek, target="_blank" rel="noopener noreferrer", with a visually-hidden "(otwiera się w nowej karcie)" suffix
    - Footer includes "Deklaracja dostępności" → /deklaracja-dostepnosci and "Kontakt" → /kontakt, plus the org line
    - Layout renders exactly one header, one nav, one main#main, one footer, with SkipLink first
    - nav.spec.ts verifies the five header links, footer links (incl. BIP url + rel), and drawer open/ESC-close/focus behavior at a mobile viewport
  </behavior>
  <action>
    Create `src/lib/components/Footer.svelte` per UI-SPEC §Footer: background `surface-warm` (or `band`), `ink`/`muted` text; the org line `Żłobek Gminny w Stromcu — jednostka organizacyjna Gminy Stromiec.`; the BIP link labelled exactly `Biuletyn Informacji Publicznej (BIP)` → `https://ugstromiec.naszbip.pl/zlobek` with `target="_blank" rel="noopener noreferrer"` and a visually-hidden suffix `(otwiera się w nowej karcie)` (RESEARCH Pitfall 14 + external-link safety); `Deklaracja dostępności` → `/deklaracja-dostepnosci` (the stub route is created in Plan 04, so this link resolves); `Kontakt` → `/kontakt`; all links `brand-blue` + underline, 44px targets. Do NOT duplicate or rebuild BIP content. Finalize `src/routes/+layout.svelte` into the semantic landmark shell (RESEARCH Pattern 3): render `<SkipLink />` first, then `<Header />`, then `<main id="main"><slot /></main>`, then `<Footer />`; import `../app.css` if not already; ensure exactly one `<header>`/`<nav>`/`<main>`/`<footer>` in the rendered tree. Create `tests/nav.spec.ts` (Playwright): assert the five header links exist with correct hrefs; assert the footer BIP link has the exact label, `href="https://ugstromiec.naszbip.pl/zlobek"`, and `rel` containing `noopener` + `noreferrer`; assert `Deklaracja dostępności` and `Kontakt` links; and at a mobile viewport (<768px) assert the hamburger opens the `role="dialog"` drawer, `Escape` closes it, and focus returns to the hamburger.
  </action>
  <acceptance_criteria>
    - `Footer.svelte` contains the exact BIP label, the URL `https://ugstromiec.naszbip.pl/zlobek`, `rel="noopener noreferrer"`, and the suffix `(otwiera się w nowej karcie)`
    - `+layout.svelte` renders SkipLink → Header → `<main id="main">` → Footer, with exactly one of each landmark
    - `npx playwright test tests/nav.spec.ts` passes (header links, footer/BIP, drawer keyboard behavior)
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run check && npx playwright test tests/nav.spec.ts && grep -q "https://ugstromiec.naszbip.pl/zlobek" src/lib/components/Footer.svelte && grep -q 'id="main"' src/routes/+layout.svelte</automated>
  </verify>
  <done>Footer with correct BIP + section links, semantic landmark shell finalized, and nav E2E test green — a visitor can navigate all five sections on desktop and mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → external site (BIP) | User follows an outbound `target="_blank"` link to naszbip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | Footer BIP external link (`target="_blank"`) | low | mitigate | `rel="noopener noreferrer"` on the BIP link (and any external link) prevents reverse tab-nabbing; verified in tests/nav.spec.ts. |
| T-01-05 | Denial of Service (client) | Mobile drawer focus trap / scroll lock | low | mitigate | Built from native elements with a bounded focus trap and explicit scroll-lock release on close/unmount; ESC and scrim always close. No high-severity exposure. |

Block threshold: high. No high-severity threats in this plan.
</threat_model>

<verification>
- `npm run check` passes
- `npx playwright test tests/nav.spec.ts` passes
- Header exposes all five section links with Polish labels + accessible active state
- Mobile drawer is keyboard-operable (focus trap, ESC, restore focus, scroll lock)
- Footer BIP link is correct (label, URL, rel, new-tab suffix); Deklaracja + Kontakt present
- Exactly one header/nav/main/footer landmark; skip link first
</verification>

<success_criteria>
A visitor can reach every one of the five sections from a persistent header on desktop and from a keyboard-operable mobile drawer, and the footer links out to BIP (safely), Deklaracja dostępności, and Kontakt — all in Polish, on the AA landmark shell.
</success_criteria>

<output>
Create `.planning/phases/01-live-homepage-design-foundation/01-02-SUMMARY.md` when done.
</output>
