---
phase: 05-gallery-fees
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/kwoty.unit.ts
  - tests/cennik-reader.unit.ts
  - tests/cennik.spec.ts
  - tests/responsive.spec.ts
  - src/lib/kwoty.ts
  - src/lib/cennik.ts
  - src/lib/content/cennik.json
  - src/lib/content/cennik.ts
  - src/routes/cennik/+page.svelte
  - src/lib/content/rekrutacja.ts
autonomous: true
requirements: [FEES-01]

must_haves:
  truths:
    - "A parent can open /cennik and read the monthly fee, why it is that amount, what ZUS covers, wyżywienie, nieobecność and how to pay (FEES-01, D-05, D-06, D-30)."
    - "The page cannot contradict its own arithmetic: the payable amount is computed from the two stored numbers, never stored (D-28)."
    - "A zero amount appears on /cennik only inside the block that also carries the ZUS condition, and the page with that block removed still satisfies the boundary-anchored no-zero regex (D-07, D-31)."
    - "With obnizka === 0 the whole breakdown block disappears rather than rendering an unconditioned zero (D-29)."
    - "The formatter groups thousands from four digits using an ASCII space, so the shipped '1 500 zł' bytes are unchanged (D-35)."
    - "OPLATY is a typed read of the store and no longer a hand-typed literal, and /rekrutacja's existing .fee-box no-zero gate still passes (D-01, D-03, D-09)."
    - "GALLERY-02 and FEES-01 are closable in this phase; D-10's deferral is reversed (D-10)."
  artifacts:
    - "src/lib/kwoty.ts exporting grupujTysiace and zlote"
    - "src/lib/content/cennik.json (hand-authored, tab-indented, trailing newline)"
    - "src/lib/cennik.ts exporting cennikZWpisu and CENNIK"
    - "src/lib/content/cennik.ts (page prose restating no amount, D-04)"
    - "src/routes/cennik/+page.svelte"
    - "tests/kwoty.unit.ts, tests/cennik-reader.unit.ts, tests/cennik.spec.ts"
  key_links:
    - "src/lib/content/cennik.json -> src/lib/cennik.ts CENNIK -> /cennik page AND OPLATY in rekrutacja.ts (one store, three surfaces)"
    - "src/lib/kwoty.ts zlote -> every rendered złoty figure on /cennik and inside OPLATY.kwota"
    - "the ZUS block id -> the scoped conditional-zero test pair in tests/cennik.spec.ts"
---

<objective>
Ship `/cennik`: the fees page a parent can read, composed from a store that the panel will
own in plan 05-04.

Purpose: FEES-01's visitor half, and the store every other fee surface in this phase reads.
`src/lib/kwoty.ts` is also the prerequisite the homepage fee tile needs in plan 05-09.

Output: the formatter, the store, the reader, the prose module, the page and its three test
files.

## Slice capability (MVP)

ROADMAP Phase 5 success criterion 2, verbatim: *"A visitor can read the fees page (opłaty,
stawki)."*

After task 2 a parent can open `/cennik` and read every fee. After task 3 the same numbers
render inside the existing `FeeBox` on `/rekrutacja` from the same source, so the two pages
cannot disagree. Nothing in this plan is a horizontal layer: the store, the reader and the
page land together in one task because a store without a page is not a capability.

> **Note for the reviewer.** The ROADMAP `**Goal:**` line for Phase 5 is not written in
> `As a / I want to / so that` form, so no user story is invented here. The slice capability
> above is quoted from the phase's own success criteria.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gallery-fees/05-CONTEXT.md
@.planning/phases/05-gallery-fees/05-UI-SPEC.md
@.planning/phases/05-gallery-fees/05-RESEARCH.md
@.planning/phases/05-gallery-fees/05-PATTERNS.md
@.planning/phases/05-gallery-fees/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0, write the three failing fee test files and extend the viewport sweep</name>
  <files>tests/kwoty.unit.ts, tests/cennik-reader.unit.ts, tests/cennik.spec.ts, tests/responsive.spec.ts</files>

  <read_first>
    - `tests/rekrutacja.spec.ts` — the whole file. `:167-184` is the existing `.fee-box`
      gate: `:183` is `expect(tresc).not.toMatch(/(^|[^0-9])0(,00)?\s*zł/)` scoped to
      `.fee-box`, and `:178` asserts only that the substring `ZUS` occurs somewhere in the
      same element. This is the file's STRUCTURE to reuse and the assertion NOT to copy.
    - `tests/dokumenty.spec.ts` — a public-route spec with `AxeBuilder`, SEO and `noindex`
      assertions; the smallest correct shape for `tests/cennik.spec.ts`'s opening tests.
    - `tests/aktualnosci-reader.unit.ts` (`:1-38` in particular) — the reader-resilience unit
      idiom: the header comment explaining why the compile-time type is a deliberate lie, and
      the key-set equality assertion.
    - `tests/admin-walidacja-nabor.unit.ts:160-190` — the byte-for-byte discipline (read from
      disk, never retype; drive from the file's current state).
    - `tests/liczebniki.unit.ts` — the smallest pure-function unit suite in the repository.
    - `tests/responsive.spec.ts` — `ROUTES` at `:32` and the `VIEWPORTS` object above it.
    - `.planning/phases/05-gallery-fees/05-RESEARCH.md` §Code Examples — the three worked
      test bodies (`kwoty.unit.ts`, the rendered-arithmetic assertion, the scoped
      conditional-zero pair). Treat them as the specification.
    - `.planning/phases/05-gallery-fees/05-VALIDATION.md` §Per-Task Verification Map rows
      FEE-1 to FEE-6 and §Wave 0 Requirements.
  </read_first>

  <behavior>
    - `grupujTysiace(0)` is `0`; `(1)` is `1`; `(999)` is `999`; `(1000)` is `1 000`;
      `(1500)` is `1 500`; `(2337)` is `2 337`; `(9999)` is `9 999`; `(10000)` is `10 000`.
      Those seven cases straddle the exact four-digit band where CLDR `pl` suppresses
      grouping, which is the whole reason the function exists.
    - `zlote(1500).codePointAt(1)` is `0x20` and is NOT `0x00a0`. This is the assertion that
      catches the NBSP regression `useGrouping: 'always'` would introduce, which is invisible
      in a diff and in a failure message.
    - `OPLATY.kwota` equals `1 500 zł miesięcznie` byte for byte, and its separator codepoint
      is `0x20`.
    - `cennikZWpisu` returns `null` and warns for: a non-object container, `null`, an array,
      a bare string, a missing required string field, a blank required string field, a
      non-integer `stawka`, a negative `obnizka`, and `obnizka >= stawka`.
    - `cennikZWpisu` sets `pokazRozbicie` to `false` when `obnizka` is `0`, and to `true`
      when `obnizka` is a positive number below `stawka`.
    - The returned view object exposes exactly one fixed key set, asserted as a sorted
      equality, so a later object spread of the raw entry turns the suite red.
    - `/cennik` responds 200, has exactly one `h1`, carries a Polish title and description
      and a `noindex` robots meta, and produces zero axe violations at `wcag2a`, `wcag2aa`,
      `wcag21a`, `wcag21aa`.
    - The three rendered breakdown values satisfy `placi === stawka - obnizka`, with
      `obnizka >= 0` and `obnizka < stawka`, read out of the DOM so an ordinary editor save
      can never turn the assertion red.
    - Every occurrence of a zero amount in `/cennik`'s rendered `main` text falls inside the
      ZUS block, and the same text with that block removed still satisfies
      `/(^|[^0-9])0(,00)?\s*zł/` being absent.
    - `/rekrutacja`'s existing `.fee-box` gate is untouched and still passes.
  </behavior>

  <action>
Create three new test files and extend one.

**`tests/kwoty.unit.ts`** (`node:test` + `node:assert/strict`, relative `.ts` imports
because bare `node --test` does not resolve `$lib`). Import `grupujTysiace` and `zlote` from
`../src/lib/kwoty.ts` and `OPLATY` from `../src/lib/content/rekrutacja.ts`. Cover the eight
grouping cases, the two codepoint assertions and the `OPLATY.kwota` byte pin listed in
`<behavior>`. Open the file with a header comment recording the measured evidence: plain
`Intl.NumberFormat('pl-PL').format(1500)` returns the unseparated four-digit string on this
runtime, and `useGrouping: 'always'` emits U+00A0 where the shipped byte is U+0020, so
neither is usable.

**`tests/cennik-reader.unit.ts`** on the `tests/aktualnosci-reader.unit.ts` model. Import
`cennikZWpisu` from `../src/lib/cennik.ts`. Declare the expected key set as a pre-sorted
array constant and assert sorted equality against `Object.keys` of a valid result. Drive
every malformed shape in `<behavior>` and assert `null`. Note in the header that the
compile-time type is deliberately bypassed because these objects simulate hand-edited
on-disk JSON.

**`tests/cennik.spec.ts`** (Playwright + `AxeBuilder`), structured like
`tests/dokumenty.spec.ts`. Include the route/heading/SEO/axe tests, the rendered-arithmetic
test and the scoped conditional-zero pair. The arithmetic test locates the breakdown rows
through the selector `.rozbicie dl > div`, reads each `dd`, strips everything except digits
and a leading minus, and does the subtraction in the test. The conditional-zero pair locates
the ZUS block through the id `zus-blok`, asserts that block's text matches both the benefit
name and the anchored zero regex, then clones `main`, removes `#zus-blok` from the clone and
asserts the remaining text does NOT match the anchored regex. Declare the regex once as a
file-level constant.

**`tests/responsive.spec.ts`**: add `'/cennik'` to the `ROUTES` tuple at `:32`. Do not touch
anything else in that file; the nav breakpoint assertions belong to plan 05-03.

All four files must fail for the right reason before task 2 begins: the unit suites fail on
a missing module, the spec fails on a 404. Record that state in the SUMMARY.

Copy rules apply to test names and comments: Polish test names, no emoji, no em dashes.
  </action>

  <verify>
    <automated>node --test tests/kwoty.unit.ts 2>&1 | grep -q 'Cannot find module\|ERR_MODULE_NOT_FOUND' && node --test tests/cennik-reader.unit.ts 2>&1 | grep -q 'Cannot find module\|ERR_MODULE_NOT_FOUND' && grep -q "'/cennik'" tests/responsive.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `tests/kwoty.unit.ts`, `tests/cennik-reader.unit.ts` and `tests/cennik.spec.ts` all
      exist.
    - `node --test tests/kwoty.unit.ts` and `node --test tests/cennik-reader.unit.ts` both
      fail with a module-resolution error naming `src/lib/kwoty.ts` and `src/lib/cennik.ts`
      respectively (proof the tests are red for the intended reason, not silently empty).
    - `grep -c "codePointAt" tests/kwoty.unit.ts` is at least `2`.
    - `grep -c "0x00a0\|0x00A0" tests/kwoty.unit.ts` is at least `1` (the NBSP negative
      assertion is present).
    - `tests/kwoty.unit.ts` asserts all eight grouping cases named in `<behavior>`.
    - `tests/cennik-reader.unit.ts` contains a sorted key-set equality assertion.
    - `grep -c "zus-blok" tests/cennik.spec.ts` is at least `2` (the block is asserted and
      then removed from a clone).
    - `tests/cennik.spec.ts` contains no assertion that pins a specific złoty amount as a
      literal; every amount it reasons about is read from the DOM.
    - `tests/responsive.spec.ts` `ROUTES` contains `'/cennik'` and the file is otherwise
      byte-unchanged apart from that one entry.
    - `npm run lint` passes (prettier formats the new test files cleanly).
  </acceptance_criteria>

  <done>
    Four test files describe the fee slice's behaviour and are red for the right reason.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The formatter, the fee store, the reader and the /cennik page</name>
  <files>src/lib/kwoty.ts, src/lib/content/cennik.json, src/lib/cennik.ts, src/lib/content/cennik.ts, src/routes/cennik/+page.svelte</files>

  <read_first>
    - `src/lib/liczebniki.ts` — the whole file (~40 lines). `:1-17` is the header idiom every
      new `$lib` utility owes: it states why the module is not under `$lib/server` (the panel
      runs in the Cloudflare Worker) and why the `Intl` API for its job is rejected.
      `:31-37` is the guard-the-shape idiom for editor-saved numbers.
    - `src/lib/server/aktualnosci.ts` `postFromEntry` and `readString` (`:75-158`) — the
      matured reader precedent: entry typed `unknown`, container guarded before any property
      access, result CONSTRUCTED key by key and never spread, malformed entry warns and
      returns null.
    - `src/routes/rekrutacja/+page.svelte:1-60` — the public-route module-header idiom
      (requirements, UI-SPEC amendment, island count, and the paragraph stating that every
      fact is interpolated and no amount is written as a literal), plus the `Seo` usage and
      the `page-head` markup.
    - `src/routes/dokumenty/+page.svelte` — the `<section aria-labelledby>` per-section
      pattern and the `dl > div > dt/dd` shape used by `/o-nas`'s headcount.
    - `src/lib/components/FeeBox.svelte` — the tint-yellow / 2px accent / radius-md / 46rem
      treatment to reuse verbatim, and its header comment stating the hard rule that the ZUS
      condition is rendered in the SAME block as the amount.
    - `src/lib/content/o-nas.json` — the byte shape every hand-authored seed must match: tab
      indentation, trailing newline, `placeholder` as the first key.
    - `src/lib/content/site.ts:1-11` — the `with { type: 'json' }` import attribute and why
      it is load-bearing under bare `node --test`.
    - `.planning/phases/05-gallery-fees/05-UI-SPEC.md` Contracts 3, 4 and 5 and the
      `/cennik` block of the Copywriting Contract (every visitor-facing string, verbatim).
    - `.planning/dane-bip-zlobek-stromiec.md:202-204` §"10. Czego NIE publikować bez
      potwierdzenia", item 1 — the conditional-zero rule. Cite it as "§10, item 1"; the
      string "10.1" does not occur in that file.
    - `.planning/phases/05-gallery-fees/05-RESEARCH.md` §Code Examples (the hand-rolled
      grouping) and §Architecture Patterns Pattern 4 (per-store degradation).
  </read_first>

  <action>
Four new source files and one new route.

**`src/lib/kwoty.ts`.** In `$lib`, never `$lib/server`, because both the prerendered page
and the panel (a Cloudflare Worker) import it. Open with the `liczebniki.ts` header idiom:
state the boundary, then state that `Intl.NumberFormat` is rejected with the measured
evidence (the plain call returns the four-digit string unseparated because CLDR `pl` sets
`minimumGroupingDigits` to 2, and pinning grouping on emits U+00A0 where the shipped
separator byte is U+0020). Export `grupujTysiace(kwota: number): string` doing hand-rolled
three-digit-from-the-right grouping with a module-private ASCII-space separator constant, and
`zlote(kwota: number): string` returning the grouped value followed by a space and the
currency word. Guard the shape as `liczebniki.ts` does: a non-integer or a negative input
must not produce a confident lie. Pure: no I/O, no clock, no framework import.

**`src/lib/content/cennik.json`.** Hand-author it tab-indented with a trailing newline, in
exactly this key order, which is also the order the panel validator will emit in plan 05-04:
`placeholder`, `stawka`, `obnizka`, `naglowek`, `kwotaOpis`, `zus`, `wyzywienie`,
`nieobecnosc`. Seed values: `placeholder` true; `stawka` 2337 and `obnizka` 837, both whole
złoty with no grosze and no separators (D-28: the uchwała quotes grosze and the store drops
them rather than widening `liczbaWZakresie`, which accepts at most four digits); and the four
strings taken verbatim from the current `OPLATY` in `src/lib/content/rekrutacja.ts`
(`naglowek`, `kwotaOpis`, `zus`, `wyzywienie`, `nieobecnosc`), read out of that file rather
than retyped. `src/lib/content/` is deliberately not in `.prettierignore`, so a
space-indented seed would block every local commit on the panel's first save.

**`src/lib/cennik.ts`.** In `$lib`, never `$lib/server`: plan 05-04's panel screen echoes the
formatted amount back to the editor. Import the JSON with the `with { type: 'json' }`
attribute and a relative path. Export:
- an interface for the stored shape and an interface for the view the page renders;
- `cennikZWpisu(wpis: unknown)` returning the view or `null`, built on the `postFromEntry`
  discipline exactly: guard the container before any property access, narrow every string
  through a single `readString`-style primitive, construct the result key by key and never
  spread, warn and return `null` on any refusal;
- the computed members: the payable amount as `stawka - obnizka`, its formatted string
  through `zlote`, the full prose form used by `OPLATY.kwota` (the formatted amount followed
  by the period word), the three formatted breakdown values, and a boolean saying whether the
  breakdown block renders;
- `CENNIK`, the module-level view built from the imported JSON.

Refusal rules, all inside `cennikZWpisu`: `stawka` and `obnizka` must be non-negative safe
integers; `obnizka` must be strictly less than `stawka`, because the boundary-anchored
zero regex does not match a negative amount and a build-time reader is the last place that
mistake can be caught; every required string must be non-blank. The breakdown boolean is
false when `obnizka` is zero, because rendering a zero reduction would be exactly the
unconditioned zero figure `dane-bip` §10 item 1 forbids. When `cennikZWpisu` returns `null`
for the committed store, the module throws at module scope so `vite build` fails: `/cennik`
has no empty state and a missing required field is a build failure by design.

**`src/lib/content/cennik.ts`.** The page's own prose, and it restates NO amount (D-04).
Holds: the page lead, the eight section headings, the breakdown `h3` and its three labels,
the ZUS "how to apply" line, the wyżywienie detail line, the "Jak i kiedy płacić" body and
the "Podstawa prawna" body and link label, all verbatim from `05-UI-SPEC` §Copywriting
Contract. The worked-example sentence lives here as a function taking the formatted amount,
so the example's amounts are interpolated from the store and can never drift from the block
above them. Mark the "Jak i kiedy płacić" body with a `// PLACEHOLDER:` line comment naming
what is unconfirmed (payment method, deadline, consequence of late payment), and the
wyżywienie detail line likewise, so the Phase 6 sweep finds them. Add a header comment
carrying the module's own hard rules: nothing here states a złoty figure; the page must not
state the ZUS benefit's own złoty amount because the banked figure is struck and no confirmed
source replaces it, so the copy says the benefit may be granted "w maksymalnej wysokości".

**`src/routes/cennik/+page.svelte`.** Prerendered and zero-island; `prerender = true` is
inherited from `src/routes/+layout.ts` and is NOT restated. No `+page.server.ts`: the reader
is a plain `$lib` import, and an empty load would be noise. Open with the module-header idiom
of `rekrutacja/+page.svelte`, including the paragraph stating that every złoty figure is
interpolated from the cennik store and none is written as a literal in this file, head
metadata included. Compose the eight sections of `05-UI-SPEC` Contract 3 in order, each a
`<section aria-labelledby>` pointing at its own `h2`, with the surface alternation the
contract tabulates. Reuse `Seo`, `Cta`, `Wave`, `SkipLink` (the layout owns `Header`,
`Footer` and `<main>`; add no second landmark and no second `h1`).

Two structural details the tests depend on and that must not drift:
- the breakdown wrapper carries the class `rozbicie` and its rows are `dl > div > dt/dd`,
  never a `<table>` and never a bare `<dl>`, because a table splits an amount from its
  condition across cells and at mobile width the two land on different visual rows;
- the ZUS block carries `id="zus-blok"` and contains, in DOM order, the stored ZUS sentence,
  the worked example as ONE paragraph, and the "jak złożyć wniosek" line. It is the only
  block on the page that may render a zero amount, and the zero and its condition are in the
  same paragraph.

The fee block reuses the `FeeBox` treatment values (tint-yellow surface, 2px accent border,
radius-md, 16px to 24px padding, 46rem max width) without importing `FeeBox` itself, which
takes no props by design. The payable amount is Baloo 700 at 20px with `white-space: nowrap`,
never a larger size, so `/cennik` and `FeeBox` do not disagree about the same amount's
importance. Every section from 2 to 7 uses the v1.6 §2 editorial split at >=1024px. No new
`@theme` token, no new colour, no new breakpoint. The breakdown block renders only when the
reader's boolean is true.

Do not add `/cennik` to `svelte.config.js` or remove it from `KNOWN_FUTURE_ROUTES` here; that
is plan 05-03's job and its acceptance gate.
  </action>

  <verify>
    <automated>npm run check && node --test tests/kwoty.unit.ts && node --test tests/cennik-reader.unit.ts && npx playwright test tests/cennik.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npm run check` passes with zero errors and zero warnings.
    - `node --test tests/kwoty.unit.ts` passes, including the two codepoint assertions.
    - `node --test tests/cennik-reader.unit.ts` passes, including the sorted key-set equality
      and every refusal case.
    - `npx playwright test tests/cennik.spec.ts` passes, including the axe scan, the rendered
      arithmetic and the scoped conditional-zero pair.
    - `npx playwright test tests/responsive.spec.ts` passes with `/cennik` in `ROUTES`.
    - `src/lib/content/cennik.json` is tab-indented with a trailing newline and its keys are
      in the order `placeholder`, `stawka`, `obnizka`, `naglowek`, `kwotaOpis`, `zus`,
      `wyzywienie`, `nieobecnosc`. Verified by `npm run lint` passing and by
      `head -c 3 src/lib/content/cennik.json | od -c` showing a tab after the opening brace
      and newline.
    - `grep -c 'Intl' src/lib/kwoty.ts` is at least `1` and every occurrence is inside the
      header comment explaining the rejection; `grep -c 'new Intl' src/lib/kwoty.ts` is `0`.
    - `grep -c 'lib/server' src/lib/kwoty.ts src/lib/cennik.ts` is `0` (neither module
      reaches across the boundary).
    - `grep -c '<table' src/routes/cennik/+page.svelte` is `0`.
    - `grep -c 'zus-blok' src/routes/cennik/+page.svelte` is at least `1`, and
      `grep -c 'rozbicie' src/routes/cennik/+page.svelte` is at least `1`.
    - `grep -c 'prerender' src/routes/cennik/+page.svelte` is `0` (the flag is inherited and
      is not restated).
    - `grep -rc 'PLACEHOLDER' src/lib/content/cennik.ts` is at least `2`.
    - `git diff --name-only -- package.json package-lock.json` is empty.
  </acceptance_criteria>

  <done>
    Visiting `/cennik` in the preview build shows the fee, the breakdown, the ZUS block with
    its worked example, wyżywienie, nieobecność, the payment placeholder section, the legal
    basis and the closing CTA, all in Polish, all axe-clean, with the payable amount computed
    from the two stored numbers.
  </done>
</task>

<task type="auto">
  <name>Task 3: OPLATY becomes a typed read of the store, and rekrutacja.ts becomes loadable under bare node</name>
  <files>src/lib/content/rekrutacja.ts</files>

  <read_first>
    - `src/lib/content/rekrutacja.ts` — the whole file. `:1-23` is the module header, whose
      D-18 paragraph declares the module code-authored only and coupled to no editorial
      screen; that claim stops being true here and must be rewritten in the same commit.
      `:23` is the `$lib/content/site` import that creates both the cycle risk and the bare
      `node --test` resolution failure. `:110-128` is the `/cennik` reservation comment and
      `OPLATY` with its six keys.
    - `src/lib/components/FeeBox.svelte` — no props by design, five paragraphs from five
      keys, `.kwota` styled for one short string. It must keep working with no change.
    - `tests/rekrutacja.spec.ts:167-184` — the gate that must stay green (FEE-6).
    - `src/lib/server/admin/walidacja/nabor.ts:13-15` — the relative `.ts` import convention
      and the sentence explaining it: the unit suites load these modules under bare
      `node --test`, where `$lib` does not resolve.
    - `src/lib/server/admin/obraz.ts:36` — a live example of an explicit `.ts` extension in a
      relative import that both Vite and `svelte-check` accept.
    - `.planning/phases/05-gallery-fees/05-CONTEXT.md` D-01, D-03, D-09, D-10.
  </read_first>

  <action>
Three edits to `src/lib/content/rekrutacja.ts`, all in one commit.

1. **Rewrite the module header.** The D-18 paragraph declaring the module code-authored only
   is now false for the fee block and must say so: the prose, dates and criteria below stay
   developer-authored, but the fee amounts and the five fee strings are a typed read of
   `src/lib/content/cennik.json`, which the editorial panel writes from `/admin/cennik`
   (plan 05-04). Keep every other standing rule in that header, including the ban on
   duplicating address, room and office-hours values.

2. **Change the two imports to relative paths with explicit `.ts` extensions.** Replace the
   `$lib` alias import of `site` with a relative `./site.ts` import, and add a relative
   `../cennik.ts` import of `CENNIK`. This is not cosmetic: it is what makes
   `tests/kwoty.unit.ts` able to import `OPLATY` under bare `node --test`, which is the only
   place the byte pin can run. It also documents the rule already recorded at
   `walidacja/nabor.ts:13-15`. Add one sentence of comment saying so.

3. **`OPLATY` becomes a typed read.** Keep the exported symbol name, the six keys and the
   `as const`-equivalent shape its consumers rely on (`FeeBox.svelte`,
   `rekrutacja/+page.svelte`, `tests/rekrutacja.spec.ts`). Every value now comes from
   `CENNIK`: `naglowek`, `kwotaOpis`, `zus`, `wyzywienie` and `nieobecnosc` pass through,
   and `kwota` is the store's composed prose form. Resolve the `/cennik` reservation comment
   at `:116-117`: `/cennik` now exists and the full breakdown lives there.

   Keep the existing HARD RULE comment above `OPLATY` naming `dane-bip` §10 item 1 (correct
   the citation form: the string "10.1" does not occur in that file, so cite it as
   "§10, item 1"). State in that comment that the rule is now enforced twice, at save time by
   the `/admin/cennik` validator and at render time by the scoped test pair on `/cennik`.

**Cycle check, which is the point of D-03.** `rekrutacja.ts` imports `site.ts`, so routing a
homepage tile through `OPLATY` would close a cycle. Nothing in this task does that, and plan
05-09 reads `CENNIK` directly for the fee tile. Add a one-line comment above the `CENNIK`
import recording that constraint so a later edit does not undo it.

`FeeBox.svelte` is NOT edited: it takes no props by design and reads `OPLATY`, which keeps
its shape. `tests/rekrutacja.spec.ts` is NOT edited: it must pass unchanged, which is FEE-6.
  </action>

  <verify>
    <automated>npm run check && npm run lint && node --test tests/kwoty.unit.ts && npx playwright test tests/rekrutacja.spec.ts tests/cennik.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npm run check` passes with zero errors.
    - `node --test tests/kwoty.unit.ts` passes, which proves `rekrutacja.ts` and its whole
      import graph load under bare `node --test` and that `OPLATY.kwota` is byte-identical to
      the historical shipped string.
    - `npx playwright test tests/rekrutacja.spec.ts` passes with the file unchanged
      (`git diff --name-only tests/rekrutacja.spec.ts` is empty). This is FEE-6.
    - `grep -c "\$lib" src/lib/content/rekrutacja.ts` is `0`.
    - `grep -c "cennik" src/lib/content/rekrutacja.ts` is at least `2` (the import and the
      resolved reservation comment).
    - `git diff --name-only src/lib/components/FeeBox.svelte` is empty.
    - The rendered `.fee-box` on `/rekrutacja` shows the same five strings as before the
      change; confirmed by `tests/rekrutacja.spec.ts` passing, which asserts them.
    - `npm run test:unit` passes in full (run by hand: nothing automated runs this tier).
    - No emoji and no em dash in any line added to `rekrutacja.ts`.
  </acceptance_criteria>

  <done>
    One store feeds `/cennik`, `OPLATY` and `FeeBox`. Changing a number in
    `src/lib/content/cennik.json` changes every fee surface at once, and the module header no
    longer claims otherwise.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor-saved JSON -> build-time reader | `src/lib/content/cennik.json` becomes panel-writable in plan 05-04. From this plan on it is untrusted input even though the person who wrote it is authenticated. |
| build-time reader -> prerendered public HTML | Whatever survives the reader is published to parents and cannot be recalled without a two-minute rebuild. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Information disclosure (regulatory, `dane-bip` §10 item 1) | `/cennik` rendered text | high | mitigate | The zero amount exists only inside `#zus-blok` together with its condition, in one paragraph, and the scoped test pair proves both halves. `/rekrutacja`'s `.fee-box` regex is deliberately not reused: it forbids ANY zero and `/cennik` renders one on purpose. |
| T-05-02-02 | Tampering (arithmetic integrity) | `cennikZWpisu` breakdown | high | mitigate | Two numbers stored, the third computed; `obnizka >= stawka` is refused by the reader, so a negative payable amount is inexpressible in the rendered output. The anchored zero regex does not match a negative figure, which is why the reader and not the test is the last catch. |
| T-05-02-03 | Denial of service (availability of the prerendered site) | module-scope throw in `src/lib/cennik.ts` | medium | accept | A malformed committed store fails `vite build` rather than publishing a blank fee page. Accepted deliberately: `/cennik` has no honest empty state, and a failed build leaves the previous deployment live. The panel validator in plan 05-04 is the guard that stops an editor reaching this state. |
| T-05-02-04 | Tampering (stored XSS through editor prose) | the five fee strings | medium | mitigate | The `/cennik` page renders every stored fee string as TEXT, never through `{@html}`. Markdown rendering is not introduced on this surface. CSP `script-src 'self'` is the second layer, not the only one. |
| T-05-02-05 | Spoofing (silent formatting drift) | `src/lib/kwoty.ts` separator | medium | mitigate | ASCII space pinned by codepoint in `tests/kwoty.unit.ts`, with the pre-existing E4 twin at `tests/home.spec.ts:112` which retypes the shipped string and runs inside `npm run test`. |
| T-05-02-SC | Tampering (supply chain) | npm installs | high | mitigate | Zero packages installed. `05-RESEARCH.md` §Package Legitimacy Audit records the phase's net supply-chain movement as nil. `git diff --name-only -- package.json package-lock.json` must be empty; a non-empty diff stops the plan. |
</threat_model>

<verification>
- Full wave gate: `npm run check && npm run lint && npm run test:unit && npm run test`.
- `npm run test:unit` must be typed by hand at each task commit. Nothing automated runs the
  `tests/*.unit.ts` tier (AG-3); pre-commit is `check` plus `lint` only and there is no CI.
  This plan adds two suites to that unrun tier and both have a second check: `kwoty.unit.ts`
  is twinned by the retyped assertion at `tests/home.spec.ts:112`, and `cennik-reader.unit.ts`
  is twinned by `npm run check` plus the build (a reader that returns `null` for the committed
  store fails `vite build`).
- `npm run build` must succeed; the prerender of `/cennik` is the E3 evidence that the reader
  never produces an undefined access.
</verification>

<success_criteria>
- `/cennik` renders in the preview build with all eight sections and zero axe violations.
- FEE-1, FEE-2, FEE-3, FEE-4, FEE-5 and FEE-6 of `05-VALIDATION.md` are green.
- `OPLATY`, `FeeBox` on `/rekrutacja` and `/cennik` all read one store.
- `package.json` is unchanged.
</success_criteria>

## Artifacts this phase produces

Produced by THIS plan:

| Symbol / path | Kind | Notes |
|---|---|---|
| `src/lib/kwoty.ts` | new module (`$lib`) | exports `grupujTysiace(number): string`, `zlote(number): string`. ASCII-space separator, hand-rolled grouping, no `Intl` call |
| `src/lib/content/cennik.json` | new JSON store | keys in order: `placeholder`, `stawka`, `obnizka`, `naglowek`, `kwotaOpis`, `zus`, `wyzywienie`, `nieobecnosc`. Tab-indented, trailing newline. Panel-written from plan 05-04 |
| `src/lib/cennik.ts` | new module (`$lib`) | exports the stored-shape interface, the view interface, `cennikZWpisu(unknown)` returning the view or `null`, and `CENNIK` |
| `src/lib/content/cennik.ts` | new content module | `/cennik` prose that restates no amount; carries two `// PLACEHOLDER:` line comments |
| `src/routes/cennik/+page.svelte` | new public route | `/cennik`, prerendered, zero islands. Structural hooks: `id="zus-blok"`, class `rozbicie` |
| `OPLATY` (changed) | exported constant in `src/lib/content/rekrutacja.ts` | same six keys, now composed from `CENNIK` |
| `tests/kwoty.unit.ts` | new unit suite | FEE-5 |
| `tests/cennik-reader.unit.ts` | new unit suite | FEE-4 |
| `tests/cennik.spec.ts` | new Playwright spec | FEE-1, FEE-2, FEE-3 |

<output>
Create `.planning/phases/05-gallery-fees/05-02-SUMMARY.md` when done.
</output>
