---
phase: 05-gallery-fees
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md
  - .planning/phases/02-about-documents-cms/02-UI-SPEC.md
  - .planning/phases/05-gallery-fees/05-UI-SPEC.md
  - .planning/DESIGN-BANK.md
autonomous: true
requirements: [GALLERY-01, GALLERY-02, FEES-01]

must_haves:
  truths:
    - "A reader who opens 01-UI-SPEC.md at the Header contract learns the nav is six items, not five (D-16)."
    - "A reader who opens 01-UI-SPEC.md at the KeyFacts and Facts-register lines cannot mistake the stale draft fee figures for live copy (D-02)."
    - "A reader who opens 02-UI-SPEC.md at the /o-nas section contract learns that section 6 and the zero-JS claim are superseded (D-19, D-20, D-13)."
    - "A reader who opens DESIGN-BANK.md at the Cennik block learns every figure in it, including the one inside the ZUS explainer, is struck (D-02)."
    - "The phase's contract set states in writing that CMS-01, CMS-02 and CMS-03 are not ticked here (D-36) and that the phase is built against open risks by decision (D-37)."
  artifacts:
    - ".planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md carries a section headed '## Amendment v1.7'"
    - ".planning/phases/02-about-documents-cms/02-UI-SPEC.md carries a supersession pointer naming 05-UI-SPEC.md"
    - ".planning/DESIGN-BANK.md §Cennik carries a STRUCK marker"
    - ".planning/phases/05-gallery-fees/05-UI-SPEC.md carries the recorded checker sign-off"
  key_links:
    - "01-UI-SPEC.md:169 and :171 (Header contract, nav count and mobile breakpoint) -> Amendment v1.7 §1"
    - "01-UI-SPEC.md:261 (Copywriting Contract nav row) -> Amendment v1.7 §1"
    - "01-UI-SPEC.md:457 and :471 (stale fee figures) -> Amendment v1.7 §2, struck in place"
    - "02-UI-SPEC.md:103, :107, :109-115, :212, :272 -> 05-UI-SPEC Contracts 1, 2, 8"
---

<objective>
Write the contract amendments that authorize every change the rest of Phase 5 makes, and
strike the stale figures those contracts still publish. This plan touches no source code.

**Why it is first and alone in wave 1.** `05-CONTEXT.md` D-20 says the amendment to a
LOCKED contract is required **before** the change, not after. `05-RESEARCH.md` C-3 proves
why: two of the project's six amendments (v1.4 and v1.5) left no trace inside
`01-UI-SPEC.md` at all, and that traceability gap is exactly how the stale `400 zł` at
`:457` and `:471` survived four phases. Producing `05-UI-SPEC.md` did **not** amend
`01-UI-SPEC.md`; only a physical edit does.

Purpose: an executor told "01-UI-SPEC.md is LOCKED, follow it" must not be able to read a
superseded line and act on it.
Output: four edited planning documents, one grep-checkable amendment, two strike markers.

**Scope statements this plan records verbatim in the amendment text** (they are decisions,
not commentary):
- D-36: this phase does NOT tick CMS-01, CMS-02 or CMS-03. They close on the Phase 04.1 UAT.
- D-37: the phase is built against its open risks (04.1 UAT rows B2 and B4) by explicit
  user decision, because the input that would retire B2 (real żłobek photography) does not
  exist yet. Both are carried to the Phase 6 launch gate.
- D-08 is SUPERSEDED and needs no action: Sveltia and `static/admin/` were removed in
  plan 04.1-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gallery-fees/05-CONTEXT.md
@.planning/phases/05-gallery-fees/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Amendment v1.7 to 01-UI-SPEC.md and strike the stale fee figures in place</name>
  <files>.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md, .planning/phases/05-gallery-fees/05-UI-SPEC.md</files>

  <read_first>
    - `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md` — read `:165-175`
      (Header contract, the literal "Links (5, Polish)" and "Mobile (<`md`)"), `:255-265`
      (Copywriting Contract nav row), `:450-475` (Amendment v1.2 §6 KeyFacts v2 and §9 Facts
      register, which carry the stale figures), and `:490-548` (Amendment v1.6, the most
      recent in-file amendment and the exact shape to copy: `## Amendment vX.Y (date): title`
      then a blockquote naming what stays in force and what is superseded, then numbered
      `### n.` sections).
    - `.planning/phases/05-gallery-fees/05-UI-SPEC.md` §"Amendment v1.7: exactly what this
      document supersedes" — the supersession table this amendment points at, and
      §"Checker Sign-Off".
    - `.planning/phases/05-gallery-fees/05-RESEARCH.md` §C-3 — the two amendment idioms and
      why only a physical edit counts.
    - `.planning/phases/05-gallery-fees/05-CONTEXT.md` D-02, D-16, D-17, D-18, D-19, D-20,
      D-32, D-36, D-37.
  </read_first>

  <action>
Append one new section to the END of `01-UI-SPEC.md`, headed exactly
`## Amendment v1.7 (2026-08-17): Galeria i Cennik`. Follow the in-file shape of Amendment
v1.6 at `:496`: the heading, then a blockquote stating that the Phase 1 contract and
Amendments v1.1 through v1.6 stay in force in full except where this amendment supersedes
them, that the amendment introduces no new colour, font, weight, type size or breakpoint
token, and that the full component contracts live in
`.planning/phases/05-gallery-fees/05-UI-SPEC.md` (point at it, do not restate it). Then
numbered `### n.` sections:

1. **Public navigation v3: six items.** Supersedes `:169` "Links (5, Polish)" and `:261`
   Copywriting Contract nav row. Order: Aktualnosci, O nas, Rekrutacja, Cennik, Dokumenty,
   Kontakt. Cennik sits after Rekrutacja because cost is part of the enrolment decision
   (D-16). Galeria is not a nav destination (D-19). Also supersedes `:171`
   "Mobile (<`md`)": the desktop nav's breakpoint moves from 768px to **1024px**, and chip
   padding, gap, 14px/700 label, the 44px minimum target and the hover / `aria-current`
   treatment are all unchanged. State the reason: a sixth chip does not fit at 768px beside
   a 52px emblem and a two-line wordmark without breaking the locked chip geometry.
2. **Fee figures superseded and struck.** Supersedes `:457` (v1.2 §6, KeyFacts v2) and
   `:471` (v1.2 §9, Facts register). Both lines carried stale draft figures that were never
   struck. The live values come from the cennik store built in plan 05-02 and are named in
   `05-UI-SPEC` Contracts 3, 4 and 7 (D-02, D-03).
3. **Footer "Na skroty" repoints.** `01-UI-SPEC.md:462` enumerates that column by LABEL,
   not href, so the repoint is compatible with it. Galeria points at `/o-nas#galeria`,
   Dojazd points at `/kontakt#dojazd`, Cennik resolves (D-17, D-19).
   `KNOWN_FUTURE_ROUTES` in `svelte.config.js` is emptied across plans 05-03 and 05-07
   (D-18).
4. **KeyFacts v4: same visuals, stored values.** `:521` (Amendment v1.6 §3, KeyFacts v3)
   stays in force verbatim: `ul`/`li` semantics, the aria-label, four `.fact-label` nodes,
   the note on its own line, the 4/2/1 column grid and `repeat(4, 1fr)` at >=1024px. Only
   the SOURCE of the four strings changes, and only two of the four tiles become
   editor-writable (D-32, D-33).
5. **Phase 5 marking and risk posture.** State that this phase does not tick CMS-01, CMS-02
   or CMS-03 (D-36), that it proceeds against a formally open Phase 04.1 dependency by
   explicit user decision (D-37), and that the two named deferrals (the HEIC decode path,
   04.1 UAT row B2; the stale-save conflict panel, row B4) are carried to the Phase 6
   launch gate rather than dissolved.

Then edit `:457` and `:471` **in place**. Do not delete the figures: prefix each superseded
figure fragment with the literal marker `STRUCK v1.7:` and leave the original text visible
after it, so a reader who lands on the line sees both the old value and the fact that it is
dead. Add, on the same line, a pointer reading that the live value is defined by Amendment
v1.7 §2 and by `05-UI-SPEC.md` Contract 4. The acceptance gate is line-scoped: every line in
the file that still carries the old fee fragment must also carry `STRUCK v1.7:`.

Finally, record the checker sign-off inside `05-UI-SPEC.md`: tick all six
`## Checker Sign-Off` boxes and change `**Approval:** pending` to name the approval recorded
in the phase's planning context (gsd-ui-checker, 6/6, 2026-08-17), and set the frontmatter
`status:` from `draft` to `approved`. That file currently contradicts its own approval
record, and this phase treats it as LOCKED.

Copy rules apply to every string written here: no emoji, no em dashes, en dash only inside
numeric ranges.
  </action>

  <verify>
    <automated>test "$(grep -c '^## Amendment v1.7' .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md)" = "1" && test "$(grep -n '400 zł' .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md | grep -vc 'STRUCK v1.7:')" = "0" && test "$(grep -n '14 zł' .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md | grep -vc 'STRUCK v1.7:')" = "0" && test "$(grep -c 'Approval:\*\* pending' .planning/phases/05-gallery-fees/05-UI-SPEC.md)" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## Amendment v1.7' .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md` returns exactly `1`.
    - Every line of `01-UI-SPEC.md` matching `400 zł` also matches `STRUCK v1.7:` (the
      command `grep -n '400 zł' <file> | grep -vc 'STRUCK v1.7:'` returns `0`).
    - Every line of `01-UI-SPEC.md` matching `14 zł` also matches `STRUCK v1.7:`.
    - The amendment body contains all five numbered `### ` sections listed in the action, and
      names the decision IDs D-02, D-16, D-17, D-18, D-19, D-32, D-36 and D-37 at least once
      each.
    - The amendment blockquote names `.planning/phases/05-gallery-fees/05-UI-SPEC.md` as the
      document holding the full component contracts.
    - `grep -c '1024' .planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md`
      increases relative to the pre-edit file (the breakpoint change is stated numerically,
      not only in prose).
    - `.planning/phases/05-gallery-fees/05-UI-SPEC.md` frontmatter reads `status: approved`
      and its `Approval:` line no longer reads `pending`; all six Checker Sign-Off checkboxes
      are ticked.
    - No emoji and no em dash appears in any line added by this task.
  </acceptance_criteria>

  <done>
    A developer opening `01-UI-SPEC.md` at line 169, 171, 261, 457 or 471 cannot act on a
    superseded instruction without seeing that it is superseded, and can find the live
    contract from the file itself.
  </done>
</task>

<task type="auto">
  <name>Task 2: Point 02-UI-SPEC.md at its supersession and strike the DESIGN-BANK Cennik block</name>
  <files>.planning/phases/02-about-documents-cms/02-UI-SPEC.md, .planning/DESIGN-BANK.md</files>

  <read_first>
    - `.planning/phases/02-about-documents-cms/02-UI-SPEC.md` — read `:100-125` (the /o-nas
      zero-JS claim at `:103`, "Nasze miejsce" as primary visual anchor at `:107`, the
      seven-section order at `:109-115`, the D-04 consent rule and the D-09 placeholder rule
      at `:120`), `:205-215` (the facility grid at `:212`) and `:265-280` (the CMS copy
      contract `obiekt_zdjecia` label row at `:272`).
    - `.planning/DESIGN-BANK.md:25-40` — the §Cennik block at `:31-32` including the
      `do 400 zł miesięcznie` inside the dofinansowanie explainer, and the §Galeria block at
      `:35-37` whose consent rule is weaker than `02-UI-SPEC.md:115` D-04.
    - `.planning/phases/05-gallery-fees/05-UI-SPEC.md` §"Amendment v1.7: exactly what this
      document supersedes" and §"Consent, RODO and placeholder contract".
    - `.planning/phases/05-gallery-fees/05-CONTEXT.md` D-02, D-14, D-15, D-19, D-20.
  </read_first>

  <action>
Two independent edits.

**A. `02-UI-SPEC.md`.** The project's amendment procedure appends to `01-UI-SPEC.md`, but
`05-RESEARCH.md` C-3 shows that a pointer only works where the reader actually lands.
Append to the END of `02-UI-SPEC.md` a section headed exactly
`## Superseded by 05-UI-SPEC (Amendment v1.7, 2026-08-17)`, followed by a blockquote and a
table with one row per superseded line, naming the line, what it said, and what replaces it:

- `:103` "prerendered, zero-JS" for `/o-nas`: the page keeps `prerender = true` and gains
  exactly ONE hydrated island, the lightbox (D-13). The header comment at
  `src/routes/o-nas/+page.svelte:2-5` is rewritten in the same commit as the island, never
  left to be discovered.
- `:107` "Nasze miejsce" as the page's primary visual anchor: the gallery section is the
  anchor, under the heading `Galeria: nasze miejsce` (D-19, D-20).
- `:109-115, :212` section 6 and the facility grid: section 6 becomes the gallery section,
  `obiekt_opis` prose above a 1 / 2 / 3-column grid at base / 768px / 1024px, gap 24px
  (D-12). The seven-section ORDER and every other section are unchanged.
- `:272` the `obiekt_zdjecia` label row of the CMS copy contract: the photos leave
  `o-nas.json` for their own store and their own panel screen (D-26).
- `:115` D-04 (environment-only images, zero identifiable people, placeholder-flagged) is
  **NOT** superseded. State explicitly that it STANDS and that it overrides the weaker rule
  in `DESIGN-BANK.md:37`, which permits an identifiable child with documented consent
  (D-15). Loosening it is a future amendment made in the open at the Phase 6 gate, not a
  decision an executor may take while placing a photo.
- `:120` D-09 (a placeholder renders no visitor-facing badge) is NOT superseded and governs
  every placeholder this phase ships (D-14).

**B. `.planning/DESIGN-BANK.md`.** Edit the §Cennik block at `:31-32` in place. Prefix the
whole three-card figure sentence and the whole dofinansowanie explainer sentence with the
literal marker `STRUCK (05 D-02):` and leave the original text readable after the marker, so
the bank stays a record of what was drafted while being unusable as a source. Add one
sentence after the block stating that the live fee copy is defined by
`05-UI-SPEC.md` Contracts 3 and 4 and that the ZUS explainer's own złoty amount is
deliberately absent because no confirmed source replaces it, so `/cennik` says
"w maksymalnej wysokości" instead.

Do **not** strike the §Galeria block at `:35-37`: its nine slots stay usable as a shot list
(D-15). Add one sentence to that block recording that its consent rule is overridden by
`02-UI-SPEC.md:115` D-04 for the surface the gallery now renders on.

Copy rules apply: no emoji, no em dashes, en dash only inside numeric ranges.
  </action>

  <verify>
    <automated>test "$(grep -c 'STRUCK (05 D-02):' .planning/DESIGN-BANK.md)" = "2" && test "$(grep -c '^## Superseded by 05-UI-SPEC' .planning/phases/02-about-documents-cms/02-UI-SPEC.md)" = "1" && test "$(grep -n '400 zł' .planning/DESIGN-BANK.md | grep -vc 'STRUCK (05 D-02):')" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'STRUCK (05 D-02):' .planning/DESIGN-BANK.md` returns exactly `2` (the card
      figures sentence and the dofinansowanie explainer sentence).
    - `grep -n '400 zł' .planning/DESIGN-BANK.md | grep -vc 'STRUCK (05 D-02):'` returns `0`,
      which proves the explainer's own figure is inside the strike and not carved out.
    - `grep -c '14 zł' .planning/DESIGN-BANK.md | ...` — every remaining occurrence sits on a
      struck line (same line-scoped rule).
    - `grep -c '^## Superseded by 05-UI-SPEC' .planning/phases/02-about-documents-cms/02-UI-SPEC.md`
      returns exactly `1`.
    - That section names line references `:103`, `:107`, `:109-115`, `:212` and `:272` and
      states for each what replaces it.
    - That section states that `02-UI-SPEC.md:115` D-04 STANDS and overrides
      `DESIGN-BANK.md:37`, naming D-15.
    - The `.planning/DESIGN-BANK.md` §Galeria block is NOT struck and still lists the nine
      slots.
    - No emoji and no em dash appears in any line added by this task.
  </acceptance_criteria>

  <done>
    Neither `02-UI-SPEC.md` nor `DESIGN-BANK.md` can be read as authorizing a stale figure or
    a superseded layout, and the consent rule that governs is stated where the weaker one
    lives.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning document -> executor agent | A locked contract is read as an instruction by later agents. A stale instruction is an untrusted input to every downstream plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering (integrity of the contract set) | `01-UI-SPEC.md:457`, `:471` | high | mitigate | Physical in-place strike with the literal `STRUCK v1.7:` plus a line-scoped grep gate. The v1.4 / v1.5 precedent proves a pointer that lives only in another file is not a mitigation. |
| T-05-01-02 | Information disclosure (regulatory) | `DESIGN-BANK.md:32` dofinansowanie explainer | high | mitigate | The explainer's own `400 zł` is inside the strike, closing D-02's refuted carve-out. Copying it forward would republish a wrong figure to parents and understate a state benefit. |
| T-05-01-03 | Information disclosure (RODO / wizerunek) | consent rule conflict between `02-UI-SPEC.md:115` and `DESIGN-BANK.md:37` | high | mitigate | The supersession section states in writing that the harder rule (zero identifiable people) governs, and records that loosening it is a Phase 6 amendment made in the open. |
| T-05-01-04 | Repudiation | `05-UI-SPEC.md` approval state | low | mitigate | The recorded 6/6 sign-off is written into the file so the locked status is checkable rather than remembered. |
| T-05-05-SC | Tampering (supply chain) | npm installs | high | mitigate | This plan installs nothing and touches no `package.json`. `05-RESEARCH.md` §Package Legitimacy Audit records the phase-wide net supply-chain movement as nil; a plan producing a `package.json` diff has drifted from the contract and must be stopped. |
</threat_model>

<verification>
- `git diff --stat` shows changes confined to `.planning/` (no file under `src/`, `tests/`
  or the repository root is touched).
- `git diff -- package.json package-lock.json` is empty.
- `npm run lint` is unaffected: `.planning/` is listed in `.prettierignore`, so these edits
  cannot change the format gate in either direction. Run it once anyway to confirm the
  baseline is green before the code plans start.
</verification>

<success_criteria>
- The four grep gates in the task `<verify>` blocks all pass.
- A reader of `01-UI-SPEC.md`, `02-UI-SPEC.md` or `DESIGN-BANK.md` can reach
  `05-UI-SPEC.md` from the line they landed on.
- No source file, test file or dependency changed.
</success_criteria>

## Artifacts this phase produces

Produced by THIS plan (documents and markers only, no code symbols):

| Artifact | Path | Kind |
|---|---|---|
| `## Amendment v1.7 (2026-08-17): Galeria i Cennik` | `.planning/phases/01-live-homepage-design-foundation/01-UI-SPEC.md` | appended section, 5 numbered subsections |
| `STRUCK v1.7:` line marker | `01-UI-SPEC.md:457`, `:471` | in-place strike marker, grep-checkable |
| `## Superseded by 05-UI-SPEC (Amendment v1.7, 2026-08-17)` | `.planning/phases/02-about-documents-cms/02-UI-SPEC.md` | appended section with a per-line supersession table |
| `STRUCK (05 D-02):` line marker | `.planning/DESIGN-BANK.md:31`, `:32` | in-place strike marker, grep-checkable |
| Recorded checker sign-off | `.planning/phases/05-gallery-fees/05-UI-SPEC.md` | frontmatter `status: approved`, six ticked boxes |

No functions, components, routes, JSON store keys or exported constants are created by this
plan. The phase-wide artifact list is carried by the plans that create each symbol
(05-02 onward).

<output>
Create `.planning/phases/05-gallery-fees/05-01-SUMMARY.md` when done.
</output>
