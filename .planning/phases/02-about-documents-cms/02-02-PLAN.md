---
phase: 02-about-documents-cms
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - svelte.config.js
  - src/lib/content/dokumenty/rekrutacja-wniosek.json
  - src/lib/content/dokumenty/rekrutacja-regulamin.json
  - src/lib/content/dokumenty/statut-zlobka.json
  - static/dokumenty/wniosek-o-przyjecie-dziecka.doc
  - static/dokumenty/regulamin-rekrutacji.pdf
  - static/dokumenty/statut-zlobka.pdf
  - src/routes/dokumenty/+page.svelte
  - src/routes/dokumenty/+page.server.ts
  - src/lib/server/dokumenty.ts
  - tests/dokumenty.spec.ts
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "A visitor navigating to /dokumenty gets a 200 and sees documents grouped under category headings, each row a single link whose meta (type, size, wersja) sits INSIDE the link (DOCS-01, WCAG 2.1 AA)."
    - "Each document row shows a human-readable Polish name plus meta in the format {TYP} · {rozmiar} · wersja z {DD.MM.RRRR} (D-14)."
    - "Document type and size are computed at build via fs.statSync + file extension, never stored or typed by staff (D-14)."
    - "Category headings render in the fixed order Rekrutacja, Statut i uchwały, RODO; the RODO group is NOT emitted while it has zero documents (D-13 dormant category)."
    - "Every document link resolves to a real file under static/dokumenty/ and returns HTTP 200 (no 404)."
    - "The page has exactly one h1 ('Dokumenty') with correct heading order and passes axe with zero WCAG 2.1 AA violations."
    - "Document files keep their source format (D-17): DOC/DOCX for fillable recruitment forms, PDF for uchwały; the TYPE badge is the uppercased extension."
  artifacts:
    - "src/routes/dokumenty/+page.svelte (prerendered, zero-JS documents page)"
    - "src/routes/dokumenty/+page.server.ts (build-time load calling the shared resolver)"
    - "src/lib/server/dokumenty.ts (shared build resolver: glob + statSync meta + grouping, reused by the homepage in Plan 03)"
    - "src/lib/content/dokumenty/*.json (folder-collection seed entries, placeholder-flagged)"
    - "static/dokumenty/*.{pdf,doc,docx} (hosted document files, served verbatim)"
    - "tests/dokumenty.spec.ts (Playwright + axe acceptance)"
  key_links:
    - "import.meta.glob('$lib/content/dokumenty/*.json') reads the collection at build; entries grouped by kategoria select value"
    - "+page.server.ts statSync resolves `static${publicPath}` bytes + extension into the meta string consumed by +page.svelte"
    - "svelte.config.js KNOWN_FUTURE_ROUTES no longer lists /dokumenty so the crawler enforces the route"
  prohibitions:
    - statement: "The page must NOT display raw filenames as document names (Złota anti-lesson); only human-readable Polish names (D-16)."
      status: resolved
      verification: "Row name comes from the entry `nazwa` field; a spec assertion checks a known Polish name renders, and no `.doc`/`.pdf` filename string appears as a visible row label."
    - statement: "An empty category must NOT emit a heading or an empty panel (D-13)."
      status: resolved
      verification: "Spec asserts the 'RODO' heading has count 0 while no RODO entry exists."
    - statement: "Document meta must NOT sit outside the link (orphaned meta breaks screen-reader announcement)."
      status: resolved
      verification: "Spec asserts a getByRole('link') whose accessible name contains both the document name and its meta."
---

<objective>
Ship the visitor-facing /dokumenty page as a complete vertical slice: a parent can open /dokumenty, see documents grouped by category (Rekrutacja, Statut i uchwały), and download each one. Documents are hosted by us (D-16) so links never rot; type and size are computed at build (D-14); the RODO category stays dormant until Phase 4 (D-13).

Purpose: DOCS-01 (visitor can browse and download documents).
Output: /dokumenty route + build-time meta resolver, seeded folder collection of document entries, hosted seed files, Playwright + axe spec.
</objective>

<phase_goal>
Vertical MVP slice. Derived from the ROADMAP Phase 2 goal + success criteria:

**As a** parent, **I want to** browse and download the żłobek's documents (regulaminy, formularze, uchwały), **so that** I can complete enrolment and understand how the żłobek operates.
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-about-documents-cms/02-UI-SPEC.md
@.planning/phases/02-about-documents-cms/02-RESEARCH.md
@.planning/phases/02-about-documents-cms/02-PATTERNS.md
@.planning/phases/02-about-documents-cms/02-CONTEXT.md
@src/lib/components/Recruitment.svelte
@src/routes/+page.svelte
@svelte.config.js
</context>

<artifacts_this_phase_produces>
New symbols/files introduced by THIS plan (exclude from drift/source-grounding checks):
- Files: `src/routes/dokumenty/+page.svelte`, `src/routes/dokumenty/+page.server.ts`, `src/lib/content/dokumenty/*.json`, `static/dokumenty/*` files, `tests/dokumenty.spec.ts`
- Content shape (each entry): `{ nazwa, kategoria, plik, wersja, zrodlo_bip?, placeholder }` where `kategoria` is one of `rekrutacja | statut | rodo`
- Build resolver: `src/lib/server/dokumenty.ts` returns entries augmented with a computed `meta` string `{TYPE} · {size} · wersja z {DD.MM.RRRR}` plus a grouping helper; `+page.server.ts` load consumes it
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Failing Playwright + axe acceptance spec for /dokumenty (RED)</name>
  <files>tests/dokumenty.spec.ts</files>
  <read_first>
    - tests/home.spec.ts (doc-row + meta-inside-link assertion style, axe tags, header discipline)
    - tests/polityka-prywatnosci.spec.ts (route-stub skeleton)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (/dokumenty composition, category order, dormant-category rule, meta format, empty-state copy)
    - .planning/phases/02-about-documents-cms/02-RESEARCH.md (Validation Architecture test map for DOCS-01)
  </read_first>
  <action>
    Create tests/dokumenty.spec.ts in the tests/home.spec.ts style with a header comment citing DOCS-01 and the "do NOT weaken these assertions" clause. Assert: page.goto('/dokumenty') returns 200; exactly one h1 with text 'Dokumenty'; the category headings 'Rekrutacja' and 'Statut i uchwały' are visible via getByRole('heading'); the 'RODO' heading has count 0 (dormant category, D-13); at least one document row is a getByRole('link') whose accessible name contains both a known Polish document name and its meta substring 'wersja z' (meta INSIDE the link); for every document link, assert its href starts with '/dokumenty/' and a request to that href returns response status 200 (no 404). Add the axe block with tags ['wcag2a','wcag2aa','wcag21a','wcag21aa'] asserting violations equal []. Polish test names, no emoji, no em dashes.
  </action>
  <verify>
    <automated>test -f tests/dokumenty.spec.ts && npx playwright test tests/dokumenty.spec.ts; echo "expect RED until Task 3 builds the route"</automated>
  </verify>
  <acceptance_criteria>
    - tests/dokumenty.spec.ts exists with the DOCS-01 assertions above including the href-resolves-200 loop and the dormant-RODO check
    - Running the spec now exits non-zero (RED) because /dokumenty is not built
    - Header comment cites DOCS-01 and the lockstep clause
  </acceptance_criteria>
  <done>tests/dokumenty.spec.ts committed RED.</done>
</task>
<task type="auto">
  <name>Task 2: Seed document collection + hosted files + build-time meta resolver</name>
  <files>src/lib/content/dokumenty/rekrutacja-wniosek.json, src/lib/content/dokumenty/rekrutacja-regulamin.json, src/lib/content/dokumenty/statut-zlobka.json, static/dokumenty/wniosek-o-przyjecie-dziecka.doc, static/dokumenty/regulamin-rekrutacji.pdf, static/dokumenty/statut-zlobka.pdf, src/lib/server/dokumenty.ts, src/routes/dokumenty/+page.server.ts</files>
  <read_first>
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (Seed document names section: real BIP set; meta format; category values)
    - .planning/phases/02-about-documents-cms/02-CONTEXT.md (D-13, D-14, D-15, D-16, D-17; BIP inventory in Specific Ideas)
    - .planning/phases/02-about-documents-cms/02-RESEARCH.md (Code Examples: build-time document size/type via statSync)
    - src/routes/+layout.ts (prerender = true inheritance)
    - .claude/CLAUDE.md (copy rules; PLACEHOLDER convention)
  </read_first>
  <action>
    Seed the folder collection under src/lib/content/dokumenty/ with one JSON entry per document, shape { "nazwa", "kategoria", "plik", "wersja", "zrodlo_bip"?, "placeholder": true }. kategoria is the fixed select value 'rekrutacja' or 'statut' (never 'rodo' this phase — that group stays dormant, D-13). Seed a representative real BIP set (D-16): at minimum 'Wniosek o przyjęcie dziecka' (rekrutacja, DOC), 'Regulamin rekrutacji' (rekrutacja, PDF), and 'Statut żłobka (uchwała XXIII.133.2026)' (statut, PDF); include the additional załączniki and uchwały from the UI-SPEC Seed list if practical. nazwa is a human-readable Polish name (never a filename, D-16 anti-lesson). wersja is a date; recruitment docs use 02.04.2026 per the BIP inventory. plik is the served path under /dokumenty. Flag every entry placeholder: true (D-09, pending client confirmation).

    Host the corresponding real files under static/dokumenty/ (served verbatim at /dokumenty/<file>). Keep source formats (D-17): .doc/.docx for fillable recruitment forms, .pdf for uchwały/zarządzenia. If the actual BIP binaries are not available to the executor, create small valid placeholder files of the correct extension (a minimal valid PDF and a minimal DOC/DOCX) so links resolve 200 and statSync returns a real size; note them // PLACEHOLDER: so Phase 6 swaps the real binaries. The plik path in each JSON entry must exactly match a file that exists in static/dokumenty/.

    Create the shared build resolver src/lib/server/dokumenty.ts exporting (a) a function that reads all entries via import.meta.glob('$lib/content/dokumenty/*.json', { eager: true, import: 'default' }), computes each file's meta at build using node:fs statSync on `static${plik}` for bytes and the uppercased extension for TYPE (format bytes as KB or MB per the RESEARCH example), and returns the entries each augmented with a computed `meta` string `{TYPE} · {size} · wersja z {wersja}`; and (b) a helper that groups a list of entries by kategoria in the fixed order rekrutacja, statut, rodo with empty groups omitted. This module is server-only (statSync) and reused by both /dokumenty and the homepage (Plan 03), so keep it free of Svelte/UI concerns.

    Create src/routes/dokumenty/+page.server.ts: a load function that calls the shared resolver and returns the grouped entries. Keep prerender = true (inherited); this runs at build only.
  </action>
  <verify>
    <automated>npm run check && node -e "const fs=require('fs');const g=fs.readdirSync('src/lib/content/dokumenty');if(!g.length)throw new Error('no entries');g.forEach(f=>{const e=require('./src/lib/content/dokumenty/'+f);if(!fs.existsSync('static'+e.plik))throw new Error('missing file '+e.plik);if(!['rekrutacja','statut','rodo'].includes(e.kategoria))throw new Error('bad kategoria '+e.kategoria)});"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/content/dokumenty/ contains one JSON per document with nazwa (Polish, not a filename), kategoria in {rekrutacja,statut}, plik, wersja, placeholder:true
    - Every entry's plik resolves to an existing file under static/dokumenty/ (node check passes)
    - src/routes/dokumenty/+page.server.ts computes type+size via statSync and returns groups in fixed order with empty groups omitted
    - No 'rodo' entry exists this phase (dormant category)
    - npm run check exits 0
  </acceptance_criteria>
  <done>The collection and hosted files exist; the build resolver produces grouped entries with computed meta.</done>
</task>

<task type="auto">
  <name>Task 3: /dokumenty route composition (GREEN)</name>
  <files>src/routes/dokumenty/+page.svelte, svelte.config.js</files>
  <read_first>
    - src/lib/components/Recruitment.svelte (.doc-row / .doc-name / .doc-meta markup lines 43-55 and styles lines 240-270 — reuse the WCAG-correct meta-inside-link pattern)
    - src/routes/+page.svelte (Seo + composition; route adds no extra main/h1)
    - src/routes/dokumenty/+page.server.ts (the data this route consumes)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (/dokumenty composition, category groups as section aria-labelledby, empty-state panel copy, provenance link treatment)
    - .planning/phases/02-about-documents-cms/02-PATTERNS.md (dokumenty route pattern assignments)
  </read_first>
  <action>
    Create src/routes/dokumenty/+page.svelte, prerendered zero-JS, consuming the +page.server.ts load data. Emit <Seo title="Dokumenty: Żłobek Gminny w Stromcu" description="..." /> (Polish, noindex default). Single h1 'Dokumenty' plus the lead paragraph from the UI-SPEC copy. Render each non-empty category as a <section aria-labelledby> with an h2 (Rekrutacja, then Statut i uchwały, then RODO) — skip any group whose entry array is empty (D-13). Render each document as a single <a class="doc-row"> wrapping <span class="doc-name"> (the Polish nazwa) and <span class="doc-meta"> (the computed `{TYP} · {rozmiar} · wersja z {DD.MM.RRRR}` string) INSIDE the link, reusing the .doc-row/.doc-name/.doc-meta styles from Recruitment.svelte (min-height 48px, brand-blue underline, hover brand-blue-hover, meta 13px muted, middot separators). Optionally add a leading Lucide file icon (20px, aria-hidden) via @lucide/svelte (note: the installed package is @lucide/svelte, not lucide-svelte). Optionally render a per-document provenance link 'Źródło: BIP' only when zrodlo_bip is set, using the inherited new-tab treatment (target="_blank" rel="noopener noreferrer" + visually-hidden "(otwiera się w nowej karcie)" suffix). Add the whole-page empty-state panel (UI-SPEC copy 'Wkrótce udostępnimy dokumenty' / body) as a safety net for when the collection is empty. Tokens-only styling, container max-width 72rem, responsive padding, section alternation white/warm.

    Edit svelte.config.js: remove '/dokumenty' from KNOWN_FUTURE_ROUTES so the crawler enforces it. Leave the remaining future routes untouched.
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/dokumenty.spec.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/dokumenty/+page.svelte renders grouped category sections with the reused .doc-row meta-inside-link pattern
    - tests/dokumenty.spec.ts passes (GREEN): 200, single h1 'Dokumenty', Rekrutacja + Statut headings, dormant RODO count 0, every link resolves 200, meta inside link, axe == []
    - svelte.config.js KNOWN_FUTURE_ROUTES no longer contains '/dokumenty'; build prerenders /dokumenty with no crawler error
    - npm run check and npm run lint exit 0
  </acceptance_criteria>
  <done>A visitor can browse and download documents grouped by category; the spec is green and the site still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed document files -> visitor download | Staff-uploaded PDF/DOC/DOCX served from static/dokumenty/ |
| entry JSON -> build resolver -> prerendered HTML | kategoria/nazwa/plik/wersja become page content at build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0202-01 | Tampering | document files served from static/dokumenty | medium | mitigate | Files are inert downloads, not executed; X-Content-Type-Options: nosniff already set in _headers; max_file_size cap enforced on upload in Plan 04 config.yml |
| T-0202-02 | Spoofing / broken trust | mislabelled document (wrong nazwa vs file) | low | accept | Editors are trusted (repo write access); nazwa is human-authored; per-editor git audit trail (D-19) provides accountability |
| T-0202-03 | Denial of service | oversized committed file bloats build/repo | low | mitigate | Sveltia media max_file_size cap (Plan 04); document files are downloads, not image-optimized, so no build-time transform cost |

No high/critical threats in this plan; nothing crosses the block-on-high threshold.
</threat_model>

<verification>
- npm run check exits 0
- npx playwright test tests/dokumenty.spec.ts passes (DOCS-01, dormant category, meta-inside-link, links resolve 200, axe AA)
- npm run lint exits 0
- Full-suite sanity: npm run test stays green (no regressions on other routes)
</verification>

<success_criteria>
A parent can open /dokumenty, see documents grouped under Rekrutacja and Statut i uchwały (RODO hidden), read each document's type/size/wersja inside the link, and download every file with no 404, at zero WCAG 2.1 AA violations.
</success_criteria>

<output>
Create `.planning/phases/02-about-documents-cms/02-02-SUMMARY.md` when done.
</output>
