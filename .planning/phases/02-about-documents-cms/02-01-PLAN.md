---
phase: 02-about-documents-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - svelte.config.js
  - src/lib/content/site.ts
  - src/lib/content/day-plan.json
  - src/lib/content/o-nas.json
  - src/lib/components/DayPlan.svelte
  - src/routes/o-nas/+page.svelte
  - src/lib/assets/uploads/sala-zabaw.jpg
  - src/lib/assets/uploads/plac-zabaw.jpg
  - tests/o-nas.spec.ts
autonomous: true
requirements: [ABOUT-01]
must_haves:
  truths:
    - "A visitor navigating to /o-nas gets a 200 and reads Misja, Wartości, Plan dnia, Kadra, and Nasze miejsce (facility) sections (ABOUT-01, D-01)."
    - "The /o-nas page has exactly one h1 ('O nas') and correct heading order h1 -> h2 -> h3 (no skips)."
    - "The plan-dnia rows rendered on the homepage and on /o-nas are byte-identical because both read the single migrated source day-plan.json (D-03)."
    - "Kadra renders a collective warm narrative plus headcount by role (opiekunki, personel pomocniczy) with NO individual profiles and NO staff photos (D-02)."
    - "Facility images render through <enhanced:img> with responsive AVIF/WebP srcset and explicit width/height, each carrying an informative Polish alt describing the environment only (D-04, D-07)."
    - "Narrative fields (misja, kadra_opis, obiekt_opis) render only paragraphs, bold, and links via marked (D-08); no headings, images, tables, or raw HTML reach the DOM."
    - "/o-nas passes axe with zero WCAG 2.1 AA violations (SITE-04 baseline)."
    - "The homepage still renders the 'Nasz dzień w żłobku' DayPlan heading after DayPlan is re-sourced (no home.spec.ts regression)."
  artifacts:
    - "src/lib/content/day-plan.json (migrated dayPlan, shared source, D-03)"
    - "src/lib/content/o-nas.json (strict-schema O nas singleton content, D-05)"
    - "src/routes/o-nas/+page.svelte (prerendered, zero-JS O nas route)"
    - "src/lib/assets/uploads/*.jpg (placeholder facility images, environment-only)"
    - "tests/o-nas.spec.ts (Playwright + axe acceptance)"
  key_links:
    - "DayPlan.svelte import re-pointed from site.ts dayPlan to day-plan.json rows"
    - "vite.config.ts registers enhancedImages() BEFORE sveltekit() so uploads under src/ are Vite-processed"
    - "svelte.config.js KNOWN_FUTURE_ROUTES no longer lists /o-nas so the prerender crawler enforces the route"
    - "o-nas.json obiekt_zdjecia[].plik filenames match files under src/lib/assets/uploads/ for the import.meta.glob lookup"
  prohibitions:
    - statement: "Narrative markdown fields must NOT emit headings, inline images, tables, or raw HTML into the prerendered page (D-08)."
      status: resolved
      verification: "marked.parseInline used for single-paragraph fields; axe + a DOM assertion that /o-nas main contains no h4/h5/h6 injected by narrative content."
    - statement: "The O nas page must NOT show any human faces or identifiable people in facility imagery (D-04, wizerunek consent)."
      status: resolved
      verification: "Seed images are environment-only placeholders flagged // PLACEHOLDER:; alt text describes environment, never people."
    - statement: "site.ts coreMessage must NOT be altered or moved into the CMS layer (D-06, D-11)."
      status: resolved
      verification: "grep confirms coreMessage constant still present in site.ts unchanged; home.spec full-message exact-match test stays green."
---

<objective>
Ship the visitor-facing /o-nas page as a complete vertical slice: a parent can open /o-nas and read the żłobek's Misja, Wartości, Plan dnia, Kadra, and Nasze miejsce sections. This plan also lands the two foundations the rest of the phase builds on: the content-layer migration out of site.ts (the shared plan-dnia dataset, D-03) and the build-time image pipeline (@sveltejs/enhanced-img + marked, D-07/D-08).

Purpose: ABOUT-01 (visitor can read O nas) and the content/image plumbing every later plan depends on.
Output: /o-nas route, day-plan.json + o-nas.json content, enhanced-img + marked wired, DayPlan re-sourced, seed placeholder facility images, Playwright + axe spec.
</objective>

<phase_goal>
This phase is planned as vertical MVP slices. Derived from the ROADMAP Phase 2 goal + success criteria:

**As a** parent visiting the site, **I want to** read the O nas page (misja, wartości, plan dnia, kadra, miejsce), **so that** I can decide whether this żłobek is right for my child.

(The parallel staff-editor story — edit this content via the CMS — is delivered by Plans 04-05.)
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-about-documents-cms/02-UI-SPEC.md
@.planning/phases/02-about-documents-cms/02-RESEARCH.md
@.planning/phases/02-about-documents-cms/02-PATTERNS.md
@.planning/phases/02-about-documents-cms/02-CONTEXT.md
@src/lib/content/site.ts
@src/lib/components/DayPlan.svelte
@src/routes/+page.svelte
@src/routes/deklaracja-dostepnosci/+page.svelte
@src/lib/components/Seo.svelte
@vite.config.ts
@svelte.config.js
</context>

<artifacts_this_phase_produces>
New symbols/files introduced by THIS plan (exclude from drift/source-grounding checks):
- Files: `src/lib/content/day-plan.json`, `src/lib/content/o-nas.json`, `src/routes/o-nas/+page.svelte`, `src/lib/assets/uploads/sala-zabaw.jpg`, `src/lib/assets/uploads/plac-zabaw.jpg`, `tests/o-nas.spec.ts`
- Build tooling: `enhancedImages()` plugin registration in `vite.config.ts`; `marked` import in the O nas route
- Content shapes: `day-plan.json` = `{ placeholder: boolean, rows: [{ time, what }] }`; `o-nas.json` = `{ placeholder, lead, misja, wartosci: [{ tytul, opis }], kadra_opis, kadra_opiekunki, kadra_personel, obiekt_opis, obiekt_zdjecia: [{ plik, alt }] }`
- `DayPlan.svelte` now consumes `dayPlan.rows` (shape change from a bare array)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Failing Playwright + axe acceptance spec for /o-nas (RED)</name>
  <files>tests/o-nas.spec.ts</files>
  <read_first>
    - tests/home.spec.ts (assertion style, axe tags, header-comment discipline lines 4-14)
    - tests/polityka-prywatnosci.spec.ts (route-stub skeleton: 200, single h1, axe)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (section order + Polish copy table for /o-nas)
    - .planning/phases/02-about-documents-cms/02-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map)
  </read_first>
  <action>
    Create tests/o-nas.spec.ts following the tests/home.spec.ts style (describe block, header comment citing ABOUT-01 + D-01/D-02/D-03 + the "do NOT weaken these assertions except in lockstep with an approved UI-SPEC change" clause). Assert: page.goto('/o-nas') returns status 200; exactly one h1 with text 'O nas'; the section headings 'Nasza misja', 'Nasze wartości', 'Nasz dzień w żłobku', 'Nasza kadra', 'Nasze miejsce' are each visible via getByRole('heading'); the Kadra headcount labels 'opiekunki' and 'personel pomocniczy' are visible; every facility image (locator 'section img' within the Nasze miejsce section, or a scoped selector) has a non-empty alt attribute. Add a D-03 shared-source assertion: collect the DayPlan row time+what text on '/' and on '/o-nas' and assert the arrays are deeply equal. Add a D-08 assertion: the main content contains zero h4/h5/h6 headings injected by narrative fields. Add the axe block with tags ['wcag2a','wcag2aa','wcag21a','wcag21aa'] asserting results.violations equals empty array. Use Polish test names. No emoji, no em dashes in test names or comments (copy rules).
  </action>
  <verify>
    <automated>test -f tests/o-nas.spec.ts && npx playwright test tests/o-nas.spec.ts; echo "expect RED (non-zero) until Task 3 builds the route"</automated>
  </verify>
  <acceptance_criteria>
    - tests/o-nas.spec.ts exists and imports { test, expect } from '@playwright/test' and AxeBuilder from '@axe-core/playwright'
    - The spec asserts a 200, a single h1 'O nas', the five section headings, the two kadra labels, non-empty facility alt, the D-03 shared-source deep-equality, the D-08 no-block-heading check, and axe violations equal to []
    - Running the spec now exits non-zero (RED) because /o-nas is not built yet
    - Header comment cites ABOUT-01 and the lockstep clause
  </acceptance_criteria>
  <done>tests/o-nas.spec.ts committed RED (fails only because the route and content do not exist yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content-layer migration + build-time image pipeline</name>
  <files>package.json, vite.config.ts, src/lib/content/day-plan.json, src/lib/content/o-nas.json, src/lib/components/DayPlan.svelte, src/lib/content/site.ts, src/lib/assets/uploads/sala-zabaw.jpg, src/lib/assets/uploads/plac-zabaw.jpg</files>
  <behavior>
    - After migration, importing day-plan.json yields { placeholder, rows: [...] } with the 7 existing schedule rows verbatim (same time/what strings).
    - DayPlan.svelte renders identically on the homepage (home.spec 'day plan panel' stays green).
    - o-nas.json parses and exposes the strict D-05 field set with placeholder: true.
    - vite build runs enhanced-img over the seed images without error.
  </behavior>
  <read_first>
    - src/lib/content/site.ts (dayPlan lines 100-109 to migrate; coreMessage lines 10-11 and recruitment lines 111-170 must stay UNTOUCHED per D-06/D-11)
    - src/lib/components/DayPlan.svelte (import line 6 + each-block line 14 to re-source)
    - vite.config.ts (current plugins array line 7)
    - .planning/phases/02-about-documents-cms/02-RESEARCH.md (Standard Stack install block; Pattern 2 enhanced-img glob; Architecture project-structure additions)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (Copywriting Contract: /o-nas Polish placeholder copy; CMS field keys table)
    - .planning/phases/02-about-documents-cms/02-CONTEXT.md (D-02, D-03, D-04, D-05, D-06, D-08, D-09, D-11)
    - .claude/CLAUDE.md (copy rules: no emoji, no em dashes; en dash only in numeric ranges)
  </read_first>
  <action>
    Install pinned deps: `npm i -D @sveltejs/enhanced-img@0.11.0` and `npm i marked@18.0.9` (exact versions per RESEARCH; keep them pinned in package-lock.json). Sharp is pulled transitively by enhanced-img; if the CI build needs an explicit binary, add `@sveltejs/enhanced-img` first and only add sharp if the build reports a missing native binary. Edit vite.config.ts to import enhancedImages from '@sveltejs/enhanced-img' and place enhancedImages() BEFORE sveltekit() in the plugins array (result order: tailwindcss(), enhancedImages(), sveltekit()).

    Create src/lib/content/day-plan.json with shape { "placeholder": true, "rows": [ ... ] } moving the 7 dayPlan rows from site.ts verbatim (each { "time", "what" }). Remove the dayPlan export from site.ts and its PLACEHOLDER comment (leave everything else in site.ts unchanged — do NOT touch coreMessage, recruitment, contact, keyFacts, perks, posts per D-06). Re-point src/lib/components/DayPlan.svelte: change the import to `import dayPlan from '$lib/content/day-plan.json';` and update the each-block to iterate `dayPlan.rows` (key still row.time). No template/style changes otherwise.

    Create src/lib/content/o-nas.json with the strict D-05 schema and placeholder: true. Fields and Polish placeholder copy come from the UI-SPEC /o-nas Copywriting Contract: lead (the page-lead sentence), misja (markdown string, paragraphs+bold+links only), wartosci as a list of { tytul, opis } seeded with Bezpieczeństwo / Radość i zabawa / Indywidualny rozwój / Rodzinna atmosfera plus short opis each, kadra_opis (markdown collective text, no names, no photos per D-02), kadra_opiekunki (number), kadra_personel (number), obiekt_opis (markdown), obiekt_zdjecia as a list of { plik, alt } referencing the two seed filenames sala-zabaw.jpg and plac-zabaw.jpg with Polish environment-only alt (for example a play-room description and a playground description), and placeholder: true. Enforce copy rules in all authored strings: no emoji, no em dashes, en dash only inside numeric ranges.

    Seed two placeholder facility images at src/lib/assets/uploads/sala-zabaw.jpg and src/lib/assets/uploads/plac-zabaw.jpg. They are placeholders (D-04): environment-only, zero identifiable people. A neutral generated placeholder is acceptable (for example a solid brand-tint JPEG produced with the available sharp binary, or a converted copy of an existing neutral static asset). They must be real decodable JPEGs so enhanced-img can process them at build. Mark them with a nearby // PLACEHOLDER: note in o-nas.json or a colocated README so the Phase 6 launch gate can grep them.
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/home.spec.ts -g "day plan"</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists @sveltejs/enhanced-img and marked at the pinned versions; package-lock.json updated
    - vite.config.ts plugins array is exactly tailwindcss(), enhancedImages(), sveltekit() in that order
    - src/lib/content/day-plan.json exists with a rows array of 7 { time, what } entries matching the former site.ts dayPlan verbatim
    - site.ts no longer exports dayPlan; grep confirms coreMessage and recruitment.docs are still present and unchanged
    - DayPlan.svelte imports day-plan.json and iterates dayPlan.rows; `npx playwright test tests/home.spec.ts -g "day plan"` passes
    - src/lib/content/o-nas.json parses and contains lead, misja, wartosci (list of {tytul,opis}), kadra_opis, kadra_opiekunki, kadra_personel, obiekt_opis, obiekt_zdjecia (list of {plik,alt}), placeholder:true
    - src/lib/assets/uploads/sala-zabaw.jpg and plac-zabaw.jpg are real decodable JPEGs referenced by o-nas.json obiekt_zdjecia[].plik
    - No em dashes or emoji appear in any authored JSON string
  </acceptance_criteria>
  <done>Content migrated, DayPlan re-sourced with the homepage still green, image pipeline wired, seed placeholder images and O nas content authored.</done>
</task>

<task type="auto">
  <name>Task 3: /o-nas route composition (GREEN)</name>
  <files>src/routes/o-nas/+page.svelte, svelte.config.js</files>
  <read_first>
    - src/routes/+page.svelte (Seo usage + composition; the route adds NO extra main/landmark and NO extra h1)
    - src/routes/deklaracja-dostepnosci/+page.svelte (self-contained sectioned page: container max-width 72rem, responsive padding-inline, tokens-only styling)
    - src/lib/components/DayPlan.svelte (reused verbatim on /o-nas per D-03)
    - src/lib/components/Recruitment.svelte (scoped-style + token patterns to mirror)
    - src/lib/content/o-nas.json (the content this route renders)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (Component Contracts: /o-nas section order 1-7, surface alternation, typography role map, accessibility contract)
    - .planning/phases/02-about-documents-cms/02-RESEARCH.md (Pattern 1 marked.parseInline; Pattern 2 enhanced-img import.meta.glob)
    - .planning/phases/02-about-documents-cms/02-PATTERNS.md (o-nas route pattern assignments)
  </read_first>
  <action>
    Create src/routes/o-nas/+page.svelte, prerendered and zero-JS (inherits prerender=true from +layout.ts — do NOT add +server.ts or prerender=false). Import onas from '$lib/content/o-nas.json', marked from 'marked', DayPlan from '$lib/components/DayPlan.svelte', and Seo. Render narrative fields with marked.parseInline (single-paragraph inline render so no block headings leak in — D-08); render markdown that may contain a link/bold only. Build-load facility images with import.meta.glob('$lib/assets/uploads/*.{jpg,jpeg,png,webp}', { query: { enhanced: true }, eager: true, import: 'default' }). Resolve each image by BASENAME so the lookup works whether o-nas.json stores a bare filename or a public path: build a map from each glob key's final path segment to its processed module, then look up by item.plik's final path segment. Render each with <enhanced:img src={pic} alt={item.alt} sizes="(min-width:768px) 50vw, 100vw" />. (Rationale: Plan 04's Sveltia config may store the image as a public path or a filename; basename resolution decouples the route from that choice.)

    Emit <Seo title="O nas: Żłobek Gminny w Stromcu" description="..." /> (Polish, noindex default true). Compose the seven sections in the UI-SPEC-locked order: (1) page header with the single h1 'O nas' and the lead paragraph; (2) Misja on warm surface with h2 'Nasza misja'; (3) Wartości with h2 'Nasze wartości' as a responsive 1->2->3 card grid; (4) Plan dnia = render <DayPlan /> verbatim (single shared source, D-03); (5) Kadra with h2 'Nasza kadra', collective narrative, and headcount as a plain <ul> or <dl> with value + label ('opiekunki', 'personel pomocniczy') — no individual profiles, no photos (D-02); (6) Nasze miejsce with h2 'Nasze miejsce', narrative, and the facility image grid (informative Polish alt on each, decorative flourishes aria-hidden); (7) optional inherited Cta 'Zapisz dziecko' -> /rekrutacja. Use accessible-tier tokens for all text/UI (var(--color-ink), var(--color-muted), var(--color-brand-blue)); expressive tints decorative only. Container max-width 72rem, responsive padding-inline 16/24/32px, section padding-block 48->64/80px, matching the DayPlan/deklaracja analogs. Correct heading order h1 -> h2 -> h3 with no skips.

    Edit svelte.config.js: remove '/o-nas' from KNOWN_FUTURE_ROUTES so the prerender crawler now enforces the route. Leave '/dokumenty' and the others in place (their routes do not exist yet).
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/o-nas.spec.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/o-nas/+page.svelte exists, renders the 7 sections in UI-SPEC order with a single h1 'O nas'
    - tests/o-nas.spec.ts now passes (GREEN): 200, headings, kadra labels, facility alt, D-03 shared-source deep-equality, D-08 no-block-heading, axe violations == []
    - Facility images emit <picture>/srcset markup (enhanced-img) with width/height in the built HTML
    - svelte.config.js KNOWN_FUTURE_ROUTES no longer contains '/o-nas'; build prerenders /o-nas with no crawler error
    - npm run check and npm run lint both exit 0
  </acceptance_criteria>
  <done>A visitor can open /o-nas and read all sections; the spec is green and the full suite still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff-authored markdown (later via CMS) -> prerendered /o-nas HTML | Narrative fields (misja, kadra_opis, obiekt_opis) become HTML at build via marked |
| npm registry -> build | New dependencies (@sveltejs/enhanced-img, marked, transitive sharp) enter the build graph |
| committed image bytes -> build pipeline | Facility uploads are processed by enhanced-img/sharp at build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0201-01 | Tampering | marked render of narrative fields | medium | mitigate | Use marked.parseInline for single-paragraph fields; site CSP script-src 'self' (svelte.config.js) blocks any injected inline script even if HTML leaks; D-08 constrains content to paragraphs/bold/links; markdown widget button constraint enforced in Plan 04 config.yml |
| T-0201-02 | Information disclosure | facility imagery | medium | mitigate | Seed images are environment-only placeholders (D-04); no faces/EXIF-people; alt describes environment only |
| T-0201-SC | Tampering (supply chain) | npm installs (enhanced-img, marked, sharp) | high | mitigate | Pin exact versions (enhanced-img 0.11.0, marked 18.0.9) in package-lock.json; marked SUS verdict is a version-recency false positive cleared by the researcher (63M downloads/wk, decade-old markedjs repo); enhanced-img is first-party @sveltejs; a supply-chain acknowledgement is surfaced in the end-of-phase human-check (Plan 05) |

Note (human_verify_mode = end-of-phase): the SUS-package acknowledgement is recorded as an end-of-phase human-check rather than a blocking checkpoint, consistent with the configured workflow.
</threat_model>

<verification>
- npm run check (types + svelte-check a11y) exits 0
- npx playwright test tests/o-nas.spec.ts passes (ABOUT-01, D-01/D-02/D-03/D-04/D-08, axe AA)
- npx playwright test tests/home.spec.ts passes (no DayPlan regression from the migration)
- npm run lint exits 0 (prettier + eslint)
</verification>

<success_criteria>
A parent can open /o-nas on the built site and read Misja, Wartości, Plan dnia, Kadra, and Nasze miejsce, with facility images optimized, narrative rich-text constrained, plan-dnia shared with the homepage, and zero WCAG 2.1 AA violations.
</success_criteria>

<output>
Create `.planning/phases/02-about-documents-cms/02-01-SUMMARY.md` when done.
</output>