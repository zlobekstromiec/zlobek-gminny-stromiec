---
phase: 02-about-documents-cms
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/lib/content/site.ts
  - src/lib/components/Recruitment.svelte
  - src/routes/+page.svelte
  - src/routes/+page.server.ts
  - tests/home.spec.ts
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "The homepage recruitment docs panel shows document names that match the real BIP set (D-18): the former enrolment-card label reads 'Wniosek o przyjęcie dziecka'."
    - "The homepage docs panel is sourced from the same dokumenty collection as /dokumenty, so its names, meta, and hrefs cannot drift from the documents page (D-18, single source)."
    - "Each homepage docs-panel row keeps its meta (type, size, wersja) INSIDE the link (WCAG 2.1 AA, unchanged from Phase 1)."
    - "The panel shows a curated subset plus a 'Zobacz wszystkie dokumenty' link to /dokumenty."
    - "The three documents that do not exist on BIP (Regulamin organizacyjny, Upoważnienie do odbioru dziecka, Oświadczenia RODO) no longer appear in the homepage docs panel (D-18)."
    - "tests/home.spec.ts is updated IN LOCKSTEP with the copy change and passes; the full suite is green."
  artifacts:
    - "src/routes/+page.server.ts (homepage build load supplying the curated docs subset)"
    - "Updated src/lib/components/Recruitment.svelte (docs via prop, plus the see-all link)"
    - "Updated tests/home.spec.ts (D-18 lockstep assertions)"
  key_links:
    - "src/routes/+page.server.ts imports the shared resolver from src/lib/server/dokumenty.ts (created in Plan 02) so the homepage and /dokumenty share one source"
    - "src/routes/+page.svelte passes the curated docs array to <Recruitment docs={...} />"
    - "site.ts recruitment object keeps heading/steps/infoCard but no longer hard-codes the docs array"
  prohibitions:
    - statement: "The home.spec.ts docs-panel assertions must NOT be weakened or deleted to make the suite pass; they change ONLY in lockstep with this approved D-18 UI-SPEC copy change."
      status: resolved
      verification: "home.spec.ts still asserts a specific doc count, the meta-inside-link structure, and the real BIP name; the change is documented as the approved D-18 amendment."
    - statement: "The homepage must NOT gain a second mailto or otherwise change the single-mailto contract while re-sourcing docs."
      status: resolved
      verification: "home.spec 'contact section owns the single mailto' assertion stays green."
---

<objective>
Realign the homepage recruitment docs panel to the real BIP document set (D-18) and re-source it from the shared dokumenty collection so it never drifts from /dokumenty. This is the highest-risk seam in the phase: tests/home.spec.ts currently hard-asserts the old count and the old enrolment-card name, so the test is updated in lockstep with the copy change (Pitfall 1) rather than weakened.

Purpose: DOCS-01 (documents surfaced consistently; homepage panel matches the real hosted set).
Output: homepage load supplying a curated docs subset from the shared resolver, Recruitment panel taking docs via prop plus a see-all link, site.ts docs array removed, home.spec.ts updated in lockstep.
</objective>

<phase_goal>
Vertical MVP slice (refinement). Derived from the ROADMAP Phase 2 goal:

**As a** parent on the homepage, **I want to** see the correct, real enrolment documents (matching what actually exists), **so that** I download the right forms and am never sent to a document that does not exist.
</phase_goal>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-about-documents-cms/02-UI-SPEC.md
@.planning/phases/02-about-documents-cms/02-CONTEXT.md
@.planning/phases/02-about-documents-cms/02-RESEARCH.md
@src/lib/content/site.ts
@src/lib/components/Recruitment.svelte
@src/routes/+page.svelte
@tests/home.spec.ts
</context>

<artifacts_this_phase_produces>
New/changed symbols introduced by THIS plan (exclude from drift/source-grounding checks):
- File: `src/routes/+page.server.ts` (new homepage build load)
- `Recruitment.svelte` gains a `docs` prop (previously read `recruitment.docs` from site.ts) and a 'Zobacz wszystkie dokumenty' link to /dokumenty
- `site.ts` `recruitment` object no longer carries the `docs` array (heading/steps/infoCard/deadline/body remain)
- Depends on `src/lib/server/dokumenty.ts` (created in Plan 02)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update home.spec.ts to the real BIP set (lockstep, RED)</name>
  <files>tests/home.spec.ts</files>
  <read_first>
    - tests/home.spec.ts (lines 93-100: the recruitment-module test asserting the doc count and the enrolment-card link name)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (Homepage Recruitment docs-panel realignment: rename target, drop list, see-all link; Test lockstep note)
    - .planning/phases/02-about-documents-cms/02-CONTEXT.md (D-18 rationale + the three docs that do not exist on BIP)
    - .planning/phases/02-about-documents-cms/02-RESEARCH.md (Pitfall 1: acceptance-test rewrite forced by D-18)
  </read_first>
  <action>
    Update the recruitment-module test in tests/home.spec.ts to reflect the real BIP set (this is the approved D-18 copy change that authorizes editing these assertions). Replace the old enrolment-card link-name expectation with a regex matching 'Wniosek o przyjęcie dziecka' followed by its meta (keep the meta-inside-the-link structure: the accessible name must contain both the document name and a meta token such as 'wersja z' or a TYPE badge). Update the '.doc-row' count assertion to the new curated homepage subset count (the exact count matches what Task 2 renders, for example 2 curated rows). Add an assertion that a 'Zobacz wszystkie dokumenty' link to /dokumenty is present. Keep the header-comment note explaining this is a lockstep change tied to the D-18 UI-SPEC amendment. Do not touch the other home.spec assertions (single mailto, key facts, perks, hero, core message).
  </action>
  <verify>
    <automated>npx playwright test tests/home.spec.ts -g "recruitment module"; echo "expect RED until Task 2 realigns site.ts + Recruitment"</automated>
  </verify>
  <acceptance_criteria>
    - tests/home.spec.ts recruitment-module test asserts the 'Wniosek o przyjęcie dziecka' name with meta inside the link
    - The '.doc-row' count expectation matches the new curated homepage subset (not the old 6)
    - A 'Zobacz wszystkie dokumenty' -> /dokumenty link assertion is present
    - Running the test now exits non-zero (RED) because site.ts + Recruitment still render the old set
    - The single-mailto and other Phase 1 assertions are untouched
  </acceptance_criteria>
  <done>home.spec.ts updated to the real BIP expectations, RED against the current homepage.</done>
</task>

<task type="auto">
  <name>Task 2: Re-source the homepage docs panel from the shared collection (GREEN)</name>
  <files>src/routes/+page.server.ts, src/routes/+page.svelte, src/lib/components/Recruitment.svelte, src/lib/content/site.ts</files>
  <read_first>
    - src/lib/components/Recruitment.svelte (current docs each-block lines 43-55; import of recruitment from site.ts line 10)
    - src/routes/+page.svelte (composition; imports posts from site.ts; where to add the load data + pass the docs prop)
    - src/lib/server/dokumenty.ts (shared resolver created in Plan 02 — reuse it; do NOT duplicate statSync logic)
    - src/lib/content/site.ts (recruitment object lines 133-170: remove only the docs array, keep heading/deadline/body/infoCard/steps)
    - .planning/phases/02-about-documents-cms/02-UI-SPEC.md (curated subset guidance + see-all link + meta-inside-link)
  </read_first>
  <action>
    Create src/routes/+page.server.ts with a load that imports the shared resolver from src/lib/server/dokumenty.ts, and returns a curated docs subset for the homepage panel (for example the entries whose kategoria is rekrutacja and whose nazwa is 'Wniosek o przyjęcie dziecka' or 'Regulamin rekrutacji' — pick a stable selection so names match the real BIP set). Each returned row must carry name (the Polish nazwa), the computed meta string (type, size, wersja), and href (the file path under /dokumenty), so the panel links the real hosted file with real meta (D-18). Keep prerender = true (inherited).

    Edit src/routes/+page.svelte to consume the load `data`, keep the existing posts/showNews logic, and pass the curated docs to the recruitment module as a prop: <Recruitment docs={data.docs} />.

    Edit src/lib/components/Recruitment.svelte: accept a `docs` prop (Svelte 5 runes: $props) instead of reading recruitment.docs; keep importing the other recruitment strings from site.ts. Render the docs rows with the existing .doc-row/.doc-name/.doc-meta markup (meta INSIDE the link, unchanged). Below the list add a 'Zobacz wszystkie dokumenty' link to /dokumenty (inherited link styling, brand-blue underline). The primary Cta 'Pobierz kartę zgłoszenia' text may stay or be updated to match the new naming at planner/executor discretion, but must not introduce a second mailto.

    Edit src/lib/content/site.ts: remove the recruitment.docs array (the panel is now collection-sourced, D-18) while keeping heading, deadline, body, infoCard, and steps intact. Update the PLACEHOLDER comment above the former docs array to note the panel now reads the dokumenty collection. Do NOT touch coreMessage, contact, keyFacts, perks, posts, or recruitmentOpen.
  </action>
  <verify>
    <automated>npm run check && npx playwright test tests/home.spec.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/+page.server.ts returns a curated docs array (name + meta + href) via the shared resolver from src/lib/server/dokumenty.ts
    - Recruitment.svelte renders docs from the prop with meta inside each link and a 'Zobacz wszystkie dokumenty' -> /dokumenty link
    - site.ts recruitment no longer contains a docs array; heading/deadline/body/infoCard/steps remain; coreMessage untouched
    - tests/home.spec.ts passes (GREEN), including the updated recruitment-module test and the untouched single-mailto/core-message assertions
    - npm run check and npm run lint exit 0
  </acceptance_criteria>
  <done>The homepage docs panel shows the real BIP names/meta from the shared collection with a see-all link; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dokumenty collection -> homepage panel (build) | Homepage now derives docs rows from the shared build resolver |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0203-01 | Tampering | homepage docs panel content | low | accept | Same trusted collection as /dokumenty; single source removes name/href drift; no new untrusted input path |
| T-0203-02 | Repudiation | mismatched doc naming across pages | low | mitigate | Both pages read one resolver (src/lib/server/dokumenty.ts), so names/meta cannot diverge |

No high/critical threats; nothing crosses the block-on-high threshold. This plan changes copy and data-sourcing only, no new external surface.
</threat_model>

<verification>
- npx playwright test tests/home.spec.ts -g "recruitment module" is RED after Task 1, GREEN after Task 2
- npm run check exits 0
- npm run test (full suite) is green, including tests/o-nas.spec.ts and tests/dokumenty.spec.ts from Plans 01-02
- npm run lint exits 0
</verification>

<success_criteria>
The homepage recruitment panel shows the real BIP document names with real type/size/wersja meta inside each link, sourced from the same collection as /dokumenty, plus a link to see all documents; the acceptance suite is updated in lockstep and fully green.
</success_criteria>

<output>
Create `.planning/phases/02-about-documents-cms/02-03-SUMMARY.md` when done.
</output>
